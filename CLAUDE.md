# kbeauty-trade-os — 프로젝트 작업 수칙

## 사양서 (진실의 위치)
- 이 프로젝트의 유일한 사양서는 **`DESIGN.md`**다. 어떤 기능이든 구현 전에 해당 절을 읽고 그대로 따른다. 추측 구현 금지.
- **`DESIGN.md`의 §17(구현 안전 계약)·§18(크로스커팅 계약)·§22(DoD 11렌즈)는 모든 세션·모든 작업의 공통 계약**이다 — 매 작업 시작 시 해당 여부를 확인한다.
- 설계와 코드(또는 사용자 요청)가 충돌하면 임의로 판단하지 말고 멈춰서 보고한다. 설계 변경은 DESIGN.md 갱신 + `docs/adr/`에 ADR 5줄이 세트다.

## 작업 방식
- 반말 사용, 호칭은 "영준이". 결론·계획 먼저 제시하고 합의 후 실행.
- 작은 단위 구현 → 즉시 실행·테스트 → 다음 단계. 큰 덩어리 일괄 생성 금지.
- "된다"는 실행 확인 후에만 말한다. 실행 못 했으면 "실행 검증 못 했음"이라고 명시.
- 완료 선언 전: DESIGN.md §22의 11렌즈 체크 → 테스트 그룹(A~K)에 케이스 추가 → 전체 테스트 통과 확인. 렌즈 미통과분은 조용히 넘기지 말고 부채로 기록.
- 영준이는 비개발자다 — 영준이가 직접 해야 하는 조작(사이트 접속, 버튼, 명령 실행)은 클릭 단위로 안내한다.

## 기술 규칙 요약 (상세·근거는 DESIGN.md §17·§18)
- 하나의 업무 동작 = 하나의 DB 트랜잭션. 트랜잭션 안 외부 호출 금지 — 알림·연동은 아웃박스(events) 경유.
- 멱등 키는 전부 DB UNIQUE(부분 인덱스 `WHERE deleted_at IS NULL`), 확정·생성 API는 idempotency key.
- stock_movements·확정 분개·audit_log는 UPDATE/DELETE 불가(DB 권한으로 강제).
- 채번 MAX+1 금지(시퀀스 행 잠금), 전 목록 API 페이지네이션 기본 50, 모든 엔드포인트 서버측 역할+소유권 검증.
- 금액=정수 최소단위+통화, 시각=UTC 저장·KST 표시, 스키마 변경은 마이그레이션 파일로만.
- 한국어 UI: `break-keep` 기본, 좁은 셀·헤더·협정명·국가명 `nowrap`, 숫자·기준값 가운데 정렬.

## 진행·커밋
- DESIGN.md §19의 Phase 순서로 진행. 현재 Phase와 다음 할 일을 세션 시작 시 확인.
- 체크포인트 커밋을 작게 자주. 커밋 메시지는 "무엇을 왜"가 드러나게.
- push마다 CI(전체 테스트+마이그레이션 드라이런)가 돌며, 실패 상태로 병합하지 않는다.
- **웹의 Merge 버튼 사용 금지. 병합은 항상 `scripts/merge-pr.sh <PR번호>`로만** — 이 스크립트가 CI green을 확인한 뒤에만 병합한다(GitHub Pro 미결제로 기계 차단 불가, 폴백 규율 — ADR-0011).
- **영준 안내용 병합 명령은 항상 PowerShell 전체 경로(`& "C:\Program Files\Git\bin\bash.exe" scripts/merge-pr.sh <번호>`)로 표기 — `bash` 단독 표기 금지**(이 PC의 PowerShell에서 `bash`는 WSL을 물어 오류가 난다. 2026-08-08 웹 세션 판정 수칙).
