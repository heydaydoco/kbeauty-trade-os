# PROGRESS

## 완료
- **S0-1 (리포 골격·인프라·CI) — 종결.** PR #1(`74aa173`).
- **S0-2 (공통 테이블·인증·채번·수직 슬라이스) — 종결.** PR #3(`c236163`) + PR #4(`2070504`).
- **S1-1 (제품 계층·DG·HS·세트·단가 이력·원가 마스킹·품목군) — 병합 완료·종결.** PR #5(`eecfa1f`) + PR #6(`1f6a573`).
- **S1-2 (성분·자재 BOM·라벨) — 병합 완료·종결.** PR #7(`8fccaa9`) + PR #8(`1f96f12`). 계획은 2026-07-30 웹 세션 조건부 승인(A~G 승인, H 축소 — GC v1.1 스크리닝 5케이스 한정·라벨 케이스 보류).

## 현재
- **S2-1 (시장·요건 템플릿 편집기) — 계획 조건부 승인(조건 12, 2026-08-07 웹 세션)·판정 반영 완료(`docs/plans/s2-1-plan.md` §0-1).** PR 2분할: **PR-1 `s2-1-markets`(#15) — 병합 완료(`dfcee0f`, origin/main 실측 2026-08-10)·완료 보고 판정 반영(§0-2: 사후 승인 2건[ADR-0032 전역 UNIQUE 구조 강제·downgrade=FK drop 완결 논거]·등재 보강 1건·증거 보완 라운드 기록[2026-08-08 — 증거 2종 누락 보완 제출, 이후 누락=보완 라운드 없이 반려 가능]·게이트 무장 계약 테스트 접수)**: 판정 반영 → 커버리지 게이트 94 별도 커밋(조건 10 — ADR-0031 채택·게이트 계약 K 테스트) → markets(코드 전역 UNIQUE — ADR-0032) → 코드 참조 FK 승격 3건+백필 MIG 2행(CA·US — 조건 3~6) → 시장 화면(공용 목록 계층) → DESIGN §2·[M2] 보강(조건 9) → passwords 케이스 3건(조건 10b — 미커버 4문=needs_rehash 전신, 케이스로 종결). 전체 pytest **770건 통과·커버리지 96.31%·게이트 94 활성 줄 실측**(로컬 14분 53초). / **PR-2 `s2-1-templates` — 구현 완료·push·병합 대기(영준 merge-pr.sh)**: 첫 커밋=§0-2 판정 반영 → 4테이블 마이그레이션(3d5723c069d1 — 확정 게이트 DB CHECK·자기참조 금지·금액 쌍, up·down·up 왕복·drift 0)+K 제약 → 서비스(3층 확정 게이트·확정 편집 잠금 409·순환 거부 BFS — 조건 1·11)+G·J·아키텍처(자동 확정 부재) → API 17개+C 골격(적용단위 5종 CRUD e2e — DoD)+GC-C8 골든 3건(양방향 — 조건 7)+K 권한(편집=CERT+관리자) → 편집 화면(드롭다운 전건 ?size=200·함정 ⑦ 셀 가드+vitest 회귀)+품목군 요건 세트 패널+금액 입력 사람 표기·서버 환산(credit_limit 선례 정합)+부채 #2 재확인 → GC v1.2(GC-C8 신설·GC-A5 휴면 등재). ADR-0033·0034·0035, DESIGN [M2] 보강(S2-1 PR-2). 전체 pytest **858건 통과·커버리지 96.40%·게이트 94 활성 줄 실측**(로컬 14분 10초, 기준선 770+신규 88), vitest 74건·typecheck 통과. 부채 #15 요건 몫 종결(잔존 두 갈래: 마일스톤 S3-2 / §4.8 자동 적용 S2-2 명시 이월).
- PR-2 실기동 관통(§22 렌즈 11 — 실 HTTP + 실 브라우저 Playwright): 스모크 관리자(s2pr2-smoke, id 9) CLI 생성 → 로그인 → 템플릿 등록 201(**KRW 1500000 사람 표기→최소단위 1500000 환산 실측**) → **근거 없는 확정 422** `REQUIREMENTS.TEMPLATE.EVIDENCE_REQUIRED`(사유 2필드·**초안 유지 실측** — GC-C8 거부) → PATCH 근거 입력 → **확정 200** → **같은 키 재클릭=동일 응답 재생(바이트 일치 cmp)** → **확정 후 편집 409** `CONFIRMED_LOCKED` → **초안 전환 200(근거 잔존 실측)** → **재확정 200**(조건 11 경로 왕복) → 확정 템플릿 체크리스트 추가 **409** → 초안 템플릿 체크리스트 2건(MSDS 참조·비참조) 201·순번순 → 선행요건 C→B 201·**B→C 깊이 2 순환 422**·**자기참조 422** `PREREQUISITE.CYCLE` → 품목군 요건 세트 추가 201·중복 422·목록 1건 → **events 28행 불변(발행 0 — 판정 ⑦ 실측)** → **scheduled_jobs 0행 재실측** → 실 브라우저 9/9(요건 템플릿 화면·**"1,500,000 KRW" 사람 표기**·한국어 라벨·상세 패널·품목군 세트 패널) → 스모크 계정 처분(선례 수단: 단일 트랜잭션 비활성+soft delete+세션 3행 폐기)·재로그인 **401** 실측. 관통 잔존물: requirement_templates 3행·체크리스트 2행·선행 2행·품목군 1행·세트 링크 1행(ZZ 선례 지위 — 처분은 반입 기준선 위생 시점).
- PR-1 실기동 관통(§22 렌즈 11 — 실 HTTP): 스모크 계정 생성 → 로그인 → 시장 등록 201(`ZZ`) → **같은 키 재요청=동일 응답 재생(바이트 일치)** → 중복 코드 422 안내 → 정식화 PATCH 200(version 1→2) → **낡은 version 409**(§17.2 지정 문구) → **미등록 시장(YY) 라벨 등록 422** `MARKETS.MARKET.NOT_REGISTERED`+선등록 안내(조건 6) → 목록 3건(CA·US·ZZ — Page 봉투) → CSV(BOM True·CRLF True·한글 헤더) → **events 28행 불변(이벤트 발행 0 — 판정 ⑦ 실측)** → **scheduled_jobs 0행 재실측** → 스모크 계정 처분(smoke-ci 선례 수단: 단일 트랜잭션 비활성+soft delete+세션 폐기)·로그인 **401** 실측. 실DB 백필 전량(조건 4): **markets MIG 2행 — CA·US**(이름=코드·note MIG 표식·검수 전).
- **S1-3 (거래처·문서보관소·엑셀 코어) — 종결(2026-08-06, PR #9·#10·#11 전건 병합).** 계획 2026-08-05 웹 세션 조건부 승인(조건 9건: ①여신한도 비마스킹 판정+금액 규약 K 편입 확인 ②이스케이프 ADR 보증 범위 명기 ③backfill 트림·정규화+자동 생성 파트너 전량 보고 ④파일 보안 4항(서버 파일명·정적 경로 밖·attachment 고정·크기 상한 테스트) ⑤컬럼 제거 4건 잔존 참조 0건 명기 ⑥document_types 시드 추적성 ⑦F 그룹 CI 수집 증거 ⑧SKU 왕복 스트레치 ⑨유지 항목 재확인).
- PR 3분할: **PR-1 `s1-3-partners`(#9) — 병합 완료·판정 승인.** / **PR-2 `s1-3-documents`(#10) — 병합 완료.** / **PR-3 `s1-3-excel-core`(#11)**(임포트 레지스트리·왕복 diff·스테이징·오류 행 리포트·CSV 3종·F 그룹 최초 도입) — **병합 완료(`6b355e4` — origin/main 실측 2026-08-06)·S1-3 종결.**
- **S1.5 — 계획 조건부 승인(조건 10, 2026-08-06 웹 세션)·판정 반영 완료(`docs/plans/s1-5-plan.md` §0-1).** PR-1 완료 보고 판정(등재 3건·PR-2 구조 확정)은 §0-2 — PR-2 첫 커밋=그 반영.
- **S1.5 PR-1 `s1-5-sku-roundtrip`(#12) — 병합 완료(`a227863`, 2026-08-06)·조건 8(SKU 왕복 스트레치) 이행 종결.** 왕복 양식 20칸=기록 가능 필드+ID(파생 표시 2칸 구조적 부재 — `/skus/roundtrip.csv` 분리 신설, 표시용 `export.csv` 21칸 유지), 제조사코드 교체+OEM 스테이징 검증+확정 재검증(verify_references 대칭), 종류·제품코드 왕복 불변(immutable_fields), CHECK 확장(a7c3e9f2d4b8 — 함정 ①·⑪ 준수, 정의문·downgrade 실측). 실기동 관통·증거 4종은 PR #12 본문. **완료 보고 등재 3건 판정(2026-08-06 웹 세션)**: 제품코드 소속 이전=왕복 불변 확정(수요 시 화면 단건 액션 별도 판정 — 관찰 항목 등재) / 품목군=왕복 제외 유지(트리거: 반입 중 일괄 배정 실수요 — 관찰 항목 등재) / 실DB PENDING 스테이징 id 6·7=반입 세션 시작 절차에서 내용 1줄 확인 후 파기(계획서 작업 2 — PR-2에서 비접촉).
- **S1.5 PR-2 `s1-5-list-paging`(#13) — 병합 완료(`dc26c05`, origin/main 실측 2026-08-07).** 범위(판정 ②·⑦): 공용 목록 계층 1회 구현(usePagedList+ListPager — 51건+ 회귀 포함) + pre-push 훅 ruff format --check 기계화(§0-2 ⑤ — 포맷 깨진 파일 push 거부 재현 실측) + compose-smoke PR-2 내 별도 커밋(§0-2 ④ — 부채 #5 종결) + 부채 #2 eslint 재확인(재이월). 적대적 리뷰(5렌즈·23에이전트): 발견 18 중 확정 13 전건 반영·기록(반박 성공 5).
- PR-2 실기동 관통(§22 렌즈 11 — 실 HTTP + 실 브라우저 Playwright): 로그인 → 거래처 목록(9건 — 한 쪽: 총계만 표시·이동 버튼 부재 실측) → 거래처 신규 220행 CSV 업로드(스테이징 13 — 신규 220·오류 0) → 검토 표 쪽 이동 **전체 220건·1/2쪽 → 다음 → 202~221행 실물 표시·다음 비활성 → 이전 복귀** → 다쪽 안내("확정은 …전체 220건에 반영") 실물 확인 → **파기 204**(soft delete 실측 — 확정 0건, 실DB 무오염: partners 9건 불변) → **PENDING 잔존 = id 6·7뿐**(`deleted_at IS NULL` 실측 — PR-1 보고와 일치, §0-2 ③ 문면 유효) → **scheduled_jobs 0행** 재실측. API 실측: rows?size=200 1쪽 200행·2쪽 20행(row_no 202~221).
- PR-3 실기동 관통(§22 렌즈 11, 실 HTTP): 로그인 → 거래처 2건 등록(`=HYPERLINK` 이름 포함) → **거래처 CSV 내보내기**(BOM True·CRLF True·1열 ID·이스케이프 `'=HYPERLINK`/`''홑따옴표` 실재) → 파일 수정(개명 1행+신규 1행+ID 훼손 1행) → **업로드 스테이징**(9행: 무변경 6·변경 1·신규 1·오류 1, 변경 행 diff `거래처명: 스모크 공급사 → 스모크 공급사 (개명)`, 오류 사유 "ID를 읽을 수 없습니다(oops)") → 같은 파일 재업로드 **422**(`IMPORTS.STAGING.DUPLICATE_PENDING`) → **확정 1클릭 200**(created 1·updated 1) → 같은 키 재클릭 = 동일 응답 재생 → 반영 실측(개명·신규 생성·훼손 행 미반영) → **재실행 diff 0**(8행 전부 무변경 — §20 J) → 표준 양식 헤더=내보내기 헤더 → 대기 스테이징 파기 204→404 → **자재 임포트**(공급사코드 SUPPLIER 검증 오류 행 → 확정 → FK 연결·CSV 코드 열 실측) → **CSV 3종**(라벨·규칙·BOM: BOM True·CRLF True·**BOM CSV 원가·통화 흔적 0**) → 20MiB+1 업로드 **413** → 헤더 불일치 **422** → **scheduled_jobs 0행** 재실측. **리뷰 수정 후 재관통**: 유형 전용 변경 스테이징 2건 → 선확정 200·후확정 **409**(`10행(다른 사용자가 먼저 수정함)` — version 증가 실증) / 자재 스테이징~확정 사이 공급사 유형 해제 → 확정 **409**(`6행(기본공급사가 더 이상 공급사 유형이 아님)` — 참조 재검증 실증).
- PR-2 실기동 관통(§22 렌즈 11, 실 HTTP): 로그인 → SET SKU·품목군 준비 → **MSDS 한글 파일명 업로드**(multipart 본문 파일 경유 — argv 함정 ⑤ 우회, sha256·크기 실측) → 같은 키 재생=동일 id → 다른 키 같은 파일 422(해시 중복) → .exe 422(`DOCUMENTS.FILE.TYPE_NOT_ALLOWED`) → **다운로드**(attachment+RFC5987 한글 파일명+octet-stream+nosniff, 바이트 왕복 일치) → 20MiB+1 업로드 **413** → SET SKU MSDS **422**(`DOCUMENTS.MSDS.SET_SKU_FORBIDDEN`)·SET SKU 인증서 201(가드 협소 자기검사) → 원산지 증빙(발급일 2026-03-15 → **보존기한 2031-03-15 자동**) → 삭제 시도 **409**(`DOCUMENTS.DOCUMENT.RETENTION_LOCKED`) → 잠금 없는 문서 삭제 204 → 라벨 아트웍 업로드 → 라벨 목록 documents 표시 → 품목군 서류 세트(추가 201·중복 422·목록 1건) → 문서 CSV(**BOM True·CRLF True·한글 헤더**) → **scheduled_jobs 0행** 재실측. 저장 볼륨 실측: 실물 2건 전부 서버 생성 hex 파일명, 재생 버림·초과 부분 파일 잔존 0.

- **S1.5 종결(2026-08-07 웹 세션 판정 — 오너 결정): 실데이터 반입 전체를 실사용 개시 시점으로 이월.** 재개 트리거 = 영준 "실사용 준비" 선언(그때 반입 세션 1부 절차 그대로 재개 — 정리 2건은 완료돼 제외). 사유: 완성 우선 — 지금 반입하면 실사용 시점에 낡은 데이터가 되어 재반입 필요. **범위 재정의**: S1.5 = PR-1(#12)·PR-2(#13) 완료 + 반입 이월(WBS S1.5 DoD의 이월 검산은 반입과 동반 이월). 종결 정리 실행분: 수칙 2건 등재(⑬·세션 기록) / PENDING id 6·7 내용 확인 후 파기·잔존 0건 재실측 / smoke-ci 처분(비활성+soft delete·세션 폐기·로그인 401 실측) / `pytest --cov` 실측+ADR-0031 초안(부채 #8 — 게이트 미도입) / scheduled_jobs 0행 재실측. 전문은 `docs/plans/s1-5-plan.md` §0-3.

## S1-2 실기동 관통 기록 (종결 — 보존)
- PR-1 실기동 관통(§22 렌즈 11, 실 HTTP): 로그인 → 브랜드·제품(한글 `--data-binary @file`) → 성분 4종 → 규칙 3건(`us`→`US` 정규화) → 전성분 4행(함량 미입력 1행 포함) → **스크리닝 US·EU**(금지 1 · 제한초과 1[함량 미입력 — 보수 분류 사유 표기] · 미등재 5 · 한도 이내 1 · 고정 문구 확인 · **판정 워딩 0건**) → **빈 전성분 422** `INGREDIENTS.FORMULA.EMPTY` → CSV(**BOM True·CRLF True·한글 무손실**) → **scheduled_jobs 0행** 실측.
- PR-2 실기동 관통(§22 렌즈 11, 실 HTTP): 자재 3종(한글·HS6·재고/로트 조합) → **로트만 켠 자재 422**(CHECK 실측) → 제품·SKU → BOM 3라인(**원산지 미지정→UNKNOWN** · 역내 KRW 1250 · 역외 USD 0.85→**85 최소단위**) → 라벨 2판(`us`→`US`, 판번 1 RETIRED·2 APPROVED+컷인일+검증 2체크) → **TRADE 시야 단가 노출** → **VIEWER 시야: `unit_cost`·`currency` 키 부재·행 3건 그대로·본문 원가 값 0건** → 제품 라벨 롤업 2건 → **다국어 라벨**(CA 영문 v1 201 · CA **불문 v1** 201[같은 판번·다른 언어] · CA 불문 v1 재등록 422) → 자재 CSV(**BOM True·CRLF True**, 원가 컬럼 없음) → **scheduled_jobs 0행** 재실측. (리뷰 수정 후 DB 초기화 상태에서 전건 재실행)

## S1-1 DoD (WBS) — 충족
| DoD | 상태 | 증거 |
|:--|:--|:--|
| 처방(제품)↔SKU 분리 구조 | ✅ | `skus.kind` + 양방향 CHECK, DB 레벨 거부 테스트 |
| 세트 유통기한 MIN 계산 함수 존재 (원장 연결은 P4) | ✅ | `catalog/sets.py::set_lot_expiry` + 단위 6건(빈 목록은 오류) |
| **HS 자동판정 기능이 없어야 함** (§1 비범위) | ✅ | 아키텍처 테스트 4종(함수명 AST·OpenAPI 경로·세율 숫자 컬럼 금지·검사기 자기검사) |
| 검증 K + 단가 이력 스냅샷 | ✅ | K 전반 + `price_at` 9건(그룹 A) |
| (추가) 조회 역할 원가 마스킹 §20 G | ✅ | 행 단위 비노출·404 + **로그 원가 0건** e2e |

**실기동 관통(§22 렌즈 11)** — 실제 HTTP로 전건 확인:
품목군 → 브랜드 → 제품(품목군 연결) → SKU(위험물 9속성+물류 5속성) → HS(`us`→`US`, `3304.99.0000`→`3304990000` 정규화) → 세트+구성(MIN 24개월) → 단가(USD 29.90→**2990센트**) → 목록 → CSV(**BOM True·CRLF True·22칸**) → **VIEWER 시야(PURCHASE 행 부재·원가 문자열 0건)** → scheduled_jobs **0행**.
한글 본문은 전부 `--data-binary @file`, 파이썬 코드도 heredoc(파이프)로 넘겼다.

## 첫 실행 방법 (영준이용)
```
docker compose up -d --build
docker compose run --rm api python -m app.cli create-admin --email <내이메일> --display-name "영준이"
# 비밀번호는 화면에서 입력받는다(셸 기록에 안 남는다). 12자 이상.
```
→ http://localhost:5173 에서 로그인. 화면: SKU / 제품(처방) / 브랜드 / 품목군, SKU를 누르면 상세(위험물·세트 구성·HS 세번·단가 이력).
pgAdmin이 필요하면 `docker compose --profile tools up -d pgadmin` → http://localhost:5050

## 부채 (렌즈 미통과·보류 — 조용한 누락 금지)
1. **병합 차단(DoD② 일부) — 미충족 확정**. GitHub Pro 미결제(ADR-0011). 폴백: pre-push 훅 + `merge-pr.sh` + 웹 Merge 버튼 금지. **재검토 트리거: 협업자 추가 또는 팀 배포**.
2. **프런트 eslint — 재이월(2026-08-10 S2-1 PR-2 재확인 — 프런트 세션 의무 이행)**. `typescript-eslint@latest` peer가 여전히 `typescript >=4.8.4 <6.1.0`인데 이 리포는 TS `7.0.2`(npm view 실측 — PR-1에 이어 2회째 동일). **재검토 트리거: typescript-eslint peer가 TS 7을 받아들일 때.** 다음 프런트 세션에서 재확인. tsc strict(typecheck)가 현 게이트.
3. ~~pgAdmin~~ — **종결**.
4. ~~담당자 라우팅 미배정~~ — **종결**(ADR-0012 / WBS v1.2).
5. ~~compose-smoke CI 잡~~ — **종결(S1.5 PR-2 — 판정 ⑦ 2026-08-06, PR-2 내 별도 커밋)**. `docker compose up` 실기동 회귀 잡 신설, 범위 최소 4항: 스택 기동 · **readyz 200 게이트**(healthz는 DB 없이도 200이라 부족 — DB 연결+마이그레이션 최신 포함) · 비인증 401 · PDF 업로드→다운로드 **바이트 왕복 1건**(브랜드→제품→SKU 3단 생성·멱등 키). **CI 추가 시간 실측**: 도입 전 run 3분50초 → 도입 후 3분48초 — compose-smoke 잡 1분25초가 backend 크리티컬 패스(3분31초)에 병렬 흡수돼 **벽시계 증가 0**, 소모 러너 분만 run당 약 +1.5분(분 단위 올림 +2분).
6. **감사 채널(원가 로그 분리)** — ADR-0008. S6(회계) 착수 시. **S1-1이 원가를 들이면서 로그 경로는 4겹으로 막았지만**, 원가가 사람이 쓴 자유 텍스트(메모)에 적히는 경우는 어떤 규칙으로도 못 막는다 — 그 영역이 이 부채다.
7. ~~J 그룹 아웃박스 케이스~~ — **종결**.
8. ~~커버리지 임계 게이트~~ — **종결(S2-1 PR-1 — 계획 판정 안건 6 승인·조건 10, 2026-08-07)**. backend CI pytest에 `--cov-fail-under=94` 도입(별도 커밋), ADR-0031 제안→**채택**, 게이트 제거·완화를 잡는 K 계약 테스트(`test_coverage_gate_is_armed`) 동반. 도입 시점 전체 실측 **96.31%**(5497문 중 203문 미실행·770케이스 통과)·게이트 활성 줄("Required test coverage of 94% reached") 로컬 실측. 보안 모듈 개별 임계는 **기각**(판정) — 대안 실측: passwords.py 미커버 4문=needs_rehash 전신(호출처 없는 재해시 통로), 동일 PR 케이스 3건으로 종결(조건 10b). 임계 조정은 ADR-0031 절차 재적용+계약 테스트 갱신 세트.
9. ~~프런트 브랜디드 금액 타입~~ — **종결**(S1-1 PR-2). `MinorAmount` 브랜디드 타입 + 표시 문자열은 `formatMoney` 단일 경로. 자릿수 출처는 서버 `/v1/system/currencies` 하나이고, 하드코딩 0건을 vitest가 지킨다.
10. ~~담당 일괄 이관 도구 미배정~~ — **종결**(ADR-0015 / WBS v1.3).
11. **아웃박스 디스패처 미구현** — events에 쌓이기만 하고 보내는 주체가 없다. **의도된 상태**(S2-3 알림 엔진). 그전까지 events는 단조 증가한다 — S2-3에서 디스패처와 함께 보존 정책도 정할 것.
12. **scheduled_jobs 비어 있음** — 배치 레지스트리 테이블만 있고 등록된 배치가 0건이다. **실행기(S2-3) 구현 전 행 삽입 금지 — 위반 커밋은 반려 대상.** S1-1 종료 시점 실측 0행.
13. ~~프런트 라우팅 없음~~ — **종결**(S1-1 PR-1). 6개 라우트 + 404, 운영 nginx SPA 폴백은 S0-1부터 있었다.
14. ~~[S1-3] 승격 대상 임시 문자열 4건~~ — **4/4 종결(S1-3 PR-2, ADR-0020 완제)**. PR-1: `manufacturer_name`→OEM FK·`default_supplier_name`→SUPPLIER FK. PR-2: `skus.msds_url`→documents(SKU×MSDS, LINK)·`labels.file_url`→documents(LABEL×LABEL_ARTWORK, LINK) — backfill(트림·MIG: 출처 표식)+downgrade 역복사, `set_has_no_dg` CHECK 수기 재정의(함정 ①)+정의문 실재 테스트, 문자열 컬럼 제거(잔존 참조 grep 0건). §7.7의 "MSDS 유효본 미첨부 차단"(P4)이 documents 유효기간으로 검증 가능해졌다.
15. **item_profiles 내용 연결 미배정분 — 요건 몫 종결(S2-1 PR-2, ADR-0035)**. 서류=S1-3 완료 / **요건=S2-1 PR-2 완료**(`item_profile_requirement_templates` + 품목군 화면 요건 세트 패널 — 편집=인증+관리자, 판정 ⑤). **잔존 두 갈래**: ① 마일스톤 연결=S3-2 ② §4.8 "신규 등록 시 자동 적용"=**S2-2 계획 보고로 명시 이월**(적용 결과물 certifications이 S2-2에 선다 — ADR-0021·0035). (ADR-0021)
16. ~~SKU 상세 단가 표 통화표 미도착 백지(`sku-detail.tsx:479`)~~ — **종결(S1-3 PR-1, 웹 세션 재개 승인 2026-08-05)**. 셀에서 `currencies.data === undefined`면 값을 만들지 않는 가드로 수정(제품 상세와 같은 패턴), vitest 회귀 테스트(`sku-detail-prices.test.tsx` — 통화 API 500에도 화면 생존) 추가.
17. ~~[S1-2 리뷰 검출] CSV 수식 인젝션 미방어~~ — **종결(S1-3: PR-1 내보내기 이스케이프 + PR-3 왕복 파서 역변환 — ADR-0027 전단사 완성)**. 통로는 여전히 둘뿐(`core/csv_export.escape_formula_cell` / `unescape_formula_cell`)이고, 전단사 왕복은 F 그룹이 고정한다(단위: 트리거 전수 escape∘unescape=항등 / e2e: 무수정 왕복 diff 0·이스케이프 값 보존).

## 마스킹 원장 (원가·마진 마스킹 대상의 발견·판정 기록 — 2026-07-30 신설)
> 신설 사유: 상시 수칙 "마스킹 신규 발견분은 구현 없이 원장 등재만, 재개는 웹 세션 판정"이 등재할 원장을 특정하지 않아 이 섹션을 만들었다. 발견분은 여기 등재하고, 구현은 웹 세션 판정 후에만.

1. **`product_boms.unit_cost`(자재 매입단가) — 판정 완료(2026-07-30) · 구현 완료(S1-2 PR-2).** 필드 부재 방식: VIEWER 응답에서 `unit_cost`·`currency` 필드 자체가 없다(응답 스키마 2종 분기 — `BomLineSummary` / `BomLineCostHiddenSummary`, 직렬화 후 삭제 없음). ADR-0024. 고정 장치: K 아키텍처 테스트 6건(두 스키마 차이가 정확히 원가 2필드·원가 없는 스키마에 금액성 이름 금지·판정 호출 1곳·역할 목록 재사용), G e2e 3건(정상·실패·PG DETAIL 경로 로그 0건). 원가 파생 합계·롤업은 이번 범위 밖이며 같은 부재 원칙을 ADR과 아키텍처 테스트가 예약해 둔다.
   - **실측 함정**: FastAPI가 `Page[A] | Page[B]` 반환 타입으로 응답 모델을 추론하면 **한 갈래로 재검증하며 원가 필드를 통째로 지웠다**. `response_model=None` + 명시적 스키마 선택으로 고정했다. 새 마스킹 엔드포인트에서 재발 지점이다.
2. **`partners.credit_limit`(여신한도) — 판정 완료(2026-08-05 웹 세션): 마스킹 비대상.** 근거: ADR-0018·0024의 보호 대상은 원가·마진이고, 여신한도는 수주 게이트(§7.10)가 소비하는 운영 필요 데이터다. 조건: ①금액 아키텍처 전면 준수(정수 최소단위+통화 쌍 CHECK, K 검사 자동 편입 확인) ②**재판정 트리거: 여신 게이트 도입 세션(S3-1) 또는 역할 세분화 시.** [ADR-0026]
3. **`requirement_templates.estimated_cost_amount`(인증 예상 비용) — 판정 완료(2026-08-07 웹 세션, S2-1 계획 판정 안건 4): 마스킹 비대상 · 구현 완료(S2-1 PR-2).** 근거: 여신한도 계보 — ADR-0018·0024의 보호 대상은 원가·마진이고, 인증 예상 비용은 요건 준비 판단이 소비하는 운영 데이터다. 조건 이행 실측: 금액 규약 전면 준수(BIGINT+CHAR(3) 쌍 CHECK+비음수+대문자 — credit_limit 선례 문면, 입력=사람 표기·서버 환산) / `_amount` 접미사로 금액 감지기 자동 편입·유니크 키 금지 스캔 통과(신규 4테이블 금액·민감 컬럼 유니크 0건). ②**재판정 트리거: 비용·레이트카드 엔진 편입 또는 역할 세분화.**

## 관찰 항목 (부채는 아니지만 지켜볼 것)
- **SKU 용량(volume) 컬럼 부재** — §4.1은 SKU를 "용량·세트" 단위로 정의하면서 컬럼 목록에는 용량을 넣지 않았다. 추측 구현 금지로 목록 그대로만 만들었고, 지금은 품명(`수분 세럼 30ml`)·바코드가 용량을 구분한다. **재판정 트리거: S3-3(서류)·S5-1(채널 리스팅)·§7.7 DG 게이트 중 먼저 도달하는 세션의 계획 보고 안건.** 표현 방식(값+단위 vs 표기 문자열)은 그때 소비자 요구 기준으로 정한다.
- ~~**`/products/:id` 상세 화면 부재**~~ — **재판정 완료(S1-2 계획 승인, 2026-07-30): 신설한다.** 성분·BOM·스크리닝이 제품에 붙어 빈 화면 사유가 소멸. 라벨 편집은 SKU 상세(데이터가 SKU×시장), 제품 상세에는 read-only 라벨 롤업 + BOM 섹션(§14 ③ 열거 밖 추가 — 승인 E에 포함). S1-2 PR-1에서 구현.
- **하드코딩 검사기의 주석 제거 한계** — 문자열 리터럴 안의 `//`·`/*`도 주석으로 보고 잘라 내므로 그 줄 뒷부분이 스캔에서 빠진다(**위반을 놓칠 수는 있고, 없는 위반을 만들지는 않는다**). 정확히 하려면 파서를 붙여야 하는데 이건 배포물이 아니라 개발기 가드라 위협 모델에 비해 과하다. 놓친 경우의 최종 방어는 "자릿수 출처가 서버 하나"라는 구조 자체다.
- **[S1-2] `ingredient_rules` 유일키 (성분, 국가) — 용도별 세분 불가** — 같은 성분×국가에 금지·제한 공존과 용도별(린스오프/리브온 등) 상이 한도를 담을 수 없다(모순 리포트 원천 차단이 우선 — 웹 세션 판정 2026-07-30). **재판정 트리거: 동일 성분×국가에 상이 한도 요구가 실데이터로 발생 시.**
- **[S1-2] 컷인 시 구라벨 재고 소진 리포트** — §4.5 명문이나 재고 원장이 없어 불가. **재판정 트리거: P4(재고 원장).**
- ~~**[S1-2] `labels`·`ingredient_rules`·`sku_hs_codes`의 country_code → markets FK 승격**~~ — **종결(S2-1 PR-1 — 계획 판정 안건 2·조건 3~6, 2026-08-07)**: 코드 컬럼 유지+markets.code 참조 FK(전역 UNIQUE — ADR-0032·ADR-0023 종착). 컬럼·유일키·CSV 불변, 신규 등록은 시장 선등록 전제(미등록 422 안내), 백필 MIG 2행(CA·US)은 반입 재개 시 MIG 검수 큐 합류.
- **[S2-1 PR-1 등재] markets 삭제·복원 액션 부재** — 시장 행의 soft delete·복원 엔드포인트가 없다(이번 범위=등록·목록·정식화). 판정 §0-1 ② 문면이 "코드 재사용=복원(명시 액션)"을 정본으로 정했으므로, 삭제가 생기는 순간 복원 액션이 세트다. **등재만, 구현 없음. 재판정 트리거: 시장 비활성 실수요 발생 또는 S2-4(신규 시장 위저드) 계획 보고.** **부기(2026-08-08 PR-1 완료 보고 판정 — s2-1-plan.md §0-2 ③)**: 삭제 설계 시 **soft delete된 시장의 선등록 가드 취급(라벨·규칙·세번 신규 등록에서 미등록 422로 볼 것인지) 판정을 포함**한다 — FK(전역 UNIQUE)는 soft delete 행도 참조 대상으로 유지하므로 서비스 가드의 판단 축이 별도로 필요하다.
- **[S2-1 PR-1 등재] 실기동 스모크 시장 `ZZ` 잔존** — 관통이 실DB에 남긴 행(코드 ZZ·이름 "S2-1 관통 스모크 정식화"). 삭제 경로가 없어(위 항목) 잔존한다 — SMK- 스모크 데이터 선례와 같은 지위. **처분(파기 또는 유지)은 반입 기준선 위생 시점(반입 재개 절차)에 MIG 검수와 함께 판정.**
- **[S2-1 PR-2 등재] requirement_templates 삭제·복원 액션 부재** — 템플릿 행의 soft delete·복원 엔드포인트가 없다(이번 범위=등록·편집·확정·전환. RETIRED 상태가 은퇴 표시를 맡는다). markets 등재 항목과 같은 계보 — 삭제가 생기면 복원·선행요건 참조 취급 판정이 세트다. **등재만, 구현 없음. 재판정 트리거: 실수 등록 정리 실수요 발생 또는 S2-4(T1 시드·위저드) 계획 보고.**
- **[S2-1 PR-2 등재] 요건 템플릿 목록 CSV 내보내기 부재** — §12.2 "전 목록 UTF-8 BOM 내보내기" 문면 대비 템플릿·체크리스트 목록 CSV가 없다(계획 승인 범위 밖이라 등재만 — markets는 PR-1 계획에 명시돼 구현됨). **재판정 트리거: S2-4(T1 시드 반입 수단 판정과 동석) 또는 실수요 발생 시.**
- **[S2-1 PR-2 등재] 실기동 관통 요건 데이터 잔존** — 관통이 실DB에 남긴 행(requirement_templates 3·체크리스트 2·선행 2·품목군 PRF-S2PR2·세트 링크 1 — US 축, 이름에 "(관통)" 표식). 삭제 경로가 없어(위 항목) 잔존한다 — 시장 ZZ·SMK- 선례와 같은 지위. **처분은 반입 기준선 위생 시점(반입 재개 절차)에 MIG 검수와 함께 판정.**
- **[S1-2] §4.4 "OEM 생산 발주 마일스톤 프로파일"** — PO는 S3-1·마일스톤은 S3-2라 이월(조용한 누락 방지 명시). **재판정 트리거: S3-1/S3-2 계획 보고.**
- ~~**[S1-2] 라벨·BOM·규칙 목록 CSV**~~ — **종결(S1-3 PR-3)**: `/labels`·`/ingredient-rules`·`/product-boms`의 `export.csv` 3종 신설. BOM CSV는 **원가 열 미포함**(웹 세션 승인 문면 — 전용 뷰에 원가 필드 자체가 없다, ADR-0024 부재 규율).
- **[S1-2] GC 라벨 케이스 보류** — GC v1.1은 스크리닝 5케이스 한정(웹 세션 판정 H 축소). 라벨 케이스는 보류·관찰 유지. **재판정 트리거: 라벨 소진 리포트(P4) 또는 라벨 관련 사고 시.**
- **[S1-3] soft delete된 문서의 실물 잔존·저장소 용량 모니터링** — 문서 soft delete는 행만 지우고 실물은 남긴다(복원 가능성·보존 정책이 실물 정리에 선행해야 한다 — ADR-0029). §18.4의 "첨부 스토리지 용량 모니터링+임계 알림"도 미구현. **재판정 트리거: S2-4(백업·복원 — files/ 세트 정책과 함께) 또는 S2-3(알림 엔진).**
- **[S1-3] 문서 유효기간 기일 스캔 부재** — §4.7 "유효기간 있는 모든 문서가 기일 엔진 스캔 대상"은 기일 엔진(S2-3)의 일이다. 이번 세션은 컬럼까지만 — scheduled_jobs 0행 유지(승인 유지 항목). **재판정 트리거: S2-3 계획 보고.**
- **[S1-3 PR-3 리뷰 검출] 거래처 유형 해제가 참조 마스터를 확인하지 않음** — 왕복 임포트가 유형 해제 경로를 처음 열었는데(그전엔 수정 API 자체가 없었다), SUPPLIER 유형을 해제할 때 그 거래처를 기본공급사로 참조 중인 자재가 있는지 보지 않는다(교차 테이블이라 DB CHECK 불가). 자재 **확정 쪽**은 확정 시점 재검증으로 닫았지만(verify_references — 같은 PR), 반대 방향은 §7.7 게이트 사상("차단 지점은 등록이 아니라 게이트")과 얽혀 임의 구현하지 않고 등재만 한다. **재판정 트리거: S3-1(소싱·발주가 공급사 유형을 소비) 또는 거래처 수정 화면 신설 시.**
- **[S1-3] 보존잠금 문서 정정 경로 부재** — 문서 메타 수정(PATCH)이 없고 보존기한 내 삭제는 409(`DOCUMENTS.DOCUMENT.RETENTION_LOCKED`)라, 잘못 등록된 보존잠금 문서(원산지 증빙)를 정정할 경로가 없다. 웹 세션 판정(2026-08-06)으로 **등재만, 구현 금지**. **재판정 트리거: 실 정정 수요 발생 또는 S2-1 요건 작업 시 웹 세션 재판정.**
- ~~**[S1-2 리뷰 검출] 목록 화면 50건 표시 한계**~~ — **종결(S1.5 PR-2 — 판정 ② 2026-08-06)**: 공용 목록 계층 1회 구현. `usePagedList`(쪽 상태+page 파라미터+필터 변경 시 1쪽 복귀+범위 이탈 보정) + `ListPager`(총계·쪽 이동)가 전 목록의 **유일한** 구현이고(화면별 개별 구현 금지), Page 봉투 표 목록 18곳 전부에 결선(비표 소형 목록 2곳 제외 — 아래 등재). 서버 계약 불변(§18.4 기본 50·최대 200 — 1쪽 요청 주소는 종전과 동일). 51건+ 회귀 vitest(`skus-paging.test.tsx` — 51건째 존재 가시·page=2 조회·경계 버튼) + 컴포넌트 케이스(`list-pager.test.tsx`). 임포트 검토 행의 "N건만 표시" 경고는 쪽 이동으로 대체(잘림 은닉 금지 의도는 "확정은 전체 반영" 안내로 존치). 드롭다운 `?size=200` 우회는 유지.
- **[S1.5 PR-2 등재] 드롭다운 4곳에 `?size=200` 우회 부재** — products 화면의 브랜드·품목군, skus 화면의 제품·품목군 드롭다운이 size 미지정(기본 50)이라 51건째부터 선택지가 조용히 빠진다(관찰 항목 구 문면 "드롭다운은 우회 적용"이 실측과 다름 — partners·ingredients·materials·document-types만 우회 적용 상태). 반입 수단 기본=화면(판정 ④)이라 브랜드·제품 50건 초과 시 SKU 화면 등록이 막히는 실장애 후보. **등재만, 구현 없음(승인 밖). 재판정 트리거: 반입 세션 시작 실측에서 브랜드·제품·품목군 중 50건 초과 확인 시 — 그 자리에서 웹 세션 판정.**
- **[S1.5 PR-1 등재] SINGLE 제품코드 변경(소속 이전) — 왕복 불변 확정(2026-08-06 웹 세션 판정)** — PR-1이 판정 침묵분이라 보수적으로 왕복 불변(immutable_fields)에 포함하고 재판정을 요청했던 건. 판정: 보수 잠금이 그대로 정본 — 왕복 편집으로는 영구 불허. **재판정 트리거: 소속 이전 실수요 발생 시 — 화면 단건 액션 신설로 별도 판정(왕복 완화 아님).**
- **[S1.5 PR-1 등재] SKU 품목군(item_profile_id) 왕복 제외 — 유지 확정(2026-08-06 웹 세션 판정)** — 기록 가능 필드지만 왕복 21칸 판정 스코프 밖이라 제외했던 건. 판정: 제외 유지. **재판정 트리거: 반입 중 일괄 배정 실수요 발생 시** — 트리거 도달 여부는 반입 세션 시작 절차의 "품목군 배정 필요 SKU 수 실측"(계획서 작업 2)으로 판정한다.
- **[S2-1 계획 판정 등재 — 조건 12] 요건 템플릿 믹스인-시드 상충 제약** — markets·requirement_templates가 믹스인 전부(Actor 포함)를 달고 서므로, S2-4 T1 템플릿 시드는 **마이그레이션 시드가 될 수 없다**(함정 ⑩ — ActorMixin의 users FK가 TRUNCATE CASCADE로 PRESERVED_TABLES를 무력화). 시드 투입 방식(앱 경로·임포트 등)은 **S2-4 계획 판정 안건으로 명기**. FK 승격 backfill이 만드는 markets 행은 시드가 아니라 MIG 계보(구분 유지 — 판정 ⑦ 문면).
- **[S1.5 종결 등재] 실데이터 반입 이월 + GC-A5 휴면(2026-08-07 웹 세션 판정 — 오너 결정)** — 반입 전체(계획서 작업 2·3·6의 실데이터 부분)를 실사용 개시 시점으로 이월. **재개 트리거: 영준 "실사용 준비" 선언 — 반입 세션 1부 절차 그대로 재개(정리 2건은 2026-08-07 완료라 제외).** GC-A5(v1.2)=**휴면** — 발동=반입 시점, 기대값 선고정(원천 파일 엔터티별 행수 실측·소급 조정 금지 — 판정 ⑥) 포함 동반 이월. 동반 이월 4건: ① MIG- 전량 검수·처분(실DB 실측 2026-08-07: 1행 — MIG-0001 한국유리(주). **S2-1 PR-1 추가분: markets MIG 계보 2행(CA·US — note 표식·이름=코드·정식화 전)도 이 검수 큐에 합류 — 판정 조건 4**) ② 품목군 배정 필요 SKU 수 실측(대장 기준) ③ 드롭다운 4곳 재판정(원천 50건 초과 실측 시 — 위 등재 항목) ④ 단계별 백업(판정 ⑨ — pg_dump+복원 실증·파일명 규약). 전문은 s1-5-plan.md §0-3.

## 주의 인계 (발견한 함정·결정)

### ① autogenerate는 **기존 테이블에 붙는** CHECK를 감지하지 못한다
신규 테이블의 CHECK는 `create_table`에 포함되어 감지되지만, 이미 있는 테이블에 CHECK를 추가하면 **초안에 한 줄도 안 나온다**. 그대로 썼으면 제약 없이 배포될 뻔했다(S1-1에서 두 번). 이 차이가 함정의 정확한 경계다.
→ **규칙**: 모델에 CHECK를 추가하면 마이그레이션에 손으로 옮겨 적고, "모델이 선언한 CHECK가 DB에 실재하는가" 테스트로 누락을 잡는다.

### ② 실패 경로에서 ORM 속성을 읽으면 원인이 뒤바뀐다
`flush()`가 실패한 뒤 세션은 롤백 대기 상태다. 그때 에러 메시지를 만들며 만료된 ORM 속성을 읽으면 새 쿼리가 나가고 `PendingRollbackError`가 난다 — 사용자에겐 "중복입니다" 대신 **500**이 가고, **로그의 원인도 진짜 원인(유니크 위반)이 아니라 롤백 오류로 바뀐다.**
→ **규칙**: 실패 경로에서 쓸 값은 `flush()` **전에** 평범한 변수로 빼 둔다. 기존 코드가 멀쩡했던 건 우연히 `payload` 딕셔너리만 읽었기 때문이다.
→ **S1-3 PR-3 재발(2026-08-06)**: 임포트 확정 루프의 `except IntegrityError`에서 `row.row_no`(ORM 속성)를 읽어 422 대신 PendingRollbackError 500이 났다 — 같은 규칙으로 해소. 루프 단위 반영처럼 flush가 반복되는 코드에서 특히 재발하기 쉽다.

### ③ pytest 두 개를 같은 test DB에 동시에 돌리면 정상 코드가 빨개진다
격리가 매 테스트 후 TRUNCATE라, 두 프로세스가 서로의 데이터를 지운다. 실패 모양이 기능 결함과 구분되지 않아 엉뚱한 곳을 파게 된다(실제로 5건이 그렇게 빨갰다).
→ **규칙**: 백그라운드 pytest가 살아 있는지 먼저 확인하고 돌린다. 로컬 전체 실행이 10분 제한을 넘으면 **폴더로 쪼개 순차 실행**한다(겹치지 않으므로 합이 곧 전체다).
→ **S1-2 재발(2026-07-30)**: 백그라운드 실행이 끝난 줄 알고 두 번째를 띄워 겹쳤다. 이번 증상은 데이터 소실이 아니라 **마이그레이션 전용 DB(`kbos_migr`) 충돌**이었다 — `test_migration_runs_as_owner_role`가 error로 났다(`507 passed, 1 error`). 기능 결함이 아니라 하네스 충돌이므로 그 결과는 증거로 쓰지 않는다.
→ **확인 명령**: `docker ps --filter "name=kbos-dev-api-run" -q | wc -l`이 0인지 보고 돌린다.

### ④ DB 예외에는 원가·비밀번호 해시가 두 경로로 실린다
- **SQLAlchemy의 `[parameters: {...}]`** — 우리가 보낸 바인드 값. 엔진에 `hide_parameters=True`로 막는다. **환경 분기·토글 없이 고정이고 호출자가 덮을 수도 없다**(§18.1에 환경 분기는 없다). `create_engine` 호출이 `build_engine` 한 곳뿐이라는 것도 아키텍처 테스트가 지킨다.
- **PostgreSQL의 `DETAIL: Failing row contains (…)`** — **서버가 만든 문자열에 행 전체 값**이라 엔진 설정으로는 안 막힌다. 마스킹 프로세서가 그 구간을 지운다.

**절단 규칙은 실측으로 세 번 졌다**: 괄호 짝(값 안의 괄호) → 줄 끝(PG가 값 안의 개행을 그대로 싣는다) → 꼬리표 앞(사용자가 메모에 `\n[SQL:`을 적으면 조기 종료). 최종형은 ⓐ꼬리표를 **마지막** 등장 위치로 찾고 ⓑ그래도 삭제 구간에 꼬리표가 보이면 **메시지 끝까지** 지운다.
→ **교훈: 부분 절단은 값 내용에 진다. 모호하면 전량 삭제(fail-closed)** — 문장을 잃더라도 값을 남기지 않는다.
→ **부기(미발화·구현 금지)**: 절단 앵커는 **래핑 예외의 꼬리표를 전제**한다. 원본 DB 예외(`e.orig` 등)를 직접 로깅하면 꼬리표가 없어 앵커를 잃는다. 현 앱 경로는 래핑 예외만 로깅하므로 발화하지 않는다. **수칙: 원본 DB 예외 직접 로깅 금지. 필요해지면 웹 세션 재개 판정.**
→ 재발 지점: 새 엔진 생성·테스트 하네스·S3-1 비용·S6-2 원가.

### ⑤ Windows Git Bash의 argv 인코딩 — 본문뿐 아니라 **코드**도
`curl -d '{"name":"한글"}'`처럼 인자로 넘긴 한글은 CP949로 깨진다. S1-1에서 같은 함정이 `python -c "print('한글')"`에서 재발했다 — argv면 무엇이든 깨진다.
→ **규칙**: 한글은 인자로 넘기지 않는다. 본문은 `--data-binary @file`, 파이썬 코드는 heredoc(파이프). 출력도 `PYTHONIOENCODING=utf-8`로 고정한다(안 하면 em-dash 하나에 죽는다).
→ heredoc으로 코드를 주면 **stdin을 차지**하므로 JSON을 파이프로 함께 줄 수 없다 — 응답도 파일 경유로 받는다.

### ⑥ 기타 (S1-1)
- **프런트 소스 스캔은 `node:fs`가 아니라 `import.meta.glob(?raw)`** — 타입(`@types/node` 미노출)과 컨테이너 바인드 마운트 IO(5초 타임아웃) 둘 다 피한다. 번들러가 이미 아는 목록을 쓴다.
- **검사기가 자기 설명을 잡는 일** — 하드코딩 검사가 `money.ts` 주석의 `1234 / 100` 예시에 걸렸다. 규칙을 지키려고 설명을 지우는 일이 생기면 그건 규칙이 잘못된 것이라, 주석을 걷어내고 코드만 검사한다.
- **jsdom은 `required` 미충족 시 폼 제출을 막는다** — 선택지가 도착하기 전에 select 값을 넣으면 반영되지 않아 제출 자체가 안 일어나고, 증상은 "POST가 없다"로 나타난다. 옵션을 기다린 뒤 값을 넣는다.
- **테스트 팩토리의 고정 코드값** — `create_product()`가 매번 같은 브랜드 코드를 쓰면 한 테스트가 제품을 둘 만드는 순간 준비 코드가 유니크에 걸리고, **그 실패가 기능 결함처럼 보인다**. 코드는 상위 코드에서 파생시킨다.

### ⑦ ListState의 로딩·오류 가드는 children을 못 막는다 (S1-2 PR-2 실측)
`<ListState isPending={…} error={…}>{table}</ListState>`에서 **children은 ListState가 그리지 않아도 이미 평가된다**(JSX 인자이므로). 그래서 표 안의 `.map()`이 실행되고, 그 안에서 던지는 함수(예: 통화표가 없을 때의 `formatMoney`)는 가드를 무시하고 렌더 중 예외를 낸다. 프런트에 ErrorBoundary가 없어(의도) 그 예외는 트리 언마운트 = **화면 백지**가 된다.
→ **규칙**: 던질 수 있는 표시 함수는 **셀에서** 전제를 확인하고 부른다(`currencies.data === undefined`면 값 대신 `—`). ListState 가드는 그 위에 얹는 보조다.
→ 같은 결함이 `sku-detail.tsx:479`(S1-1)에도 있다 — 부채 #16(승인 범위 밖 등재).

### ⑧ 응답 마스킹에서 FastAPI가 Union 응답 모델을 재검증한다 (S1-2 PR-2 실측)
`-> Page[A] | Page[B]`처럼 반환 타입으로 응답 모델을 추론시키면, FastAPI가 두 갈래 중 하나로 **다시 검증**하며 우리가 고른 스키마를 덮어쓴다 — 원가 **포함** 응답이 원가 없는 스키마로 재검증돼 `unit_cost`가 통째로 사라졌다(테스트가 잡았다).
→ **규칙**: 역할에 따라 응답 스키마가 갈리는 엔드포인트는 `response_model=None`을 명시하고, 우리가 만든 스키마 인스턴스를 그대로 반환한다(문서화는 `responses={200: {"model": …}}`). 재발 지점: S6-2 원가·S3-1 비용의 마스킹 엔드포인트.

### ⑩ 시드 테이블에 users FK가 있으면 PRESERVED_TABLES가 무력화된다 (S1-3 PR-2 실측)
테스트 정리는 더러워진 테이블을 `TRUNCATE … CASCADE`로 비우는데, CASCADE는 **그 테이블을 FK로 참조하는 모든 테이블**을 무조건 함께 비운다. 사용자 생성 테스트 하나가 users를 더럽히면, ActorMixin(created_by→users)을 단 시드 테이블이 통째로 비워진다 — PRESERVED_TABLES에 넣어 둔 것과 무관하게. 전체 실행에서만 45건이 빨개지고 파일 단독 실행은 초록이라 순서 의존처럼 보인다.
→ **규칙**: 마이그레이션 소유 시드 테이블(roles·document_types)은 **truncate 대상 테이블로의 FK를 갖지 않는다** — Version·Actor 믹스인을 달지 않는다(roles가 원형). 재발 지점: S2-1 요건 템플릿 시드, S3-4 FTA 시드.

### ⑪ 손으로 쓰는 `op.drop_constraint`에는 `op.f()`가 필수다 (S1-3 PR-2 실측)
naming convention이 걸린 리포에서 평문 이름을 주면 규약이 한 번 더 접두된다 — `drop_constraint("ck_skus_set_has_no_dg", …)`가 `ck_skus_ck_skus_set_has_no_dg`를 지우려다 실패했다. `op.f()`는 "이미 최종 이름"이라는 표식이다. create 쪽은 기존 마이그레이션들이 전부 op.f()를 쓰고 있어 drop에서만 새는 함정이다.

### ⑫ 커밋 메시지 수치는 증거가 아니다 (S1-3 PR-3, 2026-08-06)
ccc3736 제목 "F 그룹 최초 도입 — … 65케이스"의 65는 **F43+K19+B3 케이스 합**(S1.5 판정 조건 ⑧ 마커 실측: 왕복 e2e 20 + 파서 단위 23(parametrize 12값 확장 포함)=F43 / 레지스트리 6+통합 제약 13=K19 / CSV 3종=B3. 커밋 본문의 J 표기 3건·K 표기 4건은 왕복 파일 안에서 F 케이스에 겹친 복수 마커라 65 합계에 이중 계상하지 않는다 — K19 열아홉 건은 왕복 파일 밖 별도 파일분으로 전량 유효하다)인데 제목 문맥은 F 그룹이라, **제목 스코프(F 그룹)와 수치 스코프(신규 전체)가 섞여** 병합 전 대사 1라운드가 들었다. 이 항 초판의 "(F+K+B+J 합)"도 같은 실측으로 정정했다 — PR #11 대사 보고(F43+K19+B3=65)가 옳았다.
→ **규칙**: 커밋 제목의 수치는 제목이 말하는 스코프와 일치시킨다. 스코프가 섞이면 수치를 본문으로 내려 스코프별로 쪼개 적는다. 검증의 근거는 커밋 메시지가 아니라 실행 로그다.
→ 같은 규율은 기억·기록 수치에도 적용된다 — "SKU CSV 22칸"은 MSDS링크 칸 승격(PR-2)으로 **현재 21칸**이고, 리포의 MIG-0001은 테스트 픽스처 "한국 콜마"다(실DB의 MIG- 행은 세션 시작 시 실측으로 고정).

### ⑬ 원천 실데이터 파일 repo 커밋 금지 (2026-08-07 웹 세션 판정 — S1.5 반입)
원천 엑셀 원본 파일과 그 내용(행 데이터·개별 값)은 어떤 형태로도 repo에 커밋하지 않는다. **커밋 가능한 것은 행수·검수 기록 등 수치·문서뿐**이다(예: GC-A5 기대값 엔터티별 행수표). 원천 파일은 repo 밖 폴더에 두고, 문서에는 경로와 수치만 적는다.
→ **수칙**: 반입 관련 산출물을 커밋하기 전 diff에서 원천 값(개별 거래처명·품명·수량 등 실데이터 내용)이 섞이지 않았는지 확인한다. 위반 커밋은 반려 대상.

### ⑨ 이전 세션 인계 (유효)
- **마이그레이션이 시드를 넣는 테이블은 `tests/conftest.py`의 `PRESERVED_TABLES`에 반드시 추가**(S2-1 요건 템플릿, S3-4 FTA 시드 등).
- **픽스처 순서를 autouse에 기대지 말고 인자로 명시한다. "빈 목록"은 정상이 아니라 오류로 취급한다.**
- **"실패도 커밋해야 하는" 패턴** — 트랜잭션 안에서 판정만 하고 커밋 뒤에 예외를 던진다(로그인 잠금·마지막 관리자 보호). 승인 반려·검수 차이·게이트 거부에 그대로 반복된다.
- **테스트가 못 잡는 영역** — TestClient는 헤더를 정규화하고 본문을 항상 UTF-8로 보낸다. 관통은 실기동으로 한 번 더 본다(§22 렌즈 11).
- **첫 관리자는 `python -m app.cli create-admin`으로만** 만든다.
- **S4-1 인계**: `stock_movements`·확정 분개 생성 시 `table_policy.IMMUTABLE_TABLES` 등록 + 마이그레이션에서 `revoke_mutations()` 호출.
- **Phase 3 인계**: 전표에 담당자 컬럼을 추가하면 `handover/targets.py`에 한 줄 추가(아키텍처 테스트가 강제).
- DB 로케일 `LC_COLLATE 'C' + LC_CTYPE 'C.UTF-8'`(ADR-0003) / `.gitattributes` LF 강제 / 의존성 추가 후 `docker volume rm kbos-dev_web_node_modules` / DB 역할 2종(ADR-0002) / 프런트 HTTP 클라이언트는 **`httpx2`** / FastAPI는 `include_router`를 래퍼로 감싸므로 인증 커버리지 검사는 OpenAPI+실요청으로 구현.
- **병합 스크립트 호출(이 PC)**: PowerShell에서 `bash` 단독 표기는 WSL을 물어 오류가 난다. 반드시 `& "C:\Program Files\Git\bin\bash.exe" scripts/merge-pr.sh <번호>`.

## 세션 기록
- **s2pr2-smoke@local.kbos 생성·처분(2026-08-10 — S2-1 PR-2 실기동 관통)**: 렌즈 11 관통용 임시 관리자(users id 9)를 CLI로 생성, 관통 종료 직후 **smoke-ci 선례 판정 수단 그대로**(단일 트랜잭션 `is_active=false`+`deleted_at`+세션 3행 `revoked_at`) 처분 — 처분 후 재로그인 **401**(`COMMON.AUTH.INVALID_CREDENTIALS`) 실측. 잔여물은 관통 데이터(요건 템플릿 3행 등 — 관찰 항목 등재)뿐, 계정 잔존 0.
- **s21-smoke@local.kbos 생성·처분(2026-08-07 — S2-1 PR-1 실기동 관통)**: 렌즈 11 관통용 임시 관리자(users id 8)를 CLI로 생성, 관통 종료 직후 **smoke-ci 선례 판정 수단 그대로**(단일 트랜잭션 `is_active=false`+`deleted_at`+세션 `revoked_at`) 처분 — 처분 후 로그인 **401** 실측. 잔여물은 관통 데이터(시장 ZZ — 관찰 항목 등재)뿐, 계정 잔존 0.
- **smoke-ci@local.kbos 삭제 근거 등재(2026-08-07 웹 세션 판정 — 반입 기준선 위생)**: compose-smoke 로컬 실기동(S1.5 PR-2 관통)이 실DB에 남긴 CI 스모크 계정(users id 7, 2026-08-07 01:12 생성)은 반입 기준선(실데이터만 남는 상태)을 오염시키므로 삭제 대상으로 판정됐다. **삭제 시도 실측(2026-08-07)**: 하드 DELETE가 `fk_audit_log_actor_user_id_users` RESTRICT로 거부됨 — audit_log 2행(`auth.login.succeeded`)이 참조 중이고 audit_log는 UPDATE/DELETE 불가 계약(DB 권한 강제)이라 하드 삭제는 **영구 차단**이다(참조 행 총 448: audit_log 2·import_staging 계열 442·세션 2·역할 1·멱등키 1). 강행 금지 준수. **재판정(같은 날): 하드 삭제 차단=감사 불변 정상 동작 확인, 처분=비활성.** 실행 실측(2026-08-07): 단일 트랜잭션으로 `is_active=false`+`deleted_at` 설정(기존 §2 비활성+soft delete 수단) + 세션 2행 `revoked_at` 폐기(`set_account_active` 의미론 재현 — ADR-0013). 처분 후 로그인 **401**(`COMMON.AUTH.INVALID_CREDENTIALS`) 실측 — soft delete가 로그인 조회 필터(`identity/service.py` — `deleted_at IS NULL`)에서 계정을 제외하고 `is_active=false`가 이중 잠금이라 어떤 비밀번호로도 거부된다(원 비밀번호는 리포·기록 어디에도 없음 — 401 근거는 코드 경로+실요청). audit_log 직접 삽입 없음(앱 전용 테이블 — 처분 기록은 본 문서·§0-3).
- **S1-3 PR-3 적대적 리뷰(2026-08-06)** — 5렌즈 병렬 리뷰+발견별 반박 검증으로 14건 확정(반박 성공 0). 수정: ① [high] 유형 전용 변경이 partners 행 version을 안 올려 겹친 스테이징의 확정 409 계약을 우회 → apply_changes가 부모 행을 항상 dirty(updated_at 명시 대입)로 ② [med] 확정 시 기본공급사 SUPPLIER 재검증 부재(TOCTOU) → verify_references 훅 신설·409 합류 ③ [low] ID 셀 `isdigit`↔`int()` 도메인 불일치('²'로 500) → `isdecimal()` ④ 프런트 4건(업로드 후 잔상·200행 잘림 은닉·422 detail 유실·확정 키 재생성). 테스트 갭 5건(확정 IntegrityError 롤백·코드 중복 3계열·값 지우기·rename·업로드 멱등 재생) 전건 케이스 추가. 대칭 갭(유형 해제 시 참조 자재 미확인)은 관찰 원장 등재만. 수정 중 **함정 ② 재발**(확정 루프 except에서 `row.row_no` 접근 → PendingRollbackError) — flush 전 변수로 빼서 해소, 인계 ②에 부기.
- **S1-3 PR-2 사후 승인 3건 — 웹 세션 사후 승인 완료(2026-08-06)**: ① `LABEL_ARTWORK` 시드(document_types에 §4.5 라벨 파일 승격용 종류 추가 — §4.7 열거 9종 밖) ② CERT 역할의 문서 등록·삭제 권한(무역+인증, 관리자 상시 — §2 권한 절 명문화는 PR-3 문서 커밋에 반영) ③ document_types=데이터(ADR-11 적용 — §4.7 열거는 예시적 문면, ADR-0028). 3건 모두 구현은 PR-2에 선반영돼 있었고 이번 판정으로 승인 상태가 정리됐다.
- **S1-1 PR 재구성** — 원안은 PR-1(스키마)·PR-2(단가+프런트)였으나, 커밋 2가 "단품 SKU는 처방 없이 등록 불가"를 넣으면서 **제품 화면이 없으면 처방을 만들 수단 자체가 없어졌다**. 화면 없이 병합하면 main이 "SKU를 등록할 수 없는 상태"가 된다 — 각 병합은 동작 가능해야 하므로 프런트를 PR-1로 옮겼다(부채 #13도 함께 종결). 단가 화면·금액 타입은 PR-2 유지.
- **S1-1 ADR 6건** — ADR-0016(세트↔처방 분리, 정정·부기 포함) / 0017(발효일 단가 이력) / 0018(원가 마스킹·로그 4겹) / 0019(HS 근거·버전) / 0020(S1-3 승격 임시 문자열) / 0021(품목군 헤더). ADR-0003에 ⑧(물리량 NUMERIC·중량 정의) 추가.

## 다음 세션 첫 명령
```
cd C:\Users\PC\orca\kbeauty-trade-os
git checkout main
git pull origin main
docker compose up -d --build
docker compose run --rm api pytest -q
```
다음 = **PR-2 병합(영준) 후 S2-2(인증 인스턴스·상태머신) 계획 보고**(웹 세션 조건부 승인 후 착수). S2-2 계획 보고 명시 이월분 2건: §4.8 자동 적용 / 만료임박 구현 방식(scheduled_jobs 교차 — s2-1-plan.md §0-1 ⑦). 보존잠금 정정 경로 재판정 트리거("실수요 발생 또는 S2-2 계획 보고")도 도달한다. **실데이터 반입은 이월 상태**(2026-08-07 오너 결정 — 트리거: 영준 "실사용 준비" 선언, 관찰 항목·s1-5-plan.md §0-3 참조). 반입 재개 시에는 원천 엑셀 폴더 경로 실측부터.

## 영준이가 지금 할 것
1. ~~S2-1 PR-1(#15) 병합~~ — **완료(`dfcee0f` origin/main 실측 2026-08-10)**.
2. **[대기] S2-1 PR-2(`s2-1-templates`) 병합** — PR 생성 후 CI가 초록(ci-ok ✓)인지 본 뒤, PowerShell에 아래 한 줄. 웹 Merge 버튼 금지.
   `& "C:\Program Files\Git\bin\bash.exe" scripts/merge-pr.sh <PR번호>`
3. **실데이터 반입은 네가 "실사용 준비"라고 선언할 때 재개된다** — 그때 CC 세션에 원천 엑셀 대장 폴더 경로(repo 밖, 예: `C:\Users\PC\orca\kbos-data`)를 알려 주면 반입 세션 1부 절차(MIG- 검수 — **markets MIG 2행(CA·US) 정식화 포함** → 원천 행수 실측·GC-A5 기대값 선고정 → 어댑터 필요분 판정 → pg_dump 백업 실증)부터 진행한다. 그전까지 반입 관련 조작은 없다.
