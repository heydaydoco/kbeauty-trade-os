# kbeauty-trade-os 골든 케이스 명세 v1.0

> **용도**: 구현 모델(Opus/Sonnet)의 산출물이 "이 케이스들을 통과하는가"로 검수한다. 코드 스타일이 아니라 **거동(behavior)**을 고정하는 문서다.
> **우선순위 규칙**: 이 문서와 `DESIGN.md`가 충돌하면 **DESIGN.md가 우선**한다. 충돌 발견 시 구현하지 말고 보고할 것.
> **수치**: 본 문서의 모든 계산값은 Python으로 사전 검증됨(2026-07-04).
> **사용법**: 각 Phase 구현 완료 시 해당 카테고리 케이스를 자동 테스트로 옮기고(pytest), 통과 증거(실행 로그) 없이 "완료" 보고 금지 — CLAUDE.md 제7조와 동일 원칙.

---

## A. 원장 무결성 (Ledger Integrity)

**GC-A1. 음수 재고 차단**
- Given: SKU `SER-001` 가용재고 10 EA
- When: 출고 15 EA 요청
- Then: 트랜잭션 전체 거부(부분 출고 금지), 재고 10 유지, 에러에 부족 수량(5) 명시. 원장에 실패 기록이 남지 않아야 함(시도 로그는 별도 audit 테이블).

**GC-A2. 수불 정합**
- Given: 기초 100 → 입고 +50 → 출고 -30
- Then: 현재고 = **120**. 현재고는 별도 컬럼 캐시가 아니라 원장 SUM으로 재계산해도 동일해야 함(캐시 사용 시 정합 검증 쿼리 포함).

**GC-A3. 멱등성**
- Given: 동일 idempotency key로 입고 +50 요청 2회(네트워크 재시도 시나리오)
- Then: 원장 행 1개만 생성, 재고는 +50 한 번만 반영. 2차 요청은 에러가 아니라 **최초 결과를 그대로 반환**(멱등 성공).

**GC-A4. 전표 불변 + 역기록**
- Given: 확정된 출고 전표의 수량 오류 발견
- Then: UPDATE/DELETE 불가(DB 권한 레벨에서 차단). 정정은 역기록(reversal) 전표 + 신규 전표. 역기록 전표는 원전표 ID를 참조해야 함.

## B. FTA / 원산지

**GC-B1. RVC 공제법 판정**
- Given: 완제품 FOB $10,000 / 비원산지 재료 합계 $3,800 / 협정 기준 RVC 40% 이상(공제법)
- Then: RVC = (10,000 − 3,800) / 10,000 × 100 = **62.0%** → **충족**. 시스템은 계산 근거(재료별 원산지 구분 내역)를 판정 레코드에 스냅샷 저장해야 함(재료 단가가 나중에 바뀌어도 당시 판정은 불변).

**GC-B2. 원산지 미상 재료 보수 판정**
- Given: BOM 중 1개 재료의 원산지확인서 미수취
- Then: 해당 재료는 **역외산 간주**로 계산. "확인서 수취 시 판정 재실행" 액션이 대기 목록에 생성돼야 함.

**GC-B3. 판정의 HS 버전 고정**
- Given: 판정 시점 HS 2022 기준으로 CTH 충족
- Then: 판정 레코드에 HS 버전이 명시 저장. HS 개정 이후 조회해도 과거 판정은 당시 버전으로 표시(감사 대응 — 설계감사 minor 지적 사항 반영).

## C. 인증·규제 (kbeauty-global-reg 스킬과 연동)

**GC-C1. 만료 알림 리드타임**
- Given: CPNP 관련 인증서 만료일 2026-09-30, 오늘 2026-07-04
- Then: D-**88**. 알림 정책(예: D-180/D-90/D-30)에 따라 D-90 알림이 이미 발송된 상태여야 하고, 다음 알림은 D-30 예약. 동일 만료 건에 중복 발송 금지(발송 이력 기반 dedup).

**GC-C2. 미등록 시장 출고 차단**
- Given: SKU가 EU 미통보(CPNP 없음) 상태
- When: EU향 주문 확정 시도
- Then: 차단이 기본값. 단, 권한 있는 사용자의 **명시적 오버라이드 + 사유 기록**으로만 통과 가능(전면 차단이 아니라 통제된 예외 — 샘플 발송 등 실무 존재).

## D. 채널 풀필먼트

**GC-D1. FBA 입고 차이 처리**
- Given: FBA 발송 500 EA, 아마존 수취 확인 497 EA
- Then: 차이 **3 EA**는 자동 손실 처리 금지. "차이 대기" 상태로 분리 계상하고, 조사 케이스 자동 생성. 일정 기간 경과 후에만(정책값) 손실 확정 전표 → 이때도 승인 필요.

**GC-D2. 채널 재고 이원화**
- Given: 자사창고 200 EA, FBA 300 EA
- Then: 총재고 500이지만 채널별 가용재고는 분리 조회 가능해야 하고, 자사창고 주문이 FBA 재고를 끌어다 약속하면 안 됨.

## E. 세트 SKU

**GC-E1. 조립 수불**
- Given: 세트 = 본품 2 + 미니 1. 세트 20 EA 조립 지시
- Then: 본품 **−40**, 미니 **−20**, 세트 **+20**이 단일 트랜잭션(ASSEMBLY 이동유형). 구성품 부족 시 전체 롤백(부분 조립 금지). 해체(DISASSEMBLY)는 정확한 역방향.

## F. 동시성

**GC-F1. 재고 할당 경합**
- Given: 가용 10 EA, 동시에 주문 A(7 EA)와 주문 B(6 EA) 할당 요청
- Then: 한 건만 성공, 다른 한 건은 부족 응답. 합계 13이 할당되는 결과(더블 부킹)는 어떤 타이밍에서도 불가 — SELECT FOR UPDATE 또는 낙관적 잠금 재시도로 보장. 테스트는 실제 동시 실행(스레드/태스크 2개)으로 검증할 것. 순차 실행 테스트는 이 케이스의 통과 증거로 인정 안 됨.

## G. 수입원가

**GC-G1. 관세·부가세 계산**
- Given: 과세가격(CIF) $8,200, 관세율 6.5%
- Then: 관세 = **$533.00**, 부가세 = (8,200 + 533) × 10% = **$873.30**, 세액 합계 **$1,406.30**. 금액은 float 금지(정수 최소단위 또는 Decimal — CLAUDE.md 도메인 규칙). 라운딩 규칙은 통화별 설정값으로 분리.

## H. 자동화 경계 (ADR-09)

**GC-H1. AI 추출 자동확정 금지**
- Given: 거래처 인보이스 PDF를 AI로 파싱해 필드 추출(신뢰도 99%)
- Then: **자동확정 금지**. 반드시 사람 확인 큐에 적재. 자동확정은 결정적 파서 경로(CSV/고정 템플릿)만 허용. 이 경계를 흐리는 구현(예: "신뢰도 임계값 이상이면 자동확정" 옵션 추가)은 ADR-09 위반으로 즉시 반려.

**GC-H2. 외부 전송 승인 게이트**
- Given: 시스템이 바이어에게 발송할 이메일 초안 생성
- Then: 사람 승인 없이 실제 발송되는 경로가 코드상 존재하면 안 됨(발송 함수는 승인 토큰 없이 호출 불가한 시그니처로 설계).

---

## 검수 절차 (리뷰어 세션용 프롬프트)

구현 PR 검수 시 아래를 그대로 사용:

```
이 PR이 kbeauty-golden-cases-v1.md의 케이스 중 어떤 것을 커버하는지 매핑하라.
각 케이스에 대해 ①통과 증거(테스트 실행 로그 원문) ②케이스와 어긋나는 거동 ③케이스가 침묵하는 엣지(발견 시 케이스 추가 제안) 순서로 보고하라.
증거 없는 "통과" 주장은 미통과로 취급한다. DESIGN.md와 충돌하는 케이스 발견 시 구현 중단 후 보고하라.
```

## 버전 규칙
- 케이스 추가/수정 시 v1.1, v1.2로 올리고 변경 이력을 문서 하단에 기록. 케이스 삭제는 금지(폐기 시 DEPRECATED 표기 + 사유).
