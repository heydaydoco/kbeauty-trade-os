# S2-3 계획서 — 준비도 매트릭스·기일 알림·인증 보드

> 상태: **판정 확정(안건 ①~⑧ 전건 승인 + 판정 요청 19건 확정치 + 웹 조건 A~F — 2026-08-12 웹 세션)·판정 반영 완료(§0-1)·PR-1 `s2-3-notify-core` 착수.** 승인 범위 밖 구현 금지.
> 작성일 2026-08-12(계획 세션) / 판정 확정·등재 2026-08-12(PR-1 첫 커밋 = 이 문서).
> 기준 커밋: origin/main `640916e5a2ec`(PR #18 병합 — S2-2 종결). 착수 시점 워킹트리 클린·main 최신 실측.
> **수 대사 정본 기준선: pytest 1094 · vitest 79** — #18 CI green run(31557371613) 실측: backend 잡 `1094 passed in 247.87s (0:04:07)` / frontend 잡 `Test Files 17 passed (17)` `Tests 79 passed (79)`.
> 상시 수칙 유지: scheduled_jobs 행 삽입은 **판정 요청 15 해제 문면의 조건·경로로만**(그 전까지 금지 — 부채 #12) / notification_channels·webhook_subscriptions **0행 유지** / 자동화 L3 금지 4영역(§15) 절대 비접촉·출력 판정 워딩 금지 / 마스킹·승인 밖 신규 발견분은 구현 없이 등재만 / 병합은 `scripts/merge-pr.sh`만(ADR-0011) — 병합 실행=영준, 안내는 PowerShell 전체 경로 표기 / 원천 실데이터 파일 커밋 금지 / 반입 관련 조작 없음(트리거=영준 "실사용 준비" 선언 전) / TRADE-KERNEL 문서군 혼동 금지.

---

## 0-1. 웹 세션 판정 반영 (2026-08-12 — 안건 전건 승인·요청 19건 확정. 이 커밋이 그 반영이며 PR-1 첫 커밋)

판정 실측 고정: origin/main 선두 `640916e5a2ec`(12자리 대조 일치)·워킹트리 클린·기준선 pytest 1094·vitest 79 실측 일치 확인 후 브랜치 생성·착수.

**총괄**: 안건 ①~⑧ 전건 승인. 신규 테이블 2건(notification_channels·webhook_subscriptions) 사전 승인. 부채 #11·#12 소비 개시.

**판정 요청 확정치 19건** (번호 = 계획 보고 번호):

1. **색 매핑 = 추천 승인.** 🟢=필수 전건 APPROVED(임박 없음) / 🟡=EXPIRING 또는 활성 인스턴스가 진행 상태(서류준비·신청제출·심사중·보완요청·갱신중[미도과]) / 🔴=필수 중 1건이라도 활성 인스턴스 부재·미착수·EXPIRED·갱신중 도과 / ⚪=필수 요건 0건. **미착수=🔴**(착수 사실 0=실질 미충족), **셀=최악값 집계**.
2. **필수 집합 = (나) 채택.** SKU·PRODUCT 축은 **자동 적용과 동일 축**(SKU 요건=SKU 자신의 품목군 세트 중 applies_to=SKU / PRODUCT 요건=**소속 제품 자신의** 품목군 세트 중 applies_to=PRODUCT — ADR-0042 축과 일치시켜 구조적 🔴 방지) + **COMPANY**(품목군 세트 소속 COMPANY 템플릿을 자사 인스턴스[target_id NULL]와 대조) + **FACILITY**(`skus.manufacturer_partner_id` 경유 인스턴스 대조, **제조사 미지정 SKU = 해당 요건 🔴 fail-closed**). **INGREDIENT 비포함**(관찰 등재). 도출 규칙 전문을 DESIGN [M2] 보강 + ADR에 명문.
3. **스캔→알림 경로 = (가) 채택.** 스캔이 **알림 생성 코어**(수신자 결정 + dedup INSERT `ON CONFLICT DO NOTHING`)를 직접 호출하고 **아웃박스는 경유하지 않는다**(§17.4 "확인 후 INSERT" 금지 정합 — 멱등 보증은 DB 유니크에 직접 부딪히는 INSERT). **디스패처와 동일 코어 공유를 아키텍처 테스트로 강제**(웹 조건 B).
4. **커넥터 실장 = 추천 승인.** IN_APP 1종 실장 + 어댑터 계약만. 외부 커넥터 전건 S6-3. **WEBHOOK 조기 실장 불채택.**
5. **신규 2테이블 = 추천 승인.** 이번 창설 + **0행 유지**(행 공급·구독 평가·config 시크릿 취급 = S6-3 동석 판정). **마이그레이션 시드 항구 금지**(함정 ⑩).
6. **dedup_key = 추천 승인(수신자 포함 키 정본 규약).** 기일 `{종류}:{대상type}:{대상id}:{문턱}:{수신자id}` / 브리핑 `briefing:{수신자id}:{KST날짜}` / 에스컬레이션 `esc:` 접두 별도 키. ADR 명문 + 팬아웃 테스트.
7. **수신자 결정 = 수정 채택(이원화).**
   - (i) **기일 계열**(문턱·도과·갱신중 도과·에스컬레이션 원천): 담당자 실재→담당자 1인 / 부재→alert_rules / **규칙도 부재→ADMIN 역할 전원 폴백**(각 1건, 요청 6 키로 dedup, 알림 본문에 "라우팅 규칙 부재" 경위 1줄). 근거: 조용한 도과 방지가 이 축의 존재 이유 — **fail-visible**(§17.6 관리자 알림 안전망 패턴과 동형).
   - (ii) **디스패처 일반 이벤트**: 규칙 매칭만, 미매칭 = 미발송 + published 처리.
   - 테스트: 폴백 경로 1건 + **담당자 지정 건에 ADMIN 미수신(배타 라우팅) 단언**(웹 조건 E).
8. **D-3 에스컬레이션 수신자 = 추천 승인.** ADMIN 역할 전원(각 1건).
9. **브리핑 = 추천 승인.** 담당 건 보유 사용자만 각자 1통. 관리자 전사 요약 1통은 **미포함·관찰 등재**(트리거=사용자 요구).
10. **알림 규칙 CRUD 권한 = 추천 승인.** 관리자 전용(수신자·채널 결정=조직 관리 계열. 폴백 게이트가 규칙에 걸리므로 통제 강화 정합).
11. **events 보존 = 추천 승인.** 무삭제 유지, 재판정 트리거=10만 행(관찰 등재).
12. **백오프 저장 = 추천 승인.** `events.next_retry_at` 컬럼 신설 + pending 부분 인덱스 재정의. **인덱스 재정의도 수기 + `pg_get_indexdef` 정의문 테스트 동반**(웹 조건 D — 함정 ① 계보).
13. **schedule 문법 = 수정 채택(방향 승인 + 웹 사양).** 폐쇄 2형 = **`daily@HH:MM`(KST 일간)** + **`interval@N`(N분 주기, N≥1 정수 — 디스패처용)**. 리터럴 문법·파서 규칙은 ADR 문면으로 고정, **cron·croniter 등 파서 의존성 비도입**(웹 조건 F).
14. **잡 관리 화면 = 추천 승인.** 목록 읽기 전용 + `is_enabled` 토글(관리자).
15. **부채 #12 해제 = 문면 그대로 승인.** 해제 = **실행기 코드·코드-함수 매핑·테스트가 실재하는 커밋 이후, 같은 브랜치의 앱 경로(CLI) 등록부터** 허용. 병합 전 dev DB 등록 행 = 관통 실측의 일부로 정당. 실행기 코드 없는 행 삽입 = 계속 반려, 마이그레이션 시드 금지 항구. **PR별 등록 2행/2행, 종료 기대값 2행/4행.**
16. **보드 드래그 전이 = 추천 승인(비구현 확정).** 전이 통로 = 기존 상세 폼 단일 유지.
17. **파기·용량 = 추천 승인.** 실물 물리 정리 + 용량 모니터링·임계 알림 = **S2-4 단일화**(백업·복원·files/ 세트와 동석), **S2-3 갈래 소거**. 관찰 [S1-3] 트리거 문면 재작성.
18. **태스크 스키마 forbid = 추천 승인.** TaskAddRequest·TaskUpdateRequest `extra=forbid` + 아키텍처 테스트 편입 — **PR-1 내 독립 소커밋**.
19. **문서 soft delete × 태스크 링크 = 웹 결정 (나).** 태스크 링크가 실재하는 문서의 soft delete = **409**(에러 코드 신설 — 기존 카탈로그 3세그먼트 규약 준용: `DOCUMENTS.DOCUMENT.LINKED_TO_TASK`), **선해제 후 삭제**. DESIGN 명문 + ADR 부기. 편입 = **PR-2**(기일 스캔과 동석): 스캔 활성 한정 명시 케이스 + 409 + 해제 후 삭제 성공 테스트, 관통에서 기존 링크×삭제 잔존 조합 0 확인. **관찰 후보 9 = 미등재 전환**(구조 해소).

**웹 추가 조건 A~F:**
- **조건 A**: 매트릭스 범례 또는 셀 툴팁에 **집계 모집합 1줄 명시**(포함 축 + INGREDIENT 제외 — 🟢 과신 방지). [PR-3]
- **조건 B**: 스캔·디스패처의 **알림 생성 코어 단일 공유를 아키텍처 테스트로 강제**. [PR-1]
- **조건 C**: 완료 보고 **행수 실측을 5테이블로 확장** — events·scheduled_jobs·notification_channels·webhook_subscriptions·alerts. [전 PR]
- **조건 D**: 인덱스 재정의도 수기 + `pg_get_indexdef` 정의문 테스트. [PR-1]
- **조건 E**: 폴백 경로 1건 + **담당자 지정 건 ADMIN 미수신(배타 라우팅)** 단언. [PR-1]
- **조건 F**: schedule 리터럴 문법·파서 규칙 ADR 고정, 파서 의존성 비도입. [PR-1]

**등재 목록 조정(판정)**: 계획 보고 §6 후보 1~8·10·11 승인 등재 / **후보 9 삭제**(요청 19로 구조 해소) / **추가 2건** — 관리자 전사 브리핑 미포함(트리거=사용자 요구) · ack 취소 경로 부재(문면 없음 — 트리거=실수요).

---

## 0-2. 계획 산출 경과 (계획 세션 2026-08-12 — 기록 보존)

**0번 게이트 실측**: origin/main 선두 `640916e5a2ec` 12자리 일치(#18 squash) / `s2-2-tasks-autoapply` 로컬·원격 부재(fetch 시 낡은 원격 추적 참조 1건 prune 정리 — 병합 스크립트 원격 삭제 + `fetch.prune true`[S2-2 판정 ⑭] 정상 동작) / 워킹트리 클린.

**인용 실측(a7c3e9f2d4b8)**: 실재. PROGRESS.md 20행 문면 = "CHECK 확장(a7c3e9f2d4b8 — 함정 ①·⑪ 준수, 정의문·downgrade 실측)". 마이그레이션 실물 `20260806_1710_a7c3e9f2d4b8_extend_import_registry_check_with_skus.py` 실재. **위생 부기**: #18이 인용한 "올바른 실패" 워딩의 실제 위치는 원장이 아니라 **ADR-0041 15행**이다(원장 문면은 실측 표기까지). 판정 결과 비영향.

**WBS 대조 결과**: 웹 예약 목록 ①②③⑥ = WBS v1.4 문면 일치 / ④⑤⑦ = WBS 문면 부재이나 별도 정본 근거 실재(부채 #11·#12 문면·§17.1·ADR-0039·[M2] 보강 S2-2 PR-1 ②) → 포함 처리 / ⑧ = 착수 블록 인용 문면("물리 정리(S2-3)"·"용량 관찰") **리포 전체 grep 0건 — 인용 부재 판명**. 실재 문면은 PROGRESS 96행 관찰 항목과 s1-5-plan.md 197행이고, 요청 17 확정으로 S2-4 단일화.

**자기 적대 검증(제출 전 1회, 2026-08-12)**: 6렌즈 발견 에이전트(스코프 대조·자동화 4금·데이터/마이그레이션·동시성/멱등·테스트/DoD 매핑·선례/수칙 정합) → 발견 36건(렌즈 간 중복 포함) → 반박 검증 12에이전트 전건 리포 재실측 → **반박 성공 0건**, 유니크 20건 **전건 확정·계획 반영**(계쟁 0·기각 0). 주요 확정분: 부채 #12 해제 문면 자기모순(병합 시점 vs 병합 전 관통 등록) / dedup_key 구성 미정의(팬아웃 조용한 유실) / 스캔 dedup 선확인 = §17.4 금지 패턴 / 매트릭스 FACILITY·COMPANY 제외의 🟢 과대 표시 위험 + §9 ③ 오인용 / channels.config 마스킹 메커니즘 부재 / D-3 수신자·알림 규칙 권한 임의 확정 / RUNNING 잔존 처리 부재 / H 대표 케이스 미특정 / 매트릭스 K 페이지 계약 미배정 / PR-3 판정 슬롯 부재 / 포괄확인서 배정 오기(S3-4→**S4-4**) / IN_APP "문면 고정" 오서술 / §15 "알림→원클릭 액션" 조용한 누락 / 수 대사 기준선 미고정 / GC-C1 "휴면" 오기 / 디스패처 클레임 경계·브리핑 KST 날짜·§17.6 실패 알림 절반 누락·실행기 실패 알림 순환 의존. 코드 수정 필요 발견 0건(계획 전용 세션).

---

## 1. 세션 정의 (WBS v1.4 문면 그대로)

WBS "S2-3 | 준비도 매트릭스·기일 알림" — Phase 2(인증 엔진) 셋째 세션.
- 범위: §5.3, §2 ADR-07, §4.7(유효기간 문서 스캔).
- 산출물: 매트릭스 화면(계산값·미저장·신호등 4색), 기일 엔진 1차(documents+certifications 유효기간 스캔), notification_channels/webhook_subscriptions, 알림센터, 발송 이력 dedup, ack 재발송 금지, D-3 에스컬레이션, 데일리 브리핑 1통, 담당자 라우팅(ADR-0012), 인증 보드(§14 ④ 칸반+캘린더 — ADR-0036).
- DoD: 만료일 변경 → 매트릭스 즉시 반영(집계 저장 없음 증명) / 동일 기일 중복 발송 0 / 미확인 에스컬레이션 발동 / 담당자 지정 건은 담당자에게만 발송(전사 폭포 0건) / §20 H "담당 일괄 이관 후 라우팅 즉시 반영" 통과.
- 검증: C(매트릭스), H(재발송 0·에스컬레이션·담당자 라우팅) + GC-C1.

**범위 밖(경계 명시)**: 외부 채널 커넥터 실발송(슬랙·노션·메일·텔레그램)=S6-3(WBS S6-3 문면) / 대외 발신 전부 — 정체 독촉=S2-4·포괄확인서 재수취=**S4-4(§6.4)**·자동 발송 화이트리스트=S5-4 / 채널 리스팅 게이트 소비·기업 단위 요건 채널 연결=S5-1(§9 ③) / 매트릭스 재계산 검산 버튼=P6(§14 ⑭) / 재고 유통기한 임박=P4(§8.7) / rate_cards 만료 D-30=S5-4(§10.2) / 실물 파기·용량 모니터링=S2-4(요청 17) / 스케줄 실패 외부 감시=§20 I(P5).

---

## 2. 안건 전문 (판정 확정치 반영)

### 안건 ① 시장 준비도 매트릭스 [PR-3]

**(a) 목적·경계** — §5.3 메인 화면: 행 SKU×열 시장, 셀=필수 요건 집계 신호등 4색, 저장하지 않고 계산. 포함: 매트릭스 조회 API 1본 + 화면 1본, SET SKU 셀=구성품 롤업(§4.2 + [M2] S2-2 PR-2 "세트의 판정 롤업은 §5.3 매트릭스 몫"), 갱신중 도과의 셀 반영(안건 ⑦ 연동), **집계 모집합 1줄 명시**(조건 A). 제외: 채널 리스팅 게이트 소비(S5-1), 검산 버튼(P6), 매트릭스발 알림(기일 엔진 몫).

**(b) DESIGN 근거** — §5.3·§4.2·§14 ②·[M2] 보강(S2-2 PR-1·PR-2). 신설: DESIGN [M2] 보강(S2-3)에 **집계 규칙 전문**(색 매핑·필수 집합 도출 축·SET 롤업) 명문 + ADR 후보 1건(매트릭스 집계 규칙·계산값 미저장 강제).

**(c) 데이터 변경** — **없음**(테이블·컬럼·CHECK·인덱스·마이그레이션 전부 0 — 계산값 미저장이 스펙 자체).

**(d) 자동화 4금** — 법적 판정 비저촉: 스크리닝 계보(ADR-0022)의 기계적 대조·집계이고 집계 저장 테이블 부재를 아키텍처 테스트가 강제. 🟢 라벨 "판매가능"은 §5.3 명문. 판정 워딩 신규 0. **요청 2 (나) 채택으로 집계 모집합에 COMPANY·FACILITY가 포함**되어 라벨 의미와 계산 실질의 괴리가 해소되고, 잔여 축(INGREDIENT)은 조건 A의 모집합 명시로 노출된다.

**(e) 상태·전이·불변** — 신규 0. 이벤트 발행 0(읽기 전용 — S2-1 판정 ⑦ 계보).

**(f) 테스트 배분(S2-2 조건 C 준용)** — 서비스 층 전수 파라미터라이즈: 상태→색 매핑(11상태 + 인스턴스 부재 + 갱신중 도과 + 무기한 NULL), 필수 집합 도출(품목군 미지정·해당 시장 0건·제품/SKU 품목군 상이·제조사 미지정 FACILITY fail-closed·COMPANY 대조). e2e 대표 소수(C): 셀 4색 각 1·**만료일 정정 즉시 반영(DoD)**·SET 롤업·전 역할 열람. K: 매트릭스 API 페이지 계약 **명시 케이스**(그리드형 응답은 기존 K 자동 스캔이 못 잡는 실측 한계). 아키텍처: 집계 저장 테이블·컬럼 부재 스캔. 예상 backend 20~25 + vitest 3~4.

**(g) 소비·등재** — 소비 없음. 등재: 시장 열 규모 / INGREDIENT 축 비포함.

### 안건 ② 기일 엔진 1차 [PR-2]

**(a) 목적·경계** — certifications.expires_on + documents.valid_until 스캔으로 기일 임박·도과 감지 → 알림 생성. 포함: 문턱 정책(기본 D-180/90/30 — GC-C1 예시 문면, alert_rules.config 데이터화 ADR-11), **문턱 통과 의미론**(GC-C1 정본: 오늘 D-88이면 D-90분이 이미 발송된 상태 — 정각 아님), 도과 감지, D-3 에스컬레이션 판단, 갱신중 도과(안건 ⑦), **스캔 활성 한정 명시**(요청 19 동석). 제외: 상태 전이(ADR-0039 스윕 그대로), tasks.due_date 스캔(등재만), P4·S4-4·S5-4 기일 축.

**(b) DESIGN 근거** — §4.7·§2 ADR-07·[M2] 보강(S2-2 PR-1) ②·GC-C1. ADR 후보 1건(기일 문턱 데이터화·문턱 통과 의미론·스캔 경로 = 요청 3 (가)).

**(c) 데이터 변경** — 없음(문턱=alert_rules.config JSONB, 발송 이력=alerts.dedup_key[요청 6 규약]).

**(d) 자동화 4금** — 스캔=읽기 + 알림 생성만. 상태 전이 비접촉. 대외 발신 0(수신자 전원 users). 법적 판정 0(달력 산술).

**(e) 상태·전이·불변** — 0. 실행 = 실행기 잡(안건 ⑤ 등록) + CLI 수동 겸용(스윕 선례).

**(f) 테스트** — 단위: 문턱 계산 순수 함수(정각·지각·도과·무기한 NULL 제외·리드타임과 문턱 독립·KST 경계). 통합: 스캔 멱등(동일 기준일 2회 → alerts 신규 0)·건별 트랜잭션·**GC-C1 골든**(D-88: D-90분 실재 + D-30분 미발송)·문서 valid_until 스캔·갱신중 도과·요청 19 3종(활성 한정·409·해제 후 삭제). **H 대표 e2e 특정**: ① 동일 기일 2회 스캔 → 중복 발송 0(DoD) ② ack 후 재스캔 → 재발송 0(§20 H). 예상 30~35.

**(g) 소비·등재** — 소비: 관찰 "[S1-3] 문서 유효기간 기일 스캔 부재"(트리거 도달) / GC-C1 발동(WBS 매핑표 통상 배정) / 관찰 "문서 soft delete × 태스크 링크"(요청 19로 구조 해소·미등재 전환). 등재: tasks.due_date 비포함.

### 안건 ③ 알림 발송 — 알림센터·담당자 라우팅·ack·에스컬레이션·브리핑 [PR-1 / 브리핑은 PR-2]

**(a) 포함** — 알림센터 API(내 알림 목록·미확인 수·ack 1클릭) + 화면(셸 편입), **수신자 결정 이원화(요청 7)**, ack 재발송 금지(acknowledged_at 기록 + 활성 행의 dedup_key 점유 — 행이 남는 한 재INSERT 무산, ack 취소 없음[문면 없음 — 비발명·등재]), D-3 에스컬레이션(수신자=ADMIN 전원 — 요청 8), 데일리 브리핑 1통(dedup=사용자×**KST** 날짜 — 요청 6·9, PR-2), 알림 규칙 CRUD API + 관리 화면(**관리자 전용** — 요청 10), 알림 행의 entity_type/entity_id 링크로 상세 1클릭 이동(§15 "알림→원클릭 액션"의 부분 충족 — 완결형은 등재). 제외: 외부 커넥터 실발송(S6-3), 대외 발신 전부.

**(b) DESIGN 근거** — §2 ADR-07·ADR-0012(동작 몫)·ADR-0015(§20 H 검증 몫)·§14 ⑭·§15·§18.1. ADR 후보 1건(수신자 결정 규칙 + dedup 키 규약 + **IN_APP 채널화는 ADR-07 열거 4종 밖의 신설 판단**임을 명기 — 문면 근거는 별도 문구 "화면 알림센터").

**(c) 데이터 변경 — 신규 테이블 2건(사전 승인)**
- `notification_channels`: id / code(부분 유니크 active) / name_ko / **kind 5값 CHECK**(IN_APP·SLACK_WEBHOOK·TELEGRAM·EMAIL·NOTION — 4종은 ADR-07 문면, IN_APP은 신설 판단) / config JSONB / is_enabled / 믹스인 전부.
- `webhook_subscriptions`: id / event_type VARCHAR(60) / channel_id FK notification_channels RESTRICT / config JSONB / is_enabled / 믹스인 전부 / 부분 유니크(event_type, channel_id) active.
- **행 공급: S2-3 기간 두 테이블 모두 0행 유지**(요청 5) — CRUD·시드·CLI 등록 전부 없음. 마이그레이션 시드는 함정 ⑩(ActorMixin users FK)으로 **항구 금지**. 행 공급·구독 평가·config 시크릿 취급(현행 redaction이 config 키를 비커버하는 실측 포함)은 S6-3 동석 판정.
- 기존 테이블 변경: `events.next_retry_at` 추가 + `ix_events_pending` 재정의(안건 ④). alerts·alert_rules 변경 0(기존 컬럼 충족 실측).

**(d) 자동화 4금 — 대외 최초 발신 경계 논증(핵심)** — 이번 세션 발송 수신자 축은 **users(사내)뿐**. 알림센터=인앱(DB 행 생성 — 외부 호출 0). 외부 서비스 경유 채널은 테이블·열거·어댑터 계약까지만 서고 **커넥터 실장 부재로 발신 코드 경로가 물리적으로 없으며, 채널·구독 행도 0행이라 데이터로도 부재**(요청 4 WEBHOOK 조기 실장 불채택으로 이 논증이 유지된다). 거래처·기관을 수신자로 삼는 설계는 S2-3에 없다.

**(e) 상태·전이·불변** — alerts.acknowledged_at 단방향 기록. 신규 상태머신 0. 불변 테이블 신설 0·IMMUTABLE_TABLES 확장 0(alerts·events는 table_policy MUTABLE 분류가 기존부터 이 세션을 예정). status_log 계보 비적용(알림=통지 기록).

**(f) 테스트** — K: 신규 2테이블 제약·**정의문 열거 1:1**(pg_get_constraintdef)·타인 알림 403·규칙 CRUD 권한·알림센터/규칙 목록 API 페이지 계약(list_ 명명 + Page 봉투로 K 자동 스캔 편입). 서비스: 수신자 결정 파라미터라이즈(담당자 유→1인 / 무→규칙 / 규칙 부재→**ADMIN 폴백**)·**역할 팬아웃 × dedup**(2인 이상 각자 1건, 유실 0)·**배타 라우팅 단언**(담당자 지정 건 ADMIN 미수신 — 조건 E)·ack 재발송 금지·에스컬레이션 판단. H e2e: 담당자 지정 건 담당자에게만 + 타 사용자 0건(DoD)·미확인 에스컬레이션 발동(DoD)·**담당 일괄 이관 후 신규 알림이 새 담당자에게 + 기존 미확인 알림 수신자 이관**(DoD §20 H). J: 브리핑 더블 실행 dedup(KST 날짜). 아키텍처: **알림 생성 코어 단일 공유**(조건 B). 예상 30~35 + vitest 3~5.

**(g) 소비·등재** — 소비: ADR-0012 동작 몫 완결·ADR-0015 §20 H 검증 몫 완결. 등재: 규칙 0행 발송 공백(폴백으로 완화 — 운영 절차 문서화)·브리핑 시각 설정화·알림→원클릭 완결형·채널/구독 0행 유지·관리자 전사 브리핑·ack 취소 경로 부재.

### 안건 ④ 아웃박스 디스패처 (부채 #11 소비) [PR-1]

**(a) 포함** — pending 소비 루프. **클레임 경계 고정**: 건당 1클레임 — `SELECT … FOR UPDATE SKIP LOCKED` → 규칙 평가 → alerts 생성(요청 6 키·`ON CONFLICT DO NOTHING`) → published_at 표식이 **한 트랜잭션**(§17.6 건별 독립). 실패 시 attempts+1·last_error·지수 백오프(next_retry_at), **attempts 상한 5회 도달 시 대기열 제외 + 관리자 알림 1회(dedup)** — §17.6 "실패 알림"의 디스패처 몫. 매칭 규칙 없는 이벤트도 published 처리(재소비 방지). **평가 대상은 alert_rules만** — webhook_subscriptions 평가는 S6-3(0행 유지와 정합). 제외: 외부 커넥터 발송·events 삭제/아카이브.

**(b) DESIGN 근거** — §17.1·§17.6·§2 ADR-07·§16. 부채 #11. ADR 후보 1건(디스패처 계약 — 클레임 경계·백오프·상한·보존).

**(c) 데이터 변경** — `events.next_retry_at` TIMESTAMPTZ NULL 추가 + `ix_events_pending` 부분 인덱스 재정의(수기 + **pg_get_indexdef 정의문 테스트** — 조건 D, 함정 ①·⑪ 계보). 마이그레이션 1건. 신규 테이블 0(발송 이력=alerts — 인앱 채널에서 발송 사실과 알림 행이 동일물).

**(d) 자동화 4금** — 디스패처의 "발송"=alerts INSERT(내부 DB 쓰기)뿐 — 트랜잭션 안 외부 호출 0. 외부 커넥터 슬롯은 "커밋 후 발송" 계약으로 어댑터 문면에 예약.

**(e) 상태·전이·불변** — events MUTABLE 유지(발행 원장 성격 — 행 삭제 없음).

**(f) 테스트** — J: 더블 디스패치 경합(SKIP LOCKED — 이중 알림 0·attempts 이중 증가 0)·발송 실패 재시도·한 건 실패 비차단·재실행 변화 0. 통합: 규칙 매칭·미매칭 published·automatic 표식 이벤트·백오프·상한 도달 관리자 알림. K: 컬럼·인덱스 정의문. 예상 20 내외.

**(g) 소비·등재** — 부채 #11 종결. 등재: events 10만 행 보존 재판정.

### 안건 ⑤ scheduled_jobs 실행기 (부채 #12 소비) [PR-1]

**(a) 포함** — 스케줄러 루프(활성 행 조회 → **KST due 판정** → 잡 실행 → last_run_at·last_status(RUNNING→OK/FAILED)·last_error 기록), 잡 코드→함수 **매핑 레지스트리(코드 고정 폐쇄 열거** — 미등록 코드는 실행 거부 + 실패 기록), **기동 시 잔존 RUNNING 정리**(worker 사망 → FAILED[사유 "비정상 종료"] + 관리자 알림 — 크래시 경로에서도 §15 "실패 시 관리자 알림" 성립), compose 서비스 `worker` 신설(dev·prod), 잡 실패 관리자 알림, 중복 기동 방지(PG advisory lock), 등록 경로 = **CLI 서브커맨드**(마이그레이션 시드 금지 — 함정 ⑩), 등록: **PR-1 2행**(certification-sweep[ADR-0039 예약분]·outbox-dispatch) / **PR-2 2행**(deadline-scan·daily-briefing), 잡 목록 읽기 전용 API + 관리 화면(§15) + is_enabled 토글(요청 14). 제외: 스케줄 문자열 편집 화면·임의 코드 실행 경로(구조적 부재).

**(b) DESIGN 근거** — §15(스케줄 레지스트리·실패 알림)·§17.6·§2 운영 품질·ADR-0039. 부채 #12. ADR 후보 1건(실행기 계약 — **schedule 리터럴 문법(요청 13·조건 F)**·등록 경로·중복 기동 방지·미스파이어·**크래시/RUNNING 잔존 의미론**).

**(c) 데이터 변경** — scheduled_jobs 스키마 변경 0(기존 컬럼 충족 실측). 신규 테이블 0. 인프라 1건 — compose worker(dev·prod) + CI compose-smoke 영향 확인. 데이터 행: 앱 경로 등록 2 + 2행.

**(d) 자동화 4금** — 잡 전건이 내부 동작(스윕=기판정 승인분 ADR-0038·0039 / 디스패치=내부 알림 / 스캔·브리핑=읽기+알림). 4금 저촉 잡 등록을 막는 장치 = **코드→함수 매핑 폐쇄 열거**(아키텍처 테스트) — 행 등록만으로 새 동작이 생기지 않는다.

**(e) 상태·전이·불변** — 신규 상태머신 0(last_status 3값 기존 CHECK). **부채 #12 해제 문면 = 요청 15 확정치 그대로.** 부수 정리: 기존 "scheduled_jobs 0행" 단언·주석·관통 절차 문면 전수 갱신(기대값 = PR-1 2행 / PR-2 4행 · 전건 코드 매핑 실재).

**(f) 테스트** — 단위: due 판정 순수 함수(KST 경계·최초 실행·미스파이어=재기동 시 지각 1회 실행 수렴)·**schedule 문법 파싱**(`daily@HH:MM`·`interval@N` 수용 / 그 외 거부). 통합: 잡 실행·실패 기록·실패 알림·미등록 코드 거부·**잔존 RUNNING 기동 정리**·advisory lock 경합(J). 아키텍처: 매핑 폐쇄 열거·scheduled_jobs 쓰기 경로 한정(실행기·CLI). 예상 20 내외.

**(g) 소비·등재** — 부채 #12 종결. 등재: 스케줄 실패 감지 완결 한계(디스패처 자기 실패·worker 전체 다운의 자기 감지 불가 — 외부 감시 축은 §20 I·P5).

### 안건 ⑥ 인증 보드 [PR-3]

**(a) 포함** — §14 ④ 칸반 + 캘린더 화면 1본. 칸반=컬럼 11(상태 11값·한국어 라벨·nowrap)·카드=활성 인스턴스(요건명·대상·만료일·담당자)·카드 클릭=기존 상세 이동. 캘린더=월 뷰(인증 expires_on·문서 valid_until 항목·클릭=상세 이동). 제외: **드래그 전이 비구현 확정(요청 16)** — 전이 통로=기존 상세 폼 단일 / 보드 전용 집계 저장 / 필터 고도화(상태·시장 기본까지).
**(b)** §14 ④·ADR-0036. [M2] 보강(S2-3)에 상호작용 경계 명문. **(c)** 데이터 변경 없음(보드 조회 1본 신설 가능). **(d)** 읽기 전용 + 기존 전이 폼 링크 — 비저촉. **(e)** 0. **(f)** backend K(권한·페이지 계약) 5 내외 + vitest 3~5(컬럼 11·라벨·캘린더 항목·도과 배지). **(g)** ADR-0036 배정분 이행. 등재: 보드 카드 200 상한.

### 안건 ⑦ 갱신중 도과 표시·알림 [PR-2 / 표시 일부 PR-3]

RENEWING이면서 expires_on < 기준일인 인스턴스의 **도과 계산값** 표시(목록·상세·보드 배지·매트릭스 셀 🔴) + 기일 알림(도과 문턱 dedup). **상태 전이 없음** — DESIGN 문면 "갱신중은 스윕 비대상(도과 표시·알림=S2-3 계산값·기일 엔진)" 그대로. 근거 [M2] 보강(S2-2 PR-1) ②·s2-2-plan §3. 데이터 변경 0·4금 비저촉·전이 표 27방향 비접촉. 테스트: 기일 스캔 케이스 합류 + 표시 계산 단위 + vitest 배지(5 내외). S2-2 판정 ③ 예약분 소비.

### 안건 ⑧ 파기·용량 관찰 문면 정리 [PR-1 문서분]

실측: 착수 블록 인용 "물리 정리(S2-3)"·"용량 관찰"은 리포 전체 부재(grep 0건). 실재 문면 2건(PROGRESS 96행·s1-5-plan 197행)의 트리거가 "S2-4 또는 S2-3(알림 엔진)" 양자택일이었고, **요청 17 확정으로 S2-4 단일화**(실물 물리 정리 + §18.4 용량 모니터링·임계 알림이 백업·복원·files/ 세트와 동석). 관찰 [S1-3] 트리거 문면 재작성 = 이 커밋. 구현 0.

---

## 3. PR 분할 (승인 + 삽입 1건)

**PR-1 `s2-3-notify-core`** (알림 기반 — 부채 #11·#12 소비): ① 판정 반영 첫 커밋(이 문서 등재·CLAUDE.md 수칙 1건·PROGRESS 원장 6건) → **①-보 요청 18 forbid 소커밋** → ② 신규 2테이블 마이그레이션 + events.next_retry_at·pending 인덱스 재정의(K: CHECK=pg_get_constraintdef / 인덱스=pg_get_indexdef) → ③ 디스패처(클레임 단일 트랜잭션·SKIP LOCKED·알림 생성 코어·백오프·상한 5+관리자 알림·미매칭 published) + 알림 규칙 CRUD(관리자 전용) + 알림센터 API + J·H·서비스(요청 6 팬아웃·요청 7 배타 라우팅+폴백) → ④ 실행기(루프·매핑 폐쇄 열거·advisory lock·RUNNING 잔존 기동 정리+알림·미스파이어) + compose worker(dev·prod) + CLI 등록 서브커맨드 + 등록 2행(관통 실측, 요청 15 문면 준수) + 부채 #12 종결·0행 문면 전수 갱신 → ⑤ 알림센터 화면 + vitest + 부채 #2 재확인 → ⑥ DESIGN 보강 + ADR(디스패처 계약·실행기 계약·수신자 결정+dedup 규약+IN_APP 신설 판단) + 실기동 관통.
**PR-1 비포함 재확인**: 기일 스캔·브리핑·매트릭스·보드(PR-2·PR-3), 외부 커넥터 전부(S6-3), 요청 19 규칙(PR-2).

**PR-2 `s2-3-deadline-engine`** (기일 축 — PR-1 의존): ① 판정 반영 → ② 기일 스캔(문턱·지각·도과·문서 valid_until·갱신중 도과·에스컬레이션) + GC-C1 골든 + C·H 대표 2건 → ③ 데일리 브리핑 + J dedup → ④ **요청 19**(문서 soft delete 409 + 스캔 활성 한정 + 해제 후 삭제) → ⑤ 잡 등록 2행 추가(종료 기대값 4행) + 도과 배지 화면 + vitest → ⑥ DESIGN·ADR + 관통.

**PR-3 `s2-3-matrix-board`** (화면 축): ① 판정 반영 슬롯 → ② 매트릭스 집계 서비스 + API + C 전수 + 아키텍처(미저장) + K 페이지 → ③ 매트릭스 화면(**조건 A 모집합 명시**) + vitest → ④ 인증 보드(칸반+캘린더) + vitest → ⑤ DESIGN [M2] 보강(S2-3) + ADR(집계 규칙) + PROGRESS 종결 기록 + 관통.

**완료 보고 요건(전 PR 공통, 누락 2종 이상 = 보완 없이 반려)** — 표준 10종: pytest 단일 결과 줄 / vitest 줄 / CI green run 전체 URL / 커밋 해시 12자리 전체 / 마이그레이션 DDL 전문 / **수 대사 1094·79 대비 분해** / 행수 실측 / 부채 #2 한 줄 / backend 잡 시간 / 0번 게이트 결과. **행수 실측은 5테이블**(조건 C): events·scheduled_jobs(기대 2행 — 등록 잡 코드 목록 명기)·notification_channels(0)·webhook_subscriptions(0)·alerts(관통 생성분 분해). compose worker 기동 증적을 관통 요지에 포함. 제출 전 자기 적대 검증 1회·결과 명기(0건이면 0건).

---

## 4. 신규 부채·관찰 등재 목록 (판정 조정 반영 — 12건)

1. notification_channels·webhook_subscriptions **0행 유지(의도 상태)** — 행 공급·구독 평가·config 시크릿 마스킹(현행 redaction이 config 키 비커버 실측) = S6-3 커넥터와 동석 판정.
2. 알림 규칙 0행 상태의 발송 공백 — 기일 계열은 ADMIN 폴백으로 완화, 일반 이벤트는 미발송. 첫 규칙 등록 운영 절차 문서화.
3. 인증 보드 카드 상한(활성 인스턴스 200 초과 시 잘림 — `?size=200` 계열).
4. 매트릭스 시장 열 규모(열 수십 도달 시 UX·성능 재판정).
5. tasks.due_date 기일 스캔 비포함(WBS 문면 밖).
6. events 행수 임계 10만 도달 시 보존 재판정.
7. 스케줄 실패 감지 완결 한계 — 디스패처 자기 실패·worker 전체 다운의 자기 감지 불가(외부 감시 축=§20 I·P5).
8. 브리핑 발송 시각 고정(09:00 KST) — 설정화 트리거: 사용자 요구.
9. *(삭제 — 요청 19로 구조 해소)*
10. 매트릭스 INGREDIENT 축 비포함(조건 A로 모집합 명시).
11. §15 "알림→원클릭 액션" 완결형(이번은 엔터티 상세 이동까지).
12. 관리자 전사 브리핑 미포함(트리거=사용자 요구) / ack 취소 경로 부재(문면 없음 — 트리거=실수요).
