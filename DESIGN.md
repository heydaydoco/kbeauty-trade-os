# K-Beauty Trade OS — 설계서 확정판

> 이 문서가 본 시스템의 유일한 사양서다. (검증 상태: 독립 설계 감사 2회 통과 — 치명 결함 0건. 의사결정 이력은 구버전·감사보고서 파일이 담당하며, 구현 시 이 문서만 참조한다.)
> 구현 규율: 각 Phase가 Claude Code 작업 단위. 업무 규칙의 원천은 사용자 스킬(trade-docs, hs-fta, fta-origin-checker, customs-clearance, inventory-control, lc-doc-check 등) — 추측 구현 금지.
> 설계 준거: biz-app-foundations 8대 원칙, build-erp-core 4대 원리(참조 생성·헤더-라인·유형 코드·문서흐름), arch-design(지루한 기술·ADR·워킹 스켈레톤), db-design.

---

## 1. 시스템 개요

**목적** — 화장품 수출입 무역의 3대 고통을 하나로: ① 인증 리스크("이 SKU, 이 나라에 팔 수 있나" 즉답 불가·갱신 실기) ② 기일 사고(선적 마감·서류 기한·유통기한·인증 만료의 산재) ③ 숫자 불일치(재고·서류·정산·비용).

**한 줄 정의** — 제품×국가 "시장 준비도 매트릭스"를 심장으로, 인증 → FTA/원산지 → 수주·선적 → 재고 → 채널 판매 → 비용·정산이 하나의 전표 사슬로 연결되는 미니 ERP.

**규모** — 사용자 5~20명(무역·물류·인증·관리·조회), 월 수백 건 선적, 연 수출입액 1,000억 원, SKU 수백, 채널 주문 월 수천 건. 설계 상한은 현재의 10배.

**포지셔닝** — 회계 ERP(SAP/이카운트)를 대체하지 않는다. 회계·세금계산서·재무 마스터의 SoR은 사내 ERP, 본 시스템은 인증·원산지·로트·채널·기일·비용의 SoR이다.

**비범위** — 회계 기표(분개 초안까지만), 관세율·요건·원산지의 자동 "판정"(계산·정리 지원까지만), 전 세계 성분 DB 자체 구축(자사 성분만), BPM식 동적 워크플로우 엔진, 선박 실시간 추적(수동 롤오버 입력+이력이 정답). "Phase 7 이후" 항목(SMTP 자동 발송, 이카운트 API, UNI-PASS, 슬랙 인터랙티브, 노션 양방향 등)은 로드맵 §19에 명시.

---

## 2. 아키텍처 결정 (ADR)

- **ADR-01 스택**: FastAPI(Python) + PostgreSQL + React(Vite·Tailwind), Docker Compose로 사내 서버/클라우드 VM 배포(dev/prod 완전 분리). 프런트 런타임 라이브러리는 react-router(라우팅)·TanStack Query(서버 상태). 운영 정적 서빙·`/api` 리버스 프록시는 nginx 컨테이너(동일 오리진 → CORS 불필요). [S0-1 보강 — ADR-0003]
- **ADR-02 DB 규율**: 모든 변경은 버전 마이그레이션, 금액=정수 최소단위+통화코드+환율+기준일, 시각=UTC 저장·KST 표시(국제 일정은 현지 시간대 병기), FK 실제 제약, soft delete, 감사 컬럼 전 테이블.
- **ADR-03 규제·요건 = 데이터**: 국가·협정·인증 요건을 코드가 아니라 템플릿 테이블로. 규제 변경은 관리 화면에서 흡수. 모든 요건·세율·기한 데이터에 근거링크+최종확인일 필수.
- **ADR-04 수량의 진실 = 원장 하나**: 자사창고·3PL·채널창고(FBA 등)·외주처·운송중·격리를 전부 "위치"로, **품목(SKU 또는 재고관리 대상 자재)**의 모든 수량 변화는 불변 원장(stock_movements)에만 기록.
- **ADR-05 전표 사슬 = 참조 생성**: QT→PI→SO→선적→CI/PL→C/O→채권, PO→(수입)선적→통관→입고 — 후속은 선행 복사+참조, 재입력 화면 금지. CI·PL은 선적 한 원천에서 생성. 확정 전표 불변. **취소·정정 세칙**: 후속 전표가 살아 있으면 선행 취소 불가(역순 취소만), 역기록 = 원본 불변 + 반대 부호의 신규 기록(취소 전표가 원본 참조) — 원장·전표 공통.
- **ADR-06 외부 연동 3단계**: ① 파일(업로드·양식 내보내기, 멱등 임포트) → ② API(이카운트 Open API·UNI-PASS·환율) → ③ SP-API. SAP 직접 연동(RFC)은 계획하지 않음(파일 경로).
- **ADR-07 알림·발신 채널 어댑터**: notification_channels 추상화(텔레그램/슬랙 웹훅/메일/노션 발행) — 이벤트→채널→포맷을 webhook_subscriptions 데이터로 라우팅. 화면 알림센터 + 담당자 라우팅 기본(전사 폭포 금지), 데일리 브리핑 1통. **알림 정확성**: 확인(ack)된 알림 재발송 금지, 동일 기일 중복 발송 방지(발송 이력 키), 미확인 지속 시 에스컬레이션(예: D-3에도 미확인 → 상급자). 대외 자동 발송은 §15 화이트리스트 정책.
- **ADR-08 AI 레이어**: Anthropic Messages API(백엔드 경유, 키는 환경변수). **AI 출력·AI 추출 스테이징은 무예외로 사람 확정** — DB 직행 금지. API는 구독과 별개 종량 과금, 월 한도·로그 필수.
- **ADR-09 스테이징 원칙**: 파서·AI·엑셀의 모든 추출은 import_staging → 검토 → 확정. 파일 해시+문서번호 멱등. **조건부 자동 확정 예외**: 결정적 파서 경로(CSV·고정 매핑 템플릿)에 한해, 검증 전건 통과+금액 임계 이하+기존 전표 매칭 시 자동 확정 허용 — 자동 확정이 만드는 것은 인테이크 전표(주문 접수·비용 발생 기록)까지이며 원장 기록·지출 확정·분개·마감은 불가.
- **ADR-10 외부참조 매핑**: external_refs(내부ID↔SAP/이카운트 코드)를 Phase 0부터. SoR 정책 명문화.
- **ADR-11 설정화의 한계선**: 유형 코드·체크리스트·템플릿·규칙은 데이터(관리 화면), 핵심 상태 머신·원장·잔량·검증 골격은 코드 고정. "상태는 고정, 태스크는 자유." 커스텀 필드는 임시 수납장 — 검색·집계·검증이 필요해진 필드는 정식 컬럼으로 승격(마이그레이션)한다.
- **운영 품질(prod-ready)**: .env 분리, 외부 호출 타임아웃·재시도, 구조화 로깅(민감정보 마스킹), 헬스체크, 에러 알림, 백그라운드 작업 큐(AI 추출·대량 서류·집계 배치), 대시보드 집계만 야간 사전집계+원장 검산. **백업 = DB(pg_dump 일일)+파일 저장소(files/) 세트**로 수행하고 복원 리허설도 세트로(주 1회) — 백업 산출물은 암호화 저장+접근 통제(관리자 한정, 접근도 audit). 종결 후 장기 경과 트랜잭션은 조회 전용 아카이브 파티션(시행 시점은 100만 행 도달 시 ADR).
- **권한·통제**: 역할 5종(관리자/무역/물류/인증/조회 — 조회는 원가·마진을 API 응답 레벨에서 마스킹). **문서보관소(§4.7) 문서의 등록·삭제는 무역+인증 역할**이 한다(관리자는 상시 통과, 조회·물류는 열람까지 — 인증 서류(CFS·GMP·CoA 등 §5.6)의 실무 취급 주체가 인증 담당이므로 무역 단독으로 좁히지 않는다. 웹 세션 사후 승인 2026-08-06). **시장(§5.1 markets)·요건 템플릿의 등록·편집은 인증 역할**이 한다(관리자 상시 통과, 무역·물류·조회는 열람 — 요건 데이터의 실무 취급 주체가 인증 담당이다. S2-1 계획 웹 세션 판정 2026-08-07 안건 5·조건 9). **승인 워크플로우**: 기안→승인 2단, 승인 후 불변. 정(正)결재선 = 승인유형×금액임계→승인 역할 매핑 테이블(설정화), 승인자 부재 시 대결(위임 — 기간·범위·이력 기록). 대상: 임계 초과 비용, 여신 초과 수주, 신규 파트너, 권고 외 소싱, 폐기, 실사 차이. **담당 이관**: 퇴사·휴가·담당 변경 시 담당 건(전표·인증·태스크·알림) 일괄 재배정 도구 + 계정 비활성 절차. 파일 업로드 검증(확장자·크기), 반응형 웹(모바일 조회).

---

## 3. 도메인 모델 총괄 (3계층)

```
[마스터]    브랜드·제품(처방)·SKU·세트 | 성분(INCI)·자재 BOM | 라벨 버전 | 거래처(공급사/바이어/RP/IOR/
            포워더/관세사/3PL/인증대행) | 시장·채널 | 위치·로트 | 문서보관소 | 요율 카드 | 협정
[규제]      인증 요건 템플릿→인스턴스→태스크 | 원산지 판정·증빙 | 성분 규칙 | 시장 준비도 매트릭스(계산값)
[트랜잭션]  QT→PI→SO→선적→통관→채권/입금 | PO→(수입)선적→입고 | 재고 원장(할당·검수 포함) |
            채널 주문→정산 | 비용 원장 | RMA·클레임 | 이슈 | 분개 초안 | 알림·태스크
```
공통 규율: 채번(`SO-2026-0001`, MAX+1 금지 — §17.3), 거래 시점 값 스냅샷 복사, 날짜 3종(증빙/전기/입력), audit_log, 상태 변경 이력.

**전체 테이블 맵**
```
[공통]  users, roles, user_roles, user_sessions, audit_log, doc_number_seq, fx_rates, feature_flags, custom_field_defs/values,
        external_refs, sync_policies, holidays(국가별), alert_rules, alerts, tasks, approvals,
        events(아웃박스), webhook_subscriptions, notification_channels, scheduled_jobs,
        inbound_rules, auto_confirm_rules, outbound_policies, projects(출시 그룹핑)
[M1]    brands, products, skus(+DG 속성), sku_hs_codes, set_components, materials(재고·로트 플래그),
        product_boms, ingredients, product_ingredients, ingredient_rules, labels, partners,
        agency_contracts, documents(보존기한·파기잠금), signatories, item_profiles, customer_item_codes
[M2]    markets, requirement_templates, template_checklist, certifications, certification_tasks,
        certification_status_log, comm_logs(폴리모픽)
[M3]    agreements, agreement_countries, origin_determinations(+hs_version, BOM 스냅샷),
        determination_triggers_log, supplier_origin_confirmations, co_records
[M4]    quotations(+lines), proforma_invoices, sales_orders(+lines), purchase_orders(+lines),
        shipments(+lines/parties), milestones, customs_records, receivables, payments, issues,
        lc_terms(L/C번호·개설은행·금액·tolerance·유효기일·최종선적일·제시기간 — feature flag)
[M5]    locations, lots(+CoA 링크), stock_movements, allocations(할당·예약), picking_lists,
        shipment_inspections(출고 검수), lot_costs(배부 단가), item_params, stocktakes(+lines), serials(옵션)
[M6]    channels, channel_fulfillment_profiles, channel_listings, channel_orders(+lines),
        channel_shelf_rules, settlements(+lines)
[M7]    expense_types, expenses, rate_cards(+lines), rfqs, sourcing_decisions, fx_positions(계산), hedges
[M8]    rmas, claims, credit_notes, backorders(계산 뷰)
[M9]    import_staging, mapping_templates, posting_rules, journal_drafts
[M10]   ai_prompt_templates, ai_logs, knowledge_notes, feature_requests
```

> **[공통] 보강(S0-2)**: `user_roles`(사용자×역할 — 겸직 허용을 위한 정규화), `user_sessions`(서버측 세션 — §18.1 "세션 만료"와 §2 "계정 비활성 절차"가 성립하려면 발급된 세션을 즉시 무효화할 수 있어야 한다) [ADR-0013], `idempotency_keys`(§17.4의 "동일 키 재수신 시 **최초 결과 반환**"을 담을 자리 — 스코프는 액터×엔드포인트×키) [ADR-0014].

> **[공통] 보강(S2-3 PR-1 — 알림 기반)**: ① **`notification_channels`·`webhook_subscriptions`가 섰고, 이번 세션에는 0행이다**(판정 요청 5). 구조를 먼저 세우는 이유는 ADR-07의 라우팅이 "이벤트×채널×포맷을 데이터로"이기 때문이고, 행을 넣지 않는 이유는 **행이 곧 대외 발신 통로**이기 때문이다(§15 L3 "대외 최초 발송"). 이번 실장 채널은 **IN_APP 1종**이며 이는 ADR-07 채널 열거 4종 밖의 신설 판단이다(문면 근거는 같은 ADR의 "화면 알림센터"). 외부 커넥터·행 공급 경로·config 시크릿 취급은 **S6-3 동석 판정**이고, 마이그레이션 시드는 항구 금지다(함정 ⑩ — 두 테이블이 ActorMixin의 users FK를 단다). [ADR-0045] ② **알림을 만드는 통로는 하나다**(`notify()`) — 기일 스캔도 아웃박스 디스패처도 같은 함수를 부르고, `Alert` 직접 생성 0건을 아키텍처 테스트가 고정한다. **dedup_key는 수신자를 포함**하며(전역 부분 유니크 + `recipient_user_id` NOT NULL 구조상 팬아웃이 행 단위이므로), 멱등은 사전 확인이 아니라 `ON CONFLICT DO NOTHING`이다(§17.4). ③ **수신자 결정 이원화**: **담당자 → 규칙 → (기일 계열만) ADMIN 전원 폴백**(fail-visible — 조용한 미발송은 곧 도과다) / 일반 이벤트는 미발송. 담당자가 있으면 규칙 수신자도 폴백도 발동하지 않는다(배타 라우팅). **담당자가 규칙보다 앞서는 이유**: 규칙은 사건 종류당 하나인데 담당은 건마다 다르다 — 규칙을 앞세우면 규칙에 수신자를 한 명 적는 순간 전 건의 담당자 라우팅이 그 한 사람으로 덮인다. **규칙이 지목한 사람이 없는 경우(비활성 계정·보유자 0명 역할)는 "아무도 못 짚은 경우"와 같게** 처리해 폴백으로 관통시킨다(거기서 멈추면 규칙 하나가 기일 축을 조용히 침묵시킨다). 알림 규칙 CRUD는 관리자 전용. [ADR-0045] ④ **`events.next_retry_at` 신설 + `ix_events_pending` 재정의**(`(id)` → `(next_retry_at, id)`) — 디스패처 백오프의 저장형. [ADR-0043]

---

## 4. M1 — 마스터 (제품·성분·자재·라벨·거래처·문서)

**4.1 제품 계층**: 제품(처방)과 SKU(용량·세트)를 분리 — 인증·원산지 판정은 처방 단위, 재고·판매는 SKU 단위. `skus`에 바코드·중량·박스입수·shelf_life·제조사 + DG 속성(dg_flag, UN번호, Class, PG, 인화점, 알코올함량, 에어로졸, LQ, MSDS 링크). 국가별 HS는 sku_hs_codes(세율 메모+근거링크 — 자동 판정 금지). 판가·매입가는 이력 테이블로 변경 추적("그때 단가" 분쟁 대비 — 전표의 단가 스냅샷과 이중 안전).

**4.2 세트 SKU = 실물 키팅**: 세트는 실물 재고다(채널에 세트 단위로 입고되는 순간 실물일 수밖에 없다). set_components가 구성을 정의하고, 조립·해체는 재고 원장의 ASSEMBLY/DISASSEMBLY 유형(§8.2)으로 수행. **세트 로트의 유통기한 = 구성품 로트 유통기한의 MIN 자동 산정**(채널 최소기한 판정도 이 값). 인증·원산지 판정·C/O는 세트가 아니라 구성품 처방별 유지(세트 화면은 구성품 판정의 롤업 뷰). 채널 전용 가상 번들(재고 없는 조합 리스팅)은 리스팅 레벨 매핑으로 별도 지원.

**4.3 성분(INCI)**: 자사 취급 성분만 등록. product_ingredients(함량·표시순서), ingredient_rules(성분×국가: 금지/제한/농도·근거·확인일). 성분 스크리닝: 신제품 전성분×대상국 → 위반·제한 초과·미등재 리포트("참고용·근거 확인" 고정 문구).

**4.4 자재 BOM**(원산지·원가·구매·사급의 공통 전제): materials(벌크/원료/용기/단상자/라벨/부자재, HS6, 기본공급사, **재고관리 플래그·로트관리 플래그** — 사급하는 자재만 선택적으로 재고 편입) + product_boms(소요량·단가·원산지상태[역내/역외/미상 — 미상은 판정 시 역외 간주]). OEM 생산 발주(PO)에는 생산 마일스톤 프로파일(원료수급→충진→포장→출하검사) 적용 가능.

**4.5 라벨·아트웍 버전**: labels(SKU×시장, 버전, 언어, 승인상태, 컷인일, 파일) — 검증 항목에 INCI 현지어 표기·원산지 표기(Made in Korea) 포함. 컷인 시 구라벨 재고 소진 리포트.

**4.6 거래처**: partners(다중 유형: 공급사/OEM/바이어/RP/IOR/경내책임자/포워더/관세사/3PL/인증대행), 여신한도, DG 취급 가능 여부(포워더·3PL), 강점/약점 메모. 바이어 품번 매핑 customer_item_codes.

**4.7 문서 보관소**: documents(폴리모픽 소유, 종류 코드, 유효기간, 파일, 해시) — CFS·GMP·CoA·MSDS·인증서·무역서류·구매확인서·L/C 통지서(MT700)까지 한 곳. 유효기간 있는 모든 문서가 기일 엔진 스캔 대상. 원산지 증빙은 보존기한(발급일+5년)+파기 잠금. 서명권자 대장 signatories.

**4.8 품목군 프로파일**: item_profiles(제품군 → 기본 요건 세트·서류 세트·마일스톤 세트) — 신규 등록 시 자동 적용, 아이템별 프로세스 변형 흡수.

> **[M1] 보강(S1-1)**: ① **세트 SKU는 처방에 속하지 않는다** — `skus.kind`(SINGLE/SET)로 가르고, SINGLE만 `product_id`를 갖는다(DB CHECK 양방향). 세트의 인증·원산지는 4.2 원문대로 구성품 롤업이며, 세트를 구성품으로 넣는 중첩은 금지한다. SET 행의 DG 속성 취급(자체 입력 vs 롤업)은 §7.7 DG 게이트(P4)에서 확정할 **미결**이라 그때까지 입력받지 않는다. [ADR-0016] ② **단가 이력은 종료일 없는 발효일 이력**(다음 행의 발효일이 곧 종료 — 기간 겹침이 원천 불가), 기준일에 해당 단가가 없으면 0·null이 아니라 **오류**로 실패한다(S3-1 전표 스냅샷이 이 함수를 소비한다). [ADR-0017] ③ **매입가 = 원가** — 조회 역할에게는 필드가 아니라 **행 단위로** 보이지 않는다(§2 "마스킹"의 구현: 목록·건수에서 제외, 단건 404). [ADR-0018] ④ `sku_hs_codes`는 근거링크·최종확인일 NOT NULL(ADR-03)이며 **`hs_version`을 유일키에 포함**한다 — 빼면 HS 개정 시 덮어쓰기가 되어 GC-B3가 마스터 쪽에서 무너진다. 세율은 메모이고 계산에 쓰지 않는다(§1 비범위). [ADR-0019] ⑤ **MSDS 링크와 제조사**는 S1-1에서 문자열(NULL 허용) — 실체인 §4.7 `documents`와 §4.6 `partners`가 서는 **S1-3에서 승격**한다. [ADR-0020] ⑤-1 **DG 속성의 잠금은 두 방향뿐**이다: `kind='SET'`이면 DG 속성 전부 부재, `dg_flag=false`면 UN번호·Class·PG·LQ만 부재. 인화점·알코올함량·에어로졸은 "왜 DG가 아닌지"의 근거값이라 비DG 행에도 허용한다. **DG 정보의 완결성 강제와 교차 정합(에어로졸↔DG 등)은 마스터가 아니라 §7.7 DG 게이트(P4)**가 맡는다 — 차단할 지점은 등록이 아니라 선적이다. [ADR-0016 정정·부기] ⑥ `item_profiles`는 S1-1에서 헤더+적용 FK까지 — 내용 3종은 대상이 생기는 세션이 연결한다(서류 S1-3 / 요건 S2-1 / 마일스톤 S3-2). [ADR-0021]

> **[M1] 보강(S1-2)**: ① **성분 규칙은 (성분, 국가)당 한 행** — 금지·제한 공존을 유일키로 차단한다(공존하면 스크리닝이 같은 성분을 "금지"이면서 "제한초과"로도 집계하는 모순 리포트가 가능해진다). 용도별(린스오프/리브온) 상이 한도는 §4.3에 없는 축이라 담지 않는다 — 실데이터 요구 발생 시 재판정(관찰 항목). [ADR-0022] ② **스크리닝은 판정이 아니라 등록 규칙과의 기계적 대조**다(§1 비범위 비저촉의 논리): 미등재=규칙 행의 부재("안전"이 아니라 "근거 없음·확인 필요"), 함량 미입력 제한 성분=제한초과 버킷에 사유 표기(fail-closed), 전성분 0건=명시적 오류, 게이트 0·계산값 미저장(§5.3과 같은 원칙 — 부재를 아키텍처 테스트가 강제), 출력 어디에도 판정 워딩(적합/통과/합격) 금지 — 고정 문구 "참고용·근거 확인"과 함께 테스트가 지킨다. [ADR-0022] ③ **INCI명은 표기 보존 + 정규화 컬럼 유일**(공백 축약·대문자) — 'Aqua'/'AQUA'가 다른 행이 되면 유일키가 중복을 못 잡는다(§4.1 보강 ④의 HS 정규화와 같은 이유). 성분 규칙의 근거링크·최종확인일은 NOT NULL(ADR-03 — sku_hs_codes와 같은 지위). ④ `ingredient_rules.country_code`·`labels.country_code`는 markets(S2-1) 전까지 ISO alpha-2 문자열 — sku_hs_codes와 같은 처지이며 S2-1 계획 보고에서 FK 승격을 일괄 재판정한다. [ADR-0023] ⑤ **BOM 단가는 원가이며 필드 단위로 감춘다** — 조회 역할 응답에서 `unit_cost`·`currency` 필드가 **부재**하고(null이 아니다) 행·건수는 그대로 보인다. ADR-0018(행 단위)과 모순이 아니라 보완이다: 행 전체가 민감하면 행 단위(`sku_prices`), 원가 한 칸 외에는 인증·판정 업무 데이터인 **혼합 목적 행**이면 필드 부재(`product_boms`). 원가 파생 합계·롤업이 생겨도 같은 부재 규율을 따른다. [ADR-0024] ⑥ **BOM 단가는 이력을 두지 않는다**(ADR-0017 비적용) — 판정 시점의 값을 얼리는 것은 §6.2의 판정 BOM 스냅샷(S3-4)이고, 마스터에 이력을 겹치면 "그때 값"의 출처가 둘이 된다. ⑦ **라벨의 편집 지점은 SKU×시장 하나**이고(§4.5), §14 ③의 제품 상세 '라벨' 자리는 소속 SKU 라벨의 **읽기 전용 롤업**이다. 승인상태는 3값(DRAFT/APPROVED/RETIRED) 고정까지이며 **전이는 강제하지 않는다**(상태 머신 명문은 §5.2 인증뿐). 라벨 판번은 낙관적 잠금 `version`과 별개 컬럼(`label_version`)이다. 라벨의 유일키는 **(SKU, 시장, 언어, 판번)** 부분 유니크(`WHERE deleted_at IS NULL`) — 언어를 빼면 같은 시장의 타 언어판(캐나다 영문 v1 후 불문 v1)이 판번 증가를 강요받아 §4.5의 컷인 기준이 오염된다. [ADR-0025 + 부기(S1-3)] ⑧ `materials.default_supplier_name`(→ §4.6 `partners`)·`labels.file_url`(→ §4.7 `documents`)은 S1-3 승격 대상 임시 문자열이다. [ADR-0020 부기] ⑨ `materials`의 **로트관리는 재고관리를 전제**한다(CHECK) — 원장에 없는 품목의 로트는 추적할 대상이 없다. 재고 편입 자체는 P4(§8.1)다.

> **[M1] 보강(S1-3)**: ① **거래처 유형은 교차 테이블**(`partner_type_links` — 거래처×유형 코드, §4.6 열거 10종 CHECK, 부분 유니크)로 담는다 — 한 거래처가 공급사이면서 포워더인 다중 유형(§4.6 문면)을 컬럼 플래그로 펼치면 유형 추가마다 스키마가 흔들리고, 배열 컬럼은 FK·CHECK 규율(ADR-0003)과 안 맞는다. [ADR-0026] ② **여신한도는 금액 규약 그대로**(정수 최소단위+통화 쌍 CHECK — ADR-0003)이며 **마스킹 비대상**이다(웹 세션 판정 2026-08-05): ADR-0018·0024의 보호 대상은 원가·마진이고 여신한도는 운영 필요 데이터다. **재판정 트리거: 여신 게이트 도입 세션(S3-1) 또는 역할 세분화 시.** ③ `customer_item_codes`의 유일키는 **(거래처, 바이어 품번)** 부분 유니크 — 같은 품번이 두 SKU를 가리키는 모순을 차단한다(§7.4 인테이크 게이트가 이 매핑으로 품번→SKU를 결정한다). 같은 SKU에 복수 품번은 허용(리뉴얼 실무). ④ `signatories`는 **최소 헤더**(성명·직함·메모)까지 — §4.7에 컬럼 명세가 없어 추측 구현을 금하고, 상세는 소비 세션(S4-4 법정 대장)이 재판정한다(ADR-0021 방식). ⑤ ADR-0020 승격 4건 중 **제조사·기본공급사가 `partners` FK로 승격** 완료 — `skus.manufacturer_name`→`skus.manufacturer_partner_id`(유형 OEM), `materials.default_supplier_name`→`materials.default_supplier_partner_id`(유형 SUPPLIER). backfill은 트림·공백 정규화 후 distinct당 1행 생성, 문자열 컬럼은 제거(downgrade는 역복사). 둘 다 **NULL 허용 유지**(ADR-0020 필수화 재판정 — 차단 지점은 등록이 아니라 §7.7 게이트다). MSDS·라벨 파일의 `documents` 승격은 S1-3 후속 PR. ⑥ **documents(§4.7)는 폴리모픽 소유 + 열린 종류 코드 + FILE/LINK 2형이다** — 소유 열거는 소비분 2종(SKU·LABEL) 한정으로 후속 세션이 CHECK 확장(ADR-0021 규율), 종류 코드는 `document_types` 데이터(ADR-11 — §4.6 유형 10종 CHECK와 달리 §4.7 열거는 예시적 문면)로 시드는 §4.7 열거 9종+라벨 파일(§4.5 승격용), 해시 멱등은 (소유자, sha256) 부분 유니크(용량 변형이 같은 MSDS를 공유하는 실무 수용). 원산지 증빙 보존기한은 `retention_years=5` 데이터 + 발급일 필수이며, 파기 잠금은 보존기한 내 soft delete 거부(서비스 강제, 강제 삭제 액션 없음). 구 CHECK의 "SET SKU MSDS 금지"는 서비스 가드+테스트로 승계(웹 세션 승인 — 타 테이블이라 CHECK 불가). [ADR-0028] ⑦ **파일 보안 4항** — 저장 파일명=서버 생성 hex(사용자 파일명은 표시 전용), 저장 루트=정적 서빙 경로 밖 단일 디렉터리(접근은 권한 검증 다운로드 엔드포인트 하나·S2-4 세트 백업 전제), 다운로드 attachment+octet-stream+nosniff 고정(inline 렌더 금지·허용 확장자 목록에 실행물·html 부재), 크기 상한 20MiB 명시 수치+테스트 고정(운영 nginx 25m의 안쪽). [ADR-0029] ⑧ §4.8 서류 세트는 `item_profile_document_types`(품목군×종류, 부분 유니크)로 연결 완료 — "신규 등록 시 자동 적용"은 세트를 소비할 요건·태스크가 서는 후속 세션 몫(ADR-0021).

---

## 5. M2 — 인증·규제 엔진 (시장 준비도의 심장)

**5.1 3층 모델**: requirement_templates(시장별 요건 정의: 적용단위[처방/SKU/시설/기업/성분], 유형, 선행요건, 유효기간, 갱신주기·리드타임, 비용, 근거링크·확인일) → certifications(대상×템플릿 인스턴스) → certification_tasks(체크리스트, 서류 링크).

**5.2 상태 머신(코드 고정)**: 미착수→서류준비→신청제출→심사중⇄보완요청→승인(유효)→만료임박(날짜 조건 자동 부여)→갱신중→… / 반려·만료·중단(사유 필수). 전이 외 변경 거부, 이력 자동.

**5.3 시장 준비도 매트릭스(메인 화면)**: 행 SKU×열 시장, 셀=필수 요건 집계 신호등(🟢판매가능/🟡진행·임박/🔴미충족/⚪대상외). 저장하지 않고 계산. 채널 리스팅 생성의 게이트.

**5.4 대행 협업(파트너 허브 공통 패턴)**: 처리방식[직접/대행], 현재 액션 주체[사내/대행사/기관] + 정체 N일 알림, comm_logs(요지·첨부·다음액션·기한), 전달 서류 zip 패키지, 대행 계약, 스코어카드(소요일·보완율·비용) — 이 패턴은 포워더·관세사에도 동일 적용(§7.9).

**5.5 글로벌 시드(Tier — 템플릿 준비 우선순위이며, 시드 데이터 투입은 실제 진출 시장분만)**: T1 — 미국(MoCRA: 시설등록 격년·제품 리스팅 연간·US Agent·라벨·부작용 보고), EU(RP·CPNP·PIF/CPSR 10년), 영국(UK RP·SCPN), 중국(NMPA 비안/허가·경내책임자·CBEC 별도 템플릿), 일본(제조판매업 파트너·성분 포지티브/네거티브·의약부외품 판별), 아세안 6(ACD Notification, 인니·말레이 할랄 트랙), 캐나다(CNF), 호주(AICIS/TGA). T2 — 뉴질랜드, 대만(TFDA), 홍콩, 인도(CDSCO), 중동(사우디 SFDA·UAE 등 현지 대리인형), 러시아/EAEU(EAC), 아세안 잔여. T3 — 중남미(안데안 NSO: 콜롬비아 INVIMA·페루·에콰도르·볼리비아 / 브라질 ANVISA / 멕시코 COFEPRIS / 칠레 / 아르헨티나), 튀르키예, 이스라엘, 남아공, 스위스·노르웨이. 신규 시장 개설 위저드(5단계: 시장→템플릿→HS·세율→협정→성분규칙) + AI 규제 리서치(웹검색) 결과의 템플릿 초안 스테이징 변환(근거 링크 없는 항목 확정 차단).

**5.6 공통 수출 서류**: CFS·GMP증명·CoA는 체크리스트가 참조하는 공용 문서 — 유효기간 연동.

> **[M2] 보강(S2-1 PR-1)**: ① **markets가 서고 국가 축 승격이 종착했다** — `markets.code`(대문자 2자 CHECK)는 **전역 UNIQUE**다(unique_active 부분 인덱스가 아니다 — 코드 참조 FK의 대상이라 부분 유니크로는 FK가 성립하지 않고, soft delete된 코드의 재사용은 신규 행이 아니라 **복원(명시 액션)**이다. §17.4의 부분 유니크 규율은 멱등 키 대상이라 상충하지 않는다). `labels`·`ingredient_rules`·`sku_hs_codes`의 country_code는 **컬럼·유일키·CSV 불변**으로 markets.code를 값으로 참조하는 FK가 얹혔다(ADR-0023 종착 — ADR-0032). 신규 라벨·규칙·세번 등록은 **시장 선등록이 전제** — 미등록 시장은 422로 선등록을 안내한다(FK는 안전망). 승격 백필 행은 MIG 계보(note 표식·이름=코드)이며 검수는 반입 재개 시 MIG 큐 합류. ② 시장·요건 템플릿의 편집 권한은 §2 권한 절(인증+관리자 — 무역·물류·조회 열람). ③ 중국 CBEC는 시장 축이 아니라 템플릿 축이고(§5.5 문면 "별도 템플릿"), Tier(T1~T3)는 우선순위 문서 개념이라 컬럼이 아니다. [S2-1 계획 웹 세션 판정 2026-08-07]

> **[M2] 보강(S2-2 PR-1)**: ① **certifications·certification_tasks·certification_status_log가 섰다** — 인스턴스는 대상×템플릿(§5.1)이고 대상은 폴리모픽 5값(target_type=적용단위 열거·생성 시 일치 강제. 매핑: PRODUCT→products / SKU→skus / INGREDIENT→ingredients / **FACILITY→partners는 잠정**[복수 공장 실수요 시 facilities 마스터 재판정 — 관찰 원장] / COMPANY→자사 단일·target_id NULL·전용 부분 유니크). 생성은 **CONFIRMED 템플릿에서만**(3층 확정 게이트의 소비자 — DRAFT·RETIRED 422, 기존 인스턴스는 은퇴 후에도 유지), 요건 정의 7필드(이름·유형·유효기간·갱신주기·리드·근거 2필드)를 **스냅샷으로 동결**(템플릿 개정 비소급 — "그때 요건". estimated_cost는 명시 예외: 실비=S3-4 원장·예상비 최신값=템플릿 참조)하고 체크리스트를 태스크로 참조 복사한다(ADR-05). 활성 한정 유니크 술어=`deleted_at IS NULL AND status NOT IN ('REJECTED','SUSPENDED')` — 종결 2태만 유니크 밖(재도전=신규 인스턴스), 만료는 안(만료 후 재개=기존 행 경유). [ADR-0037] ② **§5.2 상태머신의 코드값·전이 표(코드 고정 — ADR-11)**: 상태 11값 = NOT_STARTED(미착수)/PREPARING(서류준비)/SUBMITTED(신청제출)/IN_REVIEW(심사중)/SUPPLEMENTING(보완요청)/APPROVED(승인·유효)/EXPIRING(만료임박)/RENEWING(갱신중)/REJECTED(반려)/EXPIRED(만료)/SUSPENDED(중단). **허용 전이 27방향 = 사람 21 + 자동 6, 미허용 83 — 전건 거부**(§5.2 "전이 외 변경 거부"). 사람 21: 미착수→서류준비 / 서류준비→신청제출(부속: 신청일) / 신청제출→심사중 / 심사중⇄보완요청 / 심사중→승인(부속: 승인일·유효시작 필수, 만료일[무기한 허용]·인증번호 선택 — **승인 진입은 이 전이와 갱신중→승인 둘뿐, 둘 다 사람 기록**[4금 ① 법적판정 비저촉·§1 비범위]) / 승인→갱신중(조기 갱신 — 리드 부재 구제) / 만료임박→갱신중 / 만료→갱신중(늦은 갱신 — 만료 후 재개의 단일 경로) / 갱신중→승인(부속 선택 — 생략=갱신 취소 복귀) / 심사중→반려·보완요청→반려(사유 필수) / 비종결 9상태 각→중단(사유 필수 — 미착수→중단은 §4.8 자동 생성분의 처분 출구). 자동 6: 달력 파생 3태 {승인, 만료임박, 만료} 상호 전부 — 만료일·리드타임(스냅샷)의 순수 함수로 수렴(부여: 승인→만료임박·만료임박→만료·승인→만료 직행 / 해소 복귀: 만료임박→승인·만료→만료임박·만료→승인 — 부여만 있고 해소가 없으면 정정 후 상태가 거짓이 되는 비대칭 결손). **만료는 종결이 아니라 달력 파생 상태**(사유는 자동 기록 — §5.2 "사유 필수"의 충족 방식)이고 **종결은 반려·중단 2태뿐**(탈출 0). **갱신은 직행 왕복**(재진입 배제 — 갱신 신청의 반려가 유효 인증을 종결시키는 축 혼동 차단. 갱신 절차 세부=태스크·comm_logs[S2-4]). 갱신중은 스윕 비대상(도과 표시·알림=S2-3 계산값·기일 엔진). **달력 즉시 수렴**: 3태 진입 전이·만료일 정정 직후 같은 트랜잭션 말미에 해당 행 수렴 — 커밋 시점에 상태·달력 상시 정합. 차단 3층 = DB(상태값·사유·기간 순서·COMPANY CHECK — **CHECK는 행 내 검증이라 전이 규칙 자체는 강제하지 못한다**: 전이 본체는 서비스 층 몫이고 아키텍처 테스트가 우회 부재를 고정한다) / 서비스(전이 표 단일 상수·상태 대입 단일 통로·행 잠금+version 409+멱등 키·PATCH와 생성 스키마에 status 구조적 부재+extra 거부) / 아키텍처 테스트(총수 27/83·자동 전이 3태 한정·도달성·탈출성·상태 대입 스캔). 만료일 정정 PATCH는 3태에서만. [ADR-0038] ③ **만료임박 "배치"(WBS DoD)의 충족 형태 = 날짜 스윕 함수의 실재 + 직접 호출 검증** — 실행 수단은 CLI(certification-sweep) 수동 호출뿐이고 scheduled_jobs 등록·스케줄은 S2-3 실행기 몫(§15 레지스트리 충족 시점도 그때 — 그전까지 0행 불변). 건별 독립 트랜잭션(§17.6)·행 잠금 후 상태 재확인(§17.2)·멱등. [ADR-0039] ④ **certification_status_log는 §17.5 확장 불변 테이블**(아래 §17.5 부기·ADR-0040) — 전이마다 자동 기록(from·to·사유·행위자[NULL=시스템]·전이 시점 만료일·인증번호 스냅샷[재승인 덮어쓰기의 "그때 값" 재구성 — §3 스냅샷 규율]). ⑤ 아웃박스 발행: 생성+전이 전건(사람·자동 공히, automatic 표식) — S2-3 기일·알림의 원료(디스패처 부재 단조 증가=부채 #11 의도 상태). ⑥ 인스턴스·태스크의 편집 권한은 §2 계보(인증+관리자 — 무역·물류·조회 열람), 태스크는 인스턴스별 추가·편집·soft delete 자유(ADR-11 "태스크는 자유"). [S2-2 계획 웹 세션 판정 2026-08-11 — 안건 ①~④·⑦·⑩·⑫·조건 A·B·E] — 적용단위 5값 CHECK(PRODUCT/SKU/FACILITY/COMPANY/INGREDIENT — WBS DoD 문면), 유형은 코드 문자열(형식 CHECK — 별도 유형 테이블 승격은 소비 발생 시 재판정, ADR-11), 상태 3값(DRAFT/CONFIRMED/RETIRED — ADR-0025 계보), 주기 3필드 nullable·positive(주기형과 1회성 공존), `estimated_cost_amount`+`estimated_cost_currency` 쌍 CHECK(금액 규약 ADR-0003 ④ — **비마스킹**, 마스킹 원장 #3·ADR-0026 계보). ② **근거 2필드(source_url·last_verified_on)는 nullable이되 확정이 3층 게이트다**(ADR-0033): DB CHECK `confirmed_requires_evidence`(+공백 URL 차단) / 서비스(확정=사람 1클릭+idempotency key, 근거 결여 422 — GC-C8. **CONFIRMED 행의 내용 편집은 409 차단, 재확정 경로는 초안 전환 명시 액션뿐**, PATCH의 status에 CONFIRMED 불가. **confirm은 DRAFT 상태에서만 — RETIRED 확정 409, RETIRED 재활성은 DRAFT 경유(근거 최신성 재검토 강제. #16 보완 판정 2026-08-11)**) / 자동 확정 경로 부재 아키텍처 테스트. ③ **선행요건은 교차 테이블**(template_prerequisites — 다중 선행 실재 판정, 자기참조 DB CHECK+순환 서비스 거부, ADR-0034). ④ template_checklist는 (템플릿, 순번) 부분 유니크·document_type 참조는 nullable(서류 없는 행정 절차 실재 — §5.6은 서류 참조 항목만 연결), S2-2가 certification_tasks로 참조 복사한다(ADR-05). ⑤ §4.8 요건 세트는 item_profile_requirement_templates로 연결 완료(부채 #15 S2-1 몫 — ADR-0035), "신규 등록 시 자동 적용"은 S2-2 계획 보고로 명시 이월. [S2-1 계획 웹 세션 판정 2026-08-07 — 안건 1·3·4·조건 1·7·8·11]

> **[M2] 보강(S2-2 PR-2)**: ① **documents 소유 열거에 CERTIFICATION 추가** — (SKU, LABEL, CERTIFICATION) 3종 CHECK(기존 테이블 CHECK 수기 재정의 + owner_type 10→13 확폭 — 'CERTIFICATION' 13자). 확장은 소비분 1종까지(안건 ⑤ 과확장 억제 — GMP류 공용 서류의 소유 공백은 관찰 등재, 트리거: S2-4 전달 서류 zip 또는 실등록 수요). 등록·삭제 권한(무역+인증)·파일 보안 4항·해시 멱등은 §4.7 계보 그대로 적용된다. [ADR-0041] ② **태스크 서류 링크 실장(§5.1 "서류 링크")** — certification_tasks.document_id 편집은 태스크 PATCH의 3값 의미론(필드 생략=미변경 / null=연결 해제 / 양수=연결·활성 문서 실재 검증 422). 소유자·종류 일치는 강제하지 않는다 — §5.6 공용 참조(CFS·CoA는 SKU 소유 문서를 여러 태스크가 참조)가 정본 용법이고, 문면에 없는 차단은 두지 않는다(종류 안내는 화면 필터 몫). ③ **§4.8 "신규 등록 시 자동 적용"의 요건 세트 몫 실장(안건 ⑥ — 부채 #15 종결분)** — 신규 제품·SKU 등록과 **같은 트랜잭션**(§17.1) 안에서 품목군 요건 세트 중 CONFIRMED이면서 적용단위 일치(제품→PRODUCT, SKU→SKU)인 템플릿 전건에 미착수 인스턴스+태스크(체크리스트 참조 복사)를 생성한다. 멱등 = 활성 한정 유니크 술어의 **사전 존재 확인**(기존재 시 생성 생략 — 예외 경로는 합류한 등록 트랜잭션을 오염시켜 배제). 행위자=등록 조작자, 아웃박스 created 이벤트에 automatic 표식(판정 ⑩ 전이 이벤트와 대칭). SET SKU 제외 규칙은 두지 않는다(수동 생성 경로와 같은 기준 — 세트의 판정 롤업은 §5.3 매트릭스 몫). **소급 3계열 비포함**(품목군 사후 지정·세트 추가 소급·FACILITY/COMPANY/INGREDIENT — 수동 등록 경로가 흡수, 관찰 원장 등재). 서류 세트·마일스톤 세트의 자동 적용은 각 소비 세션 몫(ADR-0021 규율 유지). [ADR-0042] [S2-2 계획 웹 세션 판정 2026-08-11 — 안건 ⑤·⑥]

---

## 6. M3 — FTA·원산지 (계산은 시스템, 확정은 사람)

**6.1 협정 마스터**: agreements(증명방식[기관/자율/인증수출자/병행], 자율발급 임계(한-EU 6,000유로), 신고문안 원형 — 자구 수정 금지 강제, 직접운송 규정, RVC 산식·기준율, **협정별 적용 HS 버전**) + agreement_countries 다중 매핑. 시드 = 한국 발효 FTA 전체(칠레/싱가포르/EFTA/아세안/인도/EU/페루/미국/튀르키예/호주/캐나다/중국/뉴질랜드/베트남/콜롬비아/중미5/영국/RCEP/이스라엘/캄보디아/인니/필리핀 + 신규 타결분 [발효 확인 필요]). 다중 협정국은 유리 협정 비교 화면(PSR 난이도·세율·증명방식 3축).

**6.2 판정**: origin_determinations(제품×협정, hs6, **hs_version**, PSR, 방법[WO/CTC/RVC/선택/결합], 결과[충족/불충족/추가확인], 판정 시점 BOM 스냅샷 동결, 유효상태). 재판정 트리거 자동: BOM 변경·공급사 변경·확인서 만료·단가 변동 RVC 경계·HS 개정·협정 개정 → "재검토필요" 전환+태스크.

**6.3 계산기(확정 아님)**: CTC — 비원산지 자재 전수 HS6 대조표(미상=역외 간주, 미소기준 보조). RVC — 협정별 산식(공제/집적)·기준율 데이터 소비, 경계선(±5%p) 상시 플래그. 불인정공정 체크리스트. 출력 3단계+[공식 확인 필요] 분리, 판정 확정 버튼 별도.

**6.4 증빙 자동화**: 포괄확인서 수취 루프 — supplier_origin_confirmations(공급사×자재, 포괄기간≤1년) 만료 D-45 스캔 → 재수취 요청 메일 초안 생성(발송 레벨[초안/자동]은 §15 outbound_policies가 지배) → 미수취 시 자재 역외 강등 → 연관 판정 전건 재검토 전파. 원산지소명서는 판정 데이터에서 자동 생성. 법정 대장(C/O 발급·확인서 수취·서명)은 데이터에서 출력(시스템이 곧 대장). 국내제조확인서 동일 구조.

**6.5 인증수출자**: 신규 모듈 없이 M2 엔진 재사용(업체별/품목별×협정, 유효 5년, 갱신 리드타임).

**6.6 C/O 워크플로우**: 수출 — 선적→협정 후보→판정 유효성 게이트(재검토필요면 발급 차단)→실익 계산(MFN−협정세율)×금액−발급비용 → 기관발급(신청 패키지 자동 구성) 또는 자율발급(신고문안 자동 생성, 임계 초과+인증수출자 번호 미보유 시 차단). 수입 — 수취 C/O 검증 체크리스트 + 사후적용 스캔(미적용 수입 건 자동 검출, 수리일+1년 경정청구 환급 기회 리포트).

**6.7 사후검증**: 이슈 킷(§7.8) 유형 — 회신 기한 최우선 데드라인(도과=원산지 부인), 케이스 파일에 판정 스냅샷(당시 hs_version 포함)·BOM·확인서·공정도 자동 포함, 회신문 초안(AI). 직접운송·삼각무역 플래그: 제3국 경유·스위치 B/L 건 자동 감지 → 비가공증명 태스크, 중계무역 C/O 제약 경고.

---

## 7. M4 — 무역 실행 (견적부터 채권까지)

**7.1 전표 사슬**: `QT → PI → SO → 선적(부분 1:N) → CI/PL → 수출신고 → C/O → 채권 → 입금`, `PO → (수입)선적 → 통관 → 입고(부분)` — 입고 라인은 수입 선적 라인을 참조 생성, PO 잔량은 입고 확정 시 차감, IN_PO 원장 ref는 입고 문서를 가리킨다. 잔량(open qty)이 부분·초과 방지의 기준. 결제조건은 구조화(기산점+일수) — 자유 텍스트 금지.

**7.2 전표 상태 열거(코드 고정 — ADR-11)**:
- QT: 작성→발행→(수주전환|만료|취소)
- PI: 발행→일부입금→입금완료 / 만료·취소
- SO: 접수(스테이징)→확정→할당(부분/완료)→선적진행→완료 / 취소·보류(사유)
- 선적: 계획→**출고지시**→피킹→검수완료→**출고(원장 기록 시점)**→선적(ETD 실적)→종결 / 취소 — CI/PL 생성은 검수완료 이후만 허용
- PO: 발행→공급사확인(OC)→(수입선적 연결)→부분입고→입고완료→종결 / 취소

**7.3 PI**: proforma_invoices(견적 참조, 선수금%·잔금 기산점, 은행정보 스냅샷, 유효기간) — 입금 매칭 시 상태 자동 전환. 선수금 미입금 SO 확정 게이트(경고/차단 설정)는 **결제유형이 선수금 T/T일 때만 활성**(L/C 등 타 결제는 비활성).

**7.4 오더 인테이크**: 바이어 PO PDF/이미지(AI 추출)·이메일 본문(AI 파싱)·엑셀 대량·채널 — 전부 스테이징 한 입구. 게이트: 바이어 품번 매핑(미매핑 시 등록 유도), 단가 편차(±허용치), 여신, 시장 준비도, MOQ, 중복 PO 번호. 확정 → 할당/백오더 분기(§8.3) → 선적 계획 초안(프로파일 마일스톤) → 서류 태스크 원클릭 체인. 오더 보드(파이프라인 뷰+벌크 액션+저장 필터).

**7.5 선적·기일 엔진**: shipments(Incoterms+장소·연도, 환율 고정, 구분[수출/수입/채널입고/샘플·무상]) + milestones(서류마감/Cargo Closing/ETD/ETA/신고수리/적재기한/대금만기/수출 전 검사(PSI)/수입 세금 납부기한). 자동 계산: 대금만기=기산점+일수(**결제유형 분기**: T/T=약정 기산점 / L/C=네고·인수 기준), 적재의무=수리일+30일, (L/C) 제시기한=MIN(B/L일+21, 유효기일). 실적 입력 시 후속 재계산, 국가별 휴일 캘린더 반영(ETA 현지 연휴 경고), 스케줄 변경(롤오버) 이력+통보 기록. 선적 캘린더 뷰. (Phase 7 이후 옵션: 바이어용 읽기 전용 선적 추적 링크)

**7.6 서류 생성기**: QT·PI·CI·PL·S/I·DGD 전부 선적/전표 한 원천에서 템플릿 렌더링(PDF·엑셀, 언어 변형 지원). 저장 전 검증 강제: 수량×단가=금액·라인합=총액(반올림 상수), G.W.≥N.W., Incoterms 완전성, CI↔PL 교차 일치. B/L·AWB는 생성하지 않는다(발행 주체=운송인) — 대신 S/I 생성 + B/L draft 업로드 → AI 추출 → 자동 대조(당사자·품명·수량·중량·포트·문구) → 불일치 리포트+수정요청 초안(불일치 0이면 적합 확인 자동 기록).

**7.7 DG 게이트**: DG SKU가 선적 라인에 포함되면 — 모드 제약 체크(규칙 데이터), MSDS 유효본 미첨부 차단, DGD 태스크(화주 작성 책임 상기), 포장·라벨 체크리스트(LQ 마크), RFQ·S/I에 DG 자동 기입, DG 미취급 파트너 소싱 제외. (수치는 IATA DGR·IMDG 최신판 확인 원칙)

**7.8 통관 이슈 킷**: issues(발생지, 유형[서류불일치/HS이의/인증요구/검사/가격심사/원산지검증/**부작용(AE) 보고** — 접수일 기준 법정 보고기한을 기일 엔진에 연결], 심각도, 데드라인, 액션주체, 비용 링크). 원클릭 케이스 파일(전 서류 zip+타임라인 PDF), 체류비용 카운터(일할 누적 가시화), 유사 사례·지식 노트 사이드 표시, AI 소명·대응 메일 초안, 종결 시 "배운 것 기록" → M10.

**7.9 파트너 협업 허브(§5.4 패턴의 전면 적용)**: 포워더 — 부킹 요청서 생성·S/I 전달·draft 회신 기한 추적·운임 RFQ. 관세사 — 통관 패키지 zip·신고 상태 추적·질의 기록. 커뮤 템플릿 라이브러리(부킹/송부/독촉/일정변경), 파트너별 회신 SLA 초과 정체 알림.

**7.10 채권·입금**: receivables(만기 자동 — 결제유형 분기 반영)·payments(부분 허용, 은행 CSV 매칭은 Phase 7 이후), aging 30/60/90, 여신 초과 수주 승인 게이트. L/C flag ON 시: lc_terms 등록(MT700 인테이크 → 초안), 하자 체크리스트 화면(lc-doc-check 점검 항목 체크박스+근거), tolerance 상·하한 자동 표시, 유효·선적기일 임박 적색.

---

## 8. M5 — 재고·로트 (원장 + 할당 + 검수 + FEFO + 리콜)

**8.1 위치·로트·주체**: locations(자사/3PL/채널창고[FBA 등 시장별]/외주처/운송중/**격리(QUARANTINE)**), lots(제조번호·제조일·유통기한·CoA 시험성적서 링크). **원장 주체 = 품목(SKU 또는 재고관리 대상 자재 — materials 플래그로 선택 편입).** 로트 없는 재고 금지(유일한 예외: 기초이월 OPENING의 레거시 로트 — `LEG-` 접두, 유통기한은 보수 추정 필수 입력).

**8.2 이동유형(코드 고정)**: IN_PO(+입고) / OUT_SHIP(−수출출고) / OUT_CH(−채널판매출고) / TRF쌍(위치이동) / ASSEMBLY·DISASSEMBLY(세트 키팅: 구성품∓·세트± 한 트랜잭션, 세트 로트 유통기한=구성품 MIN) / PROD_CONSUME(사급·생산 소비 — 백플러시) / RECEIPT_DIFF(수취차이) / OPENING(기초이월) / ADJ±(실사조정, 사유 필수) / DISPOSAL(폐기 — 승인 후) / RETURN_IN(반품입고: 판매가능=일반 위치, 판매불가=격리 위치). 원장은 **기본 단위(EA)로만** 기록 — 박스 입력은 화면 환산("3 BOX = 36 EA"). 수량=정수, 중량·CBM=소수(자릿수 상수).

**8.3 할당·가용재고**: **가용재고 = 현재고 − 유효 할당 − 격리 위치분.** 할당(allocations)은 SO 확정 시 기록(품목·수량·SO라인 ref·로트지정 옵션·상태[유효/소진/해제]) — 출고 검수 확정 시 소진, SO 취소 시 해제. 할당 생성·소진은 §17.2 직렬화 잠금 대상. 백오더(§11)의 '가용재고'는 이 정의. 잔량 파생 테이블 유무·갱신 방식(원장 SUM vs 유지형+검산)은 Phase 4 착수 ADR로 확정하되 원장이 진실(ADR-04)은 불변.

**8.4 출고 검수(오출하 차단)**: 선적 '출고지시' 시 **피킹 리스트 자동 생성**(품목·로트·위치·수량) → 창고 피킹 → 검수 화면(리스트 vs 실물 대조 — 바코드 스캔 또는 수량 체크, 로트 불일치·수량 차이 시 확정 차단, FEFO 내 로트 교체 허용+사유) → **검수 통과 후에만 OUT_SHIP 원장 기록+'출고' 상태 전환.** 검수자·시각 기록(shipment_inspections). PL의 로트 표기는 검수 확정 로트 기준 — 서류·실물 일치의 원천.

**8.5 사급(OEM) 흐름**: 자재 구매 입고(IN_PO, 자사) → 사급 출고 TRF(자사→외주처) → OEM 완제품 입고 시 BOM 기준 자재 차감 PROD_CONSUME(외주처 위치, 백플러시) — 외주처 위치가 사급재고 대사 지점. 수율 차이 등 세부는 Phase 4 원장 ADR에 포함.

**8.6 채널입고 2단**: TRF(자사→운송중[채널향]) → 수취 확정 시 TRF(운송중→채널창고) + 차이분 RECEIPT_DIFF(운송중에서 차감, 클레임 근거) — 미수취 잔량이 원장 안 "운송중"에 남아 수취 대사의 기준이 원장 그 자체가 된다.

**8.7 FEFO·유통기한**: FEFO 출고 제안 + 채널별 최소 잔여 유통기한 차단(§9), 임박 리포트(D-180/90/30)·폐기 승인 워크플로우. **리콜 추적 1클릭**: 로트 → 원장 ref 역추적 → 선적·채널별 유통 경로 재구성.

**8.8 실사·보충**: 실사 = 스냅샷 방식 + **실사 시작 시 해당 위치 입출고 잠금**(긴급 출고는 관리자 해제+해당 실사 무효 경고). ROP: 일평균판매 원장 자동 산출, `가용+미입고+운송중 ≤ ROP` 권고 발주(자동 발주 금지). 마이너스 재고 정책은 Phase 4 착수 시 확정(차단 vs 경고+일일 리포트). 시리얼 모듈은 feature flag.

---

## 9. M6 — 채널·풀필먼트 (FBA 골격의 프로파일화)

channels + channel_fulfillment_profiles(입고 라벨 규칙, 박스·중량 제한, **최소 잔여 유통기한 기본값**, 입고지 위치, 정산 리포트 매핑, 반품 흐름, 리드타임, 위탁/사입 구분) — channel_shelf_rules는 SKU×채널 **예외**(예외가 기본값에 우선). FBA·Shopee(SLS)·Qoo10(Qxpress)·자사몰이 같은 9단계 골격을 프로파일 교체로 공유: ① 시장 준비도 게이트(🟢 아니면 리스팅 차단, override 사유 기록) ② 리스팅(ASIN/FNSKU, 심사 추적) ③ 수입 체계 — IOR·Customs Bond·EORI·VAT를 "기업 단위 요건"으로 M2가 관리(아마존은 IOR 불가 명시) ④ 입고 계획(선적 구분=채널입고, 라벨·유통기한 로트 검증) ⑤ 통관 ⑥ 수취 대사(§8.6 — 차이는 RECEIPT_DIFF+클레임 태스크) ⑦ 재고 운영(판매속도·재고일수·장기보관 임박) ⑧ 반품(리포트 업로드→RETURN_IN 분류, 판매불가=격리) ⑨ 정산 대사(리포트 업로드→주문 매칭→미매칭·차이만 사람). 채널 주문은 채널주문번호 UNIQUE 멱등 임포트, 소비자 개인정보 비보관.

---

## 10. M7 — 비용·요율·소싱·환노출

**10.1 비용 원장**: expense_types(설정화: 운임/THC/CFS/D-O/유류/내륙/통관수수료/관세/수입부가세/검사/보관/체선체화/보험/인증비/채널수수료/환급(−)) + expenses(참조 전표, 단계[견적→발생→청구→지불], 예상·청구·확정액, 증빙, 지불예정일). Incoterms 연동 부담자 기본값(수정 가능·근거 기록). 수입 관세·부가세 납부기한 기일 연동. landed cost는 이 원장에서 자동 집계(배부 기준 상수) → **lot_costs**(로트×배부 차수, 재배부 이력 보존 — lots에 덮어쓰지 않음) → SKU×채널 손익.

**10.2 요율 카드**: 포워더·관세사·3PL·인증대행 4유형 통일 구조(종류[스팟견적/계약요율], 경로·모드·구간[컨테이너/CBM/중량/건당/보관CBM·일]·최소요금·유효기간 — 만료 D-30 재견적 태스크). RFQ 회신이 AI 추출로 카드 초안이 되어 입고(확정은 사람 — ADR-08).

**10.3 RFQ 컴포저**: 케이스 폼 1회(품목·HS, 포장·스태커블, CBM/중량, 인보이스 금액, Incoterms, 모드, DG+MSDS, 온도, 주소, 통관 주체, 희망 스케줄, 보험, L/C, 회신 기한) → 유형별 국/영문 템플릿 다건 생성. 회신 리마인드 자동(§15 정책).

**10.4 소싱 추천(자동 선택 아님)**: 케이스 → 유효 요율 전건 all-in 시뮬레이션(최소요금·구간 반영) → 가중치 랭킹(비용/리드타임/스코어카드) → 1순위 권고+사람 확정, 권고 외 선택 시 사유. 단독 요율 시뮬레이터(영업 원가 산정용).

**10.5 절감 측정**: 기준선 강제 택1(차순위 견적/직전 계약가/전년 동월) — 보고서에 기준 명기. 월별·경로별·조합별 절감 대시보드 + PDF 보고서.

**10.6 월 비용 마감**: 해당 월(**귀속 판정 = 전기일 기준**) 미확정 비용 리스트 → 청구서 미도착 독촉 → 대사 완료 → 마감 잠금(이후 정정 기록만) → 정산 리포트+분개 초안 일괄. 마감률 진행바.

**10.7 환노출**: 통화별 포지션(미수−미지급) 대시보드 + 선물환 기록·만기(기일 연동) — 헤지 판단은 사람.

---

## 11. M8 — RMA·백오더·클레임

**RMA**: 접수→승인→회수중→입고검수→처리(재판매/재작업/폐기→손실 분개 초안/교환출고=신규 선적 참조)→종결. 유형에 수출 반송(통관 거부·전량 리턴) 포함 — 재수입 면세 요건 체크리스트 태스크. **클레임**: 대외 분쟁·구상 트랙(사진·보험·책임귀속), 합의 시 **credit_notes 발행 → 채권 차감** — RMA와 상호 링크. **백오더**: 잔량−가용재고(§8.3 정의)로 계산되는 보드, 예상 가용일이 PO 입고예정에 자동 추종, 변경 시 고객 안내 초안+이력, 할당 룰 설정, 지연 에스컬레이션.

---

## 12. M9 — 연동·파일 파이프라인·회계 인터페이스

**12.1 파일 파이프라인(ADR-09)**: 업로드/인바운드(해시 중복 감지)→파서 라우팅→스테이징(원본 나란히 검토)→확정(원본 문서 각인). 엑셀 = 매핑 템플릿 재사용+BOM 처리, PDF = 텍스트/비전 AI 추출(벤더 인보이스→PO·비용, B/L draft, 인증서, 정산서, **L/C 통지서 MT700→lc_terms 초안**), 인젝션 격리(문서 내 지시문 무시). **거부·수정 이력 보존** — 파서·프롬프트 개선 데이터.

**12.2 범용 엑셀 프레임워크**: 임포트 레지스트리(SO 대량/로트 입고/실사/마스터/가격/비용/시리얼 — 표준 양식 다운로드=스키마 문서), 왕복 편집(목록 다운로드→오프라인 수정·취합→업로드→변경분 diff만 스테이징), 오류 행 리포트(행번호+사유) 왕복. 전 목록 UTF-8 BOM 내보내기.

> **§12.2 보강(S1-3)**: 전 목록 내보내기는 **CSV 수식 이스케이프**를 거친다(§18.1 연계) — **문자열 타입 셀**의 선두가 `=` `+` `-` `@` 탭 CR `'`이면 `'` 하나를 접두하고(숫자·금액 타입 셀은 비대상 — 음수가 텍스트로 변하는 부작용 차단), 왕복 임포트 파서는 정확한 역변환(선두 `'` 다음 문자가 그 집합이면 `'` 한 개 제거)을 적용한다. 변환이 전단사라 왕복 값이 보존되고 diff 오검출이 없다. **보증 범위는 본 시스템 export→import 쌍 기준**이다. `'` 접두는 엑셀 자체의 텍스트 접두 관례와 일치해 중간에 엑셀 편집이 끼어도 일반 케이스는 보존되며, 극단 케이스(원값이 `'`+수식문자 시작으로 엑셀을 경유)는 업계 표준 트레이드오프로 수용한다 — 이 이상 과잉 방어를 더하지 않는다. [ADR-0027]

> **§12.2 보강(S1-3 PR-3)**: 왕복 편집의 구현 확정 — ① 왕복 CSV의 **1열은 ID**다: 빈 ID=신규 / ID 있는 행=변경분만 스테이징 / 파일에 없는 행=무시(삭제 아님) / ID 훼손·값 검증 실패=오류 행 리포트(행번호+사유, 헤더가 1행이라 데이터는 2행부터 — 엑셀에서 사람이 보는 번호). ② 표준 양식과 목록 내보내기는 임포트 레지스트리의 **한 헤더 상수**를 공유한다(자재 기본공급사는 **코드** 열 — 표시명은 유일키가 아니라 왕복 결정성이 없다). ③ 확정은 **원자적**이다: 스테이징이 행마다 대상 version을 기록하고, 확정은 전 행 대조 후 하나라도 어긋나면(선수정·선삭제) 전체 409로 거부한다 — 부분 반영은 사람이 검토한 diff와 다른 결과를 만든다(§17.2). ④ 같은 파일(sha256)은 검토 대기 중 한 번만(PENDING 한정 부분 유니크 — ADR-09 "파일 해시 멱등"의 M9 구현형), 확정 후 재업로드는 diff 0으로 수렴한다("동일 임포트 재실행 변화 0" — §20 J). ⑤ 확정은 **사람 1클릭 + idempotency key**뿐이다(§15 L2) — 마스터 임포트는 ADR-09 자동 확정 예외("인테이크 전표까지")의 범위 밖이며, 자동 확정 경로의 부재를 아키텍처 테스트가 고정한다. [ADR-0030]

> **§12.2 부기(S1.5 PR-1)**: SKU 왕복 양식은 **기록 가능 필드+ID만**(웹 세션 판정 ① 2026-08-06). 파생 표시 칸(제품명·브랜드명)은 왕복 양식에서 구조적 부재 — 표시 수요는 목록 CSV 몫이라 SKU만 왕복 내보내기가 표시용 목록 CSV와 분리된다. ②의 "한 헤더 상수"는 SKU에서 표준 양식↔왕복 내보내기 쌍에 적용. 종류·제품코드는 왕복 불변(변경 diff=오류 행). [ADR-0030 부기]

**12.3 ERP 연동**: external_refs 크로스레퍼런스(Phase 0), SoR 정책, 이카운트 웹업로드 양식/SAP 플랫 파일 내보내기(매핑 누락 차단). 모든 기능 REST 노출(OpenAPI 자동 문서).

**12.4 회계 인터페이스(기표 아님, 초안)**: posting_rules(이벤트[선적확정/입고/비용확정/입금/폐기손실/반품/재고조정]→차·대변 계정·코스트센터) → journal_drafts(**차=대 검산 강제**) → 이카운트/SAP 양식 내보내기. 월마감 패키지: 수출실적·재고수불(재고자산 금액=lot_costs×수량)·비용집계·미수잔액·분개 초안 일괄(귀속=전기일).

---

## 13. M10 — AI 레이어·지식·기능화

**13.1 Claude in ERP**: 모든 상세 화면 AI 패널, 백엔드가 엔터티 컨텍스트 자동 조립. 기능 = 프롬프트 템플릿 데이터: 이메일 초안(국/영문), 서류 크로스체크 보조, 문서 요약·번역, 규제 리서치(웹검색+근거 링크 필수)→템플릿 초안 변환, 자연어 조회(text-to-SQL 금지 — 사전 정의 읽기 전용 조회 함수 tool use), 회의 정리→태스크. ai_logs(토큰·비용·채택), 월 한도 차단, 개인정보·원가 전송 기본 제외. AI 출력은 무예외로 초안(ADR-08).

**13.2 지식→기능화**: knowledge_notes(노하우/법령[확인일 필수·분기 재확인]/사례/실수복기/SOP — 인수인계·교육·장애 운영 수칙 포함, 태그·엔터티 링크) — 관련 화면 사이드 자동 노출, 이슈 종결 시 기록 프롬프트. **projects**(출시명, 대상 SKU·시장 세트)로 신제품 출시의 인증·라벨·판정 태스크를 그룹핑 롤업. feature_requests 백로그 → 기능화 버튼: Claude Code 작업 지시서(설계 발췌+배경+수용 기준+§22 렌즈 체크리스트) 자동 조립→repo 커밋.

---

## 14. 화면 구성

① 대시보드(준비도 요약·D-30 만료·주간 마일스톤·미수 aging·유통기한·ROP·이슈·백오더·마감률·환노출) ② 시장 준비도 매트릭스(메인) ③ 제품 상세(기본/전성분·스크리닝/인증/원산지 판정/라벨/문서/채널/로트) ④ 인증 보드(칸반+캘린더) ⑤ FTA 관제탑(판정 커버리지·재검토·확인서 만료·검증·환급 기회) ⑥ 오더 보드 ⑦ 선적 관리(마일스톤 타임라인·서류 버튼·문서흐름 뷰·DG 표시·피킹/검수) ⑧ 선적 캘린더 ⑨ 재고(현황·원장·실사·리콜 추적) ⑩ 채널(주문 업로드·입고 계획·수취/정산 대사) ⑪ 비용·소싱(원장·요율 비교·RFQ·추천·절감) ⑫ RMA·클레임·이슈 ⑬ 보고서 센터(절감·월마감·파트너 평가·월간 운영 — PDF, 노션 발행) ⑭ 관리(템플릿 편집기·협정·프로파일·알림 규칙·posting_rules·AI 템플릿·결재선·사용자·검산 버튼[원장 vs 잔량/라인합 vs 헤더합/매트릭스 재계산/집계 vs 원장/**데이터 완성도 헬스체크** — HS 없는 SKU·**HS6 없는 자재**(S1-2 보강 — §6.3 CTC 전수 대조가 소비한다)·BOM 없는 판정 대상·MSDS 없는 DG·근거 미확인 템플릿·매핑 없는 활성 품목]). 전역 검색(전표·거래처·SKU·문서·인증번호). 한국어 UI: break-keep 기본, 좁은 셀·헤더·협정명·국가명 nowrap, 숫자·기준값 가운데 정렬.

> **용어 주의(S0-1 보강)**: 여기 ⑭의 "데이터 완성도 헬스체크"는 도메인 기능(HS 없는 SKU 등 결측 탐지, Phase 6)이다. 이와 별개로 인프라 헬스체크 엔드포인트 `/api/v1/system/healthz`(liveness)·`/readyz`(DB·마이그레이션 readiness)가 있다(§2 운영 품질, S0-1 구현). 두 "헬스체크"는 다른 것이니 혼동하지 않는다. [ADR-0003]

---

## 15. 자동화 레벨 맵

**3레벨** — L3 완전 자동(무인, 기록만) / L2 부분 자동(시스템이 전부 준비, 사람 1클릭) / L1 사람 판단. **L3 금지 영역(코드 고정)**: 지출·발주 확정, 법적 판정(HS·원산지·요건), 대외 최초 발송·협상·클레임, 장부 확정(분개·마감·전표·원장).

- **인바운드 무인 수신(L3)**: 전용 수신 메일함(IMAP 폴링, 발신자 화이트리스트)+감시 폴더 — 도착 문서가 사람 업로드 없이 스테이징까지 자동. inbound_rules(발신자/제목 패턴→문서종류·매핑·대상 추정), 미인식은 분류 대기함.
- **조건부 자동 확정**: ADR-09 예외 규정 그대로 — 결정적 파서 경로 한정, AI 추출은 무예외 사람 확정.
- **B/L draft 대조 무인화**: 수신→추출→대조 자동, 불일치 0이면 적합 확인 자동 기록+알림, 불일치 시 수정요청 초안 대기(L2).
- **정산 대사·환율 수집·월마감 준비·리포트 생성/게시(L3)**: 예외·잠금·검토만 사람.
- **정형 리마인드 자동 발송 화이트리스트**: outbound_policies(템플릿×상황) — 신규 내용 없는 정형 리마인드(RFQ 회신 D-1, 포괄확인서 재수취 1차, draft 회신 독촉 1차)만 승인된 템플릿으로 자동 발송. 협상·클레임·최초 컨택·2차 이상 독촉은 초안(L2).
- **알림→원클릭 액션**, **스케줄 레지스트리**(모든 배치를 scheduled_jobs로 관리 화면 노출, 실패 시 관리자 알림).

> **§15 보강(S2-3 PR-1 — 실행기)**: 스케줄 레지스트리가 실동작에 들어갔다(부채 #12 종결). **코드→함수 매핑은 코드 고정 폐쇄 열거**이고 등록되지 않은 code는 실행 거부 + 실패 기록이다 — 행 등록만으로 새 동작이 생기면 scheduled_jobs 한 줄이 곧 코드 배포이고, 이 문단의 L3 금지 4영역을 데이터로 우회하는 통로가 된다. `schedule`은 **폐쇄 2형**(`daily@HH:MM` KST · `interval@N` 분)이며 cron 파서 의존성을 두지 않는다. 다운 중 밀린 회차는 **1회로 수렴**하고, 비정상 종료로 남은 RUNNING은 **기동 시 FAILED로 정리 + 관리자 알림**이다(정상 종료 경로만 적으면 크래시에서 "실패 시 관리자 알림"이 미발화한다). 중복 기동은 PG advisory lock이 막고, 등록 경로는 CLI 하나이며 마이그레이션 시드는 항구 금지다. 화면은 목록 열람 + 켜기·끄기까지(관리자) — 스케줄 편집 통로는 없다. **"알림→원클릭 액션"은 이번에 부분 충족**이다(알림 행의 entity 링크로 상세 이동까지. 알림에서 액션을 직접 실행하는 완결형은 관찰 등재). [ADR-0044·0045]

---

## 16. 통합 허브 — 슬랙·노션·이메일

**구조**: 도메인 이벤트 발행(events, 아웃박스 §17.1) → webhook_subscriptions(이벤트×채널×포맷×대상) → 커넥터 어댑터 발송. 코어는 슬랙·노션을 모른다(ADR-07).
- **슬랙**: ① Outbound 웹훅(데일리 브리핑·이슈·정체·마감·절감 — 채널별 라우팅) ② Interactive 승인 버튼(승인/반려=시스템 승인 기록, Phase 7 이후) ③ 조회 커맨드 `/kbos SO-…`(읽기 전용 조회 함수 재사용, 쓰기 금지, Phase 7 이후).
- **노션**: 단방향 발행 원칙(리포트·경영 요약) — knowledge_notes만 양방향 라이트(충돌 시 시스템 우선+알림). SoR은 항상 본 시스템.
- **이메일 인바운드**: §15의 기반 — 첨부 검증(확장자·크기·해시).
- 난이도: 슬랙 웹훅·노션 발행·환율 수집 쉬움 / IMAP·자동 확정 규칙 중 / 슬랙 인터랙티브·UNI-PASS 중상.

---

## 17. 구현 안전 계약 (모든 Phase 공통 — repo CLAUDE.md가 본 장을 참조)

**17.1 트랜잭션**: 하나의 업무 동작 = 하나의 DB 트랜잭션(헤더+라인+원장+이력+이벤트 전부 커밋 or 전부 롤백). 트랜잭션 시작·종료는 서비스 레이어에서만. **트랜잭션 안에서 외부 호출 금지**(AI·HTTP·메일·슬랙) — 알림·연동은 같은 트랜잭션으로 events(아웃박스)에 기록 → 커밋 후 디스패처 발송(실패 재시도).

**17.2 동시성**: 낙관적 잠금 기본(전표 헤더·주요 마스터에 version, 충돌 시 409 "다른 사용자가 먼저 수정했습니다"). 잔량을 깨뜨리는 지점(재고 출고·할당 생성/소진·수주 잔량·여신 체크)만 행 잠금(SELECT FOR UPDATE) 또는 advisory lock으로 "확인→기록" 직렬화(방식은 Phase 4 ADR). 격리 수준 READ COMMITTED + 위 잠금 — 전면 SERIALIZABLE 금지.

**17.3 채번**: MAX+1 금지. doc_number_seq 행 잠금 증가(연도 리셋 수용) + 발급 번호 UNIQUE 이중 안전. 취소 건 번호 재사용 금지.

**17.4 멱등**: 멱등 키 전부 DB UNIQUE — (channel_id, 채널주문번호)/파일 해시/문서번호/(partner_id, 바이어 PO번호)/(시스템, 외부코드)/시리얼/(정산ID, 라인키). "코드에서 확인 후 INSERT"는 race로 뚫린다 — ON CONFLICT upsert. **soft delete 상호작용**: 멱등 UNIQUE는 부분 인덱스(`WHERE deleted_at IS NULL`) — 삭제 행이 재유입을 영구 차단하지 않되, 재유입은 부활이 아니라 신규(복원은 명시 액션). **쓰기 API 멱등 키**: 확정·생성 요청에 클라이언트 idempotency key(더블클릭·재시도 흡수, 동일 키 재수신 시 최초 결과 반환).

**17.5 불변의 DB 강제**: stock_movements·확정 journal·audit_log는 앱 DB 계정에 INSERT/SELECT만 GRANT(UPDATE/DELETE 권한 제거) 또는 금지 트리거. 가능한 불변식은 CHECK(수량≠0, 상태값, 기간 순서). 라인합=헤더합·차=대는 저장 시점 검증 + 야간 검산 이중망.

> **§17.5 확장(S2-2)**: 불변 대상에 **certification_status_log**(인증 상태 변경 이력 — §5.2 "이력 자동")를 추가한다. 명시 3종과 같은 수단(IMMUTABLE_TABLES 등재+revoke_mutations — INSERT/SELECT만)이고, 정정은 원본 수정이 아니라 새 전이 기록이다. 이후 "상태 변경 이력" 성격의 테이블은 같은 지위로 신설 세션이 등재한다. [S2-2 판정 안건 ④ — ADR-0040]

**17.6 실패·재시도**: 배치·임포트는 청크+실패 지점 기록, 재실행은 멱등 덕에 안전해야 한다. 외부 호출 타임아웃·지수 백오프·실패 알림 — 아웃박스 디스패처 동일.

> **§17.6 보강(S2-3 PR-1 — 디스패처 실장)**: 디스패처의 몫이 구체화됐다(부채 #11 종결). **클레임=처리=표식이 한 트랜잭션**이다(건당 1클레임 `FOR UPDATE SKIP LOCKED`) — 행 잠금의 수명이 트랜잭션이라, 배치로 잠그고 건별로 커밋하면 첫 커밋에서 나머지 잠금이 풀려 동시 디스패처와 이중 처리가 열린다. **실패 기록은 별도 트랜잭션**이다(같은 트랜잭션이면 롤백과 함께 사라져 같은 실패를 영원히 반복한다). 지수 백오프는 `events.next_retry_at`에 저장하고, **시도 상한 5회**에 닿으면 대기열에서 빠지며 관리자 알림이 1회 나간다 — 대기열 이탈을 `published_at`으로 적지 않는다(보내지 않은 것을 보냈다고 적으면 events가 발행 원장으로서 거짓이 된다). 매칭 규칙이 없는 이벤트도 published 처리한다(대기열 정체 방지). events는 삭제하지 않으며 재판정 트리거는 10만 행이다. [ADR-0043]

---

## 18. 크로스커팅 계약 (보안·환경·CI·성능 UX)

**18.1 보안**: 비밀번호 argon2/bcrypt, 로그인 연속 실패 잠금(5회), 세션 만료. **인가는 API에서 강제** — 모든 엔드포인트가 역할+소유권 검증(IDOR 차단), 원가·마진 마스킹도 API 응답 레벨. 전 쿼리 파라미터 바인딩, 업로드 MIME·크기 검증+실행 불가 경로+파일명 정규화(path traversal 차단), 다운로드 권한 검증. 외부 공유 링크 = 만료 토큰+읽기 전용+범위 최소+접근 로그. 로그인·권한변경은 audit 필수. **로그 마스킹**: 비밀번호·토큰·DSN·원가·마진 등 민감값은 로그에도 남지 않는다(화면에서 못 보는 값이 로그에 평문이면 통제가 우회된다) — 로거 프로세서 레벨에서 강제. 진단이 필요한 원가·집계 상세는 짧은 보존·접근통제된 별도 감사 채널로 분리. [S0-1 보강 — ADR-0003·0008]

**18.2 환경 분리**: dev/prod DB·.env·compose 완전 분리, 개발은 시드·픽스처(운영 데이터 복사 금지), 운영 반영은 마이그레이션+배포 절차로만.

**18.3 CI**: push마다 GitHub Actions가 전체 테스트+마이그레이션 드라이런, 실패 시 병합 금지 — 회귀를 기계가 잡는다. **마이그레이션 드라이런 정의**: 실 PG에 ① `alembic heads` 단일 ② 빈 DB→`upgrade head` ③ `downgrade base`→`upgrade head` 왕복 ④ `alembic check` 드리프트 0. **에러 응답 계약**: 봉투 `{error:{code,message,detail,request_id}}`, 코드는 `<도메인>.<대상>.<사유>` 3세그먼트, `request_id`로 사용자 신고와 로그를 연결. 사용자용 detail과 로그 전용 컨텍스트는 타입으로 분리(응답 직렬화 경로가 로그 컨텍스트에 접근 불가). 병합 차단은 GitHub Pro(개인 private 리포는 Free에서 rulesets 잠김) 필요. [S0-1 보강 — ADR-0003·0007·0010]

**18.4 성능·UX**: 전 목록 API 페이지네이션 기본 50(무페이지네이션 금지), N+1 금지(리뷰 항목), 느린 쿼리 로그. 전역 검색. 에러 메시지 이원화(사용자=한국어+원인+조치 / 로그=코드+상세), 빈 목록·로딩 표준 컴포넌트. 첨부 스토리지 용량 모니터링+임계 알림.

---

## 19. 빌드 로드맵 (각 Phase 완료 = 해당 테스트 그룹 통과 + 실데이터 리허설 1건)

```
Phase 0  기반(1~2세션): FastAPI+PostgreSQL+Docker(dev/prod), 마이그레이션(멱등 부분 인덱스 규율 §17.4 포함),
         역할 5종·담당자 라우팅, 작업 큐, external_refs·custom_fields·feature_flags,
         GitHub Actions CI(테스트+드라이런, 실패 시 병합 금지), SKU 등록→목록→CSV 끝-끝 관통
Phase 1  마스터(3세션): 제품/성분/자재BOM(재고·로트 플래그)/라벨/거래처/문서보관소 + 성분 스크리닝
         + 엑셀 프레임워크 코어(왕복 diff)
Phase 1.5 기존 엑셀 대장 이전(검산·병행 운영 — OPENING 기초이월 규칙 사용)
Phase 2  인증 엔진(3~4세션): 템플릿 편집기→인스턴스·상태머신→매트릭스→만료 알림→대행 협업
         + T1 템플릿(시드 투입은 진출 시장분만)  ← ★ MVP: 여기서부터 실사용
Phase 3  무역 실행(4세션): 상태 열거(§7.2) 구현 → QT→PI→SO(인테이크·게이트·보드)→선적·기일 엔진
         →CI/PL/S-I 생성→채권/입금(결제유형 분기) + 협정·판정·계산기·신고문안 + 비용 원장 코어 + 백오더
         + 승인 코어(여신·폐기·실사 게이트가 사용) + 휴일 캘린더 + 가용재고 산식(§8.3) 선적용
         + 임시 규칙: DG SKU 선적=수동 체크리스트 태스크, 통관 이슈=태스크+메모(P4·P5에서 정식 대체)
Phase 4  재고·로트(3~4세션): 착수 ADR 1건 확정(잔량 구조·할당 저장·직렬화 방식·사급 수율·마이너스 정책)
         → 원장 전 유형(§8.2)·할당·피킹/검수·FEFO·실사(위치 잠금)·ROP·리콜 추적 + RMA·클레임
         + DG 게이트 + 월 비용 마감 + 선적 캘린더 + 포괄확인서 루프·서명/법정 대장·재판정 트리거
Phase 5  채널(3~4세션): 풀필먼트 프로파일(FBA/Shopee/Qoo10)·채널입고 2단·수취/정산 대사(예외만 사람)
         + B/L draft 대조(무인 적합) + 통관 이슈 킷 + 요율 카드·RFQ 컴포저 + 사후검증·수입 사후적용 스캔
         ·FTA 관제탑 + 조사→템플릿 변환 + 이메일 인바운드·자동 확정 규칙(결정적 파서 한정)
Phase 6  지능·정산(3세션): AI 레이어 + 소싱 추천·파트너 비교·절감 보고서 + 회계 인터페이스·월마감 패키지
         + 환노출 + 슬랙 웹훅·리포트 자동 게시·데일리 브리핑 + KPI + 출시 프로젝트 뷰
Phase 7~ 확장(="문서 내 v2" 항목): 지식→기능화 파이프라인, 이카운트 API·UNI-PASS(마일스톤 자동 실적),
         SMTP 화이트리스트 자동 발송, 슬랙 인터랙티브·조회 커맨드, 노션 발행·지식 양방향,
         대행사·바이어 외부 링크, 효능 표방 라이브러리, REWORK 이동유형(백로그)
```
> **담당자 라우팅의 위치(S0-2 보강)**: Phase 0의 "담당자 라우팅"은 **구조**(담당자 컬럼 — `tasks.assignee_id`, `alert_rules`의 수신 대상)까지다. 이벤트→수신자 결정→채널 발송이라는 **동작**(ADR-07 "전사 폭포 금지")은 notification_channels·webhook_subscriptions·알림센터가 서는 **Phase 2(S2-3)**가 담당한다 — Phase 0에는 발송 채널이 없어 라우팅을 구현해도 §20 H로 검증할 수단이 없다. [ADR-0012]

AI가 Phase 6인 이유 — 데이터가 쌓인 위에서 가치가 나며, 스테이징 구조는 Phase 1부터 깔려 있어 그대로 꽂힌다.

---

## 20. 테스트 시나리오 (그룹 A~K, Phase 매핑: P0=J·K골격 / P1=F·K / P2=C·H / P3=A·B·E / P4=A·C·D·J / P5=B·D·I / P6=E·F·G·H / P7=I잔여)

> **S1-2 매핑(보강)**: P1=F·K 중 F(엑셀 왕복·연동)는 전부 S1-3 몫이라 S1-2에 F 항목이 없다. S1-2 케이스는 성분 규칙·스크리닝·자재 BOM 원산지·라벨=**C**, BOM 단가 마스킹=**G**(아래 G "조회 역할 원가 마스킹"), 멱등=**J**, 마이그레이션·아키텍처·제약=**K**로 배정한다. [웹 세션 판정 2026-07-30 / ADR-0022]

**A. 전표·정합**: 부분선적 잔량 0·초과 거부 / 확정 건 환율·단가 불변 / 역방향 정정 후 잔액 복원(후속 생존 시 선행 취소 차단 포함) / 세트 ASSEMBLY: 구성품 차감·세트 로트 생성·유통기한=MIN / 기초 OPENING 이월 검산 / PI 일부입금 SO 게이트(선수금 T/T만 활성) / 중복 바이어 PO 0건 / 월 마감(전기일 기준) 후 직접 수정 거부
**B. 서류**: CI↔PL 교차 일치·G.W.<N.W. 거부 / 무상(금액 0) 생성 / 한글·장문·BOM CSV / B/L draft 불일치 5종 검출·불일치 0 자동 적합 / DGD·MSDS 게이트 / 신고문안 자구 일치·6,000유로 게이트 / 검수 미완료 선적의 CI·PL 생성 차단
**C. 인증·원산지**: 비정상 전이 거부·만료임박 자동 / 매트릭스 즉시 반영(집계 미저장) / CTC 전수(1개 미충족=전체 불충족) / RVC 경계 플래그·환율 재계산 경고 / 확인서 만료→역외 강등→재검토 전파 / 재검토 상태 C/O 차단 / 스냅샷(hs_version 포함) 불변 / 검증 케이스 파일 완전성 / 사후적용 기한 정확
**D. 재고·채널**: FEFO·채널 최소기한 차단(프로파일 기본+shelf_rules 예외 우선) / 실사 위치 잠금·스냅샷 정확성 / 채널 주문 멱등 / 정산 미매칭 검출 / 리콜 경로 재구성 / 프로파일 교체만으로 3채널 입고 동작 / 채널입고 2단: 미수취 잔량=운송중 위치, 차이=RECEIPT_DIFF / 피킹과 다른 로트 검수 입력 차단·검수 수량 차이 시 원장 0 / BOX 입력 환산·원장 EA 단일 / 판매불가 반품=격리 이동·가용 제외 / 사급 TRF→PROD_CONSUME 대사 / 시리얼 flag on·off
**E. 비용·소싱**: CBM 경계값·최소요금 / 견적-청구 차이·이중 청구 차단 / Incoterms 부담자 / 기준선 3방식 검증·보고서 명기 / 권고 외 선택 사유 필수 / DG 미취급 파트너 제외 / 체류비용 일할 / 환노출 검산
**F. 회계·연동**: 차대변 불일치 생성 거부 / 매핑 누락 내보내기 차단 / 왕복 diff 3행만 반영·ID 훼손 오류 / 폐기→손실 분개 금액 일치 / 재고자산=lot_costs×수량 검산
**G. AI·보안**: AI DB 직행 불가 / 첨부 인젝션 무력 / 파일 해시 멱등 / 월 한도 차단 / 근거 링크 없는 템플릿 확정 차단 / 조회 역할 원가 마스킹
**H. 운영**: 동시 20명 벌크 경합 정합 / 야간 집계 vs 원장 일치 / 승인 우회 차단·승인 후 불변·결재선 매핑·대결 기간 유효+이력 / 백오더 가용일 자동 추종 / 정체 N일 독촉 / 확인 알림 재발송 0·에스컬레이션 발동 / 담당 일괄 이관 후 라우팅 즉시 반영 / DB+파일 세트 복원 리허설 / 헬스체크 결측 5종 검출 / 기능 플래그 오프 완전 비활성
**I. 자동화·통합**: 인바운드 화이트리스트 외 격리 / 자동 확정 조건 1개 미충족 시 대기·AI 추출 자동 확정 불가 / 화이트리스트 외 자동 발송 차단 / 슬랙 승인=시스템 기록 일치 / 노션 발행 실패 재시도·알림 / 스케줄 실패 감지 / 조회 커맨드 쓰기 불가
**J. 안전 계약**: 재고 10에 동시 출고 7+7 → 1건 거부 / 확정 더블클릭 → 전표 1건 / 채번 동시 100건 중복 0 / 트랜잭션 중간 실패 완전 롤백 / 앱 계정 원장 UPDATE → DB 거부 / version 충돌 409 / 아웃박스: 커밋 실패 시 발송 0·발송 실패 재시도 / 동일 임포트 재실행 변화 0 / soft delete 후 동일 키 재유입=신규
**K. 보안·품질**: 타 사용자 전표 URL 403 / 로그인 5회 잠금 / 업로드 실행 불가·경로 탈출 차단 / 공유 링크 만료 거부 / 전 목록 페이지네이션 자동 스캔 / 로그 마스킹 / CI 실패 병합 차단 / L/C 제시기한 MIN·tolerance 상하한

---

## 21. 운영·리스크 원칙

규제·세율·판정은 시스템이 단정하지 않는다(근거링크+확인일+분기 재확인 태스크, 중요 건 관세사 확인 문구). 백업은 DB+파일 세트 복원 리허설까지가 백업. 범위 압박(회계 기표·자동 판정·실시간 API·선박 추적)은 ADR과 §1 비범위 근거로 거절. 관리 메뉴 검산·헬스체크 버튼으로 사용자 스스로 건강검진. **장애 운영 수칙(SOP, Phase 2 작성)**: 다운 중 실물 발생분은 수기 표준 양식 기록 → 복구 후 소급 입력(증빙일=실제 발생일·입력일=복구일 — 날짜 3종이 수용) + 검산 1회 의무. 변경마다 ADR 5줄, repo CLAUDE.md가 본 설계서(특히 §17·§18·§22)를 참조.

---

## 22. 변경 검증 11렌즈 — Definition of Done

**모든 기능 추가·변경(§13.2 기능화 파이프라인 포함)은 아래 11렌즈를 통과해야 완료다.** 작업 지시서 템플릿과 PR 체크리스트에 내장.

1. 기능 — 요구·수용 기준 충족, 요청 항목 누락 0
2. 데이터 — 마스터/트랜잭션 구분, 스냅샷·불변·채번·soft delete 규율(§2)
3. 트랜잭션 — 업무 동작=단일 트랜잭션, 외부 호출은 아웃박스(§17.1)
4. 동시성·멱등 — version/잠금/UNIQUE/idempotency key 검토(§17.2~4)
5. 보안·권한 — API 레벨 역할·소유권, 입력·파일 검증(§18.1)
6. 시간 — UTC 저장·KST 표시·현지 병기·전기일 규율
7. 성능 — 페이지네이션·N+1·인덱스·집계 영향(§18.4)
8. 테스트 — 해당 그룹(A~K)에 케이스 추가, CI 통과
9. 운영 — 로그·알림·스케줄 레지스트리·백업 영향, 설정화 가능분은 데이터로(ADR-11)
10. 문서 — 설계서 해당 절 갱신, 되돌리기 어려운 결정은 ADR 5줄
11. 워크스루 — 변경이 걸린 실무 시나리오를 입구부터 출구까지 1회 관통(단계 누락은 관통 검증에서 잡힌다)

렌즈 미통과 항목은 "완료"가 아니라 "부채"로 feature_requests에 사유와 함께 기록 — 조용한 누락 금지.
